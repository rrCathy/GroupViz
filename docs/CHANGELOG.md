# 变更记录 (Changelog)

> 所属文档集：GroupViz 开发文档。入口见 [AGENTS.md](../AGENTS.md)。
> 本文档沉淀**已完成**的功能里程碑与逐次开发记录（2026-08-15 由 ROADMAP §5 决策记录表与已完成规划章节整体迁入）；规划性内容（未做事项、决策）见 [ROADMAP.md](ROADMAP.md)。

## 1. 已完成里程碑

### 1.1 群作用系统（P0）— ✅ 已完成（v1.8）

当前最大的数学缺口：已有陪集、同态、商群，但缺少"作用"这一统一框架。现已补齐统一框架。

- **数学内容**：任意有限群 G 作用在集合 X 上；轨道 Orb(x)、稳定子 Stab(x)；轨道-稳定子定理 `|Orb(x)|·|Stab(x)| = |G|`；Burnside 引理（可选）。
- **作用来源**：群在陪集空间 G/H 上的左乘作用；群在自身上的平移/共轭作用；自定义作用（按元素指定置换）；共轭/正则/陪集/自定义/Sylow 五来源（Sylow 属 §1.2）。
- **可视化**：轨道视图（轨道划分、代表元、轨道-稳定子定理数值自检）、Schreier 图（生成元作用图）、稳定子结构与共轭稳定子展示。
- **工程**：`actions` 领域 Provider；核心逻辑 `core/algebra/actions.ts` 纯函数 + 单测；i18n zh/en 成对补键；与陪集条带（cosetstrip）、同态/置换表示视图打通。
- 几何顶点集置换作用：v1.8 曾开发后移除（视觉不佳；正确路径 = 按元素阶在几何体上找 n 次轴并组织各轴，联系空间群/点群 DLC，暂缓，见 docs/ACTIONS.md §4）。

### 1.2 Sylow 定理可视化（P0）— ✅ 已完成（2026-08-06）

- **数学内容**：Sylow p-子群查找——p-元素 → 循环 p-子群 → pair-join 闭包闭合，本地算到 S₅=120（SYLOW_MAX_ORDER=240 守卫）；逐素数验证计数定理 `n_p ≡ 1 (mod p)`、`n_p | m`（`|G| = p^k·m`）；共轭性（所有 Sylow p-子群互为共轭）。
- **可视化**：Sylow 型视图（SylowView.tsx，第 11 种视图模式）——元素节点 + p 可选素数；凯莱图布局（点击子群边切换为子群生成元作用）；单选 → 陪集条带（Lagrange）；Ctrl/⌘/⊕ 复选两个子群 → 上下布局 + 竖直共轭箭头（Sylow 第二定理）+ 子群内部生成元边（P 青 / Q 紫）；轨道视图验证（G 共轭作用在 Syl_p(G) 上，轨道大小 = n_p，稳定子 = 正规化子）。
- **收尾**：欢迎页"即将推出"列表已移除 Sylow 项；Sylow 概览面板（OperationsPanel 第 5 tab）与子群格高亮联动已被视图取代并移除。

### 1.3 数学缺口补全（M1–M4、M8）— ✅ 已完成（2026-08-10）

- **M1 正则作用 + 陪集作用**（✅ 2026-08-10）：左平移作用 = Cayley 定理可视化（G ≅ Sym(G) 的嵌入，正则表示视角）；G ↷ G/H 传递作用（稳定子 = 共轭 H）；与轨道-稳定子面板打通；types/actions/context/panel/ActionView/RightPanel/i18n + 11 单测（actions.test.ts）。
- **M2 GL(2,p)**：纯 TS 2×2 矩阵模 p 实现（无需 GAP 数据）；GL(2,2) ≅ S₃、GL(2,3) = 48 阶（|SL(2,3)| = 24）；面板/按阶创建/groupFactory 接入（`core/groups/GeneralLinearGroup.ts` + generalLinearGroup.test.ts）。
- **M3 作用深化三小件**（✅ 2026-08-10）：共轭作用特化标注（固定点 = Z(G) 自检，轨道=共轭类/Stab=C_G(x)）；Burnside 引理自检（|X/G| = (1/|G|)·Σ|Fix(g)|，右侧通用行）；Sylow 作用面板入口（p 素数下拉 + 按钮，createSylowAction 接线）；computeBurnsideCount + 右侧固定点/Burnside 标注行 + 面板 Sylow 行 + 7 单测。
- **M4 半直积分解展示（识别方向）**（✅ 2026-08-10）：findSemidirectDecompositions（order≤60 守卫，去重/排序 verified 优先）+ buildSubgroupGroup/minimalGenerators helper + verified 双通道（isoSymbol 相等 || 不变量回退：isAbelian + 元素阶多重集）+ 面板分解列表（✓/✗ 徽标、点击切换）+ 分裂短正合列 TeX + 14 单测（semidirectDecompositions.test.ts，共 23）；S₄ 双型分解（A₄⋊C₂ / V₄⋊S₃）、D₁₂ 19 候选、Q₈ 无分解均验证。
- **M8 群族识别升级（部分完成，2026-08-10）**：subgroups.ts 的 detectIsomorphicGroup 阿贝尔分支升级为**有限阿贝尔分类定理精确识别**（detectAbelianType：因子链 d1|d2|…|dk 枚举 + 公式阶分布匹配 Σ Π φ(eᵢ)，纯算术无构造群、支持任意因子数 C₂×C₂×C₄ 等）；非阿贝尔分支保留 Dₙ/Q₈/A₄/A₅/S₃/S₄/S₅ 候选；8 处调用方（商群/Aut/半直积/M4 verified/展示群）自动受益；6 单测；gappy IdGroup 砍除（E1 未做）。

### 1.4 支撑项与质量 — ✅ 已完成

- **群结构分析升级**：换位子群 [G,G]、子群列（导列/上·下中心列/合成列）、可解/幂零判定——见 core/algebra/series.ts（SERIES_MAX_ORDER=240 守卫，大群二期走后端）。
- **群族扩充**：小群注册表扩至阶 1-31 全部 93 个群（GAP 4.16 SmallGroups 导入）；GL(2,p) 单独交付（§1.3 M2）。
- **质量**：覆盖率 75.8% → 85%+（2026-08-09：88.64% stmts / 91.18% lines，35 文件 706 tests；2026-08-10：88.92% stmts / 91.34% lines，37 文件 763 tests；2026-08-13：88.98% stmts / 91.82% lines；2026-08-15 起 vitest.config 设 CI 阈值 stmts/lines/funcs ≥ 85、branches ≥ 70，实测 88.56% stmts / 91.49% lines）；后端计算与前端本地兜底功能对齐（S₆ 全通）。

## 2. 逐次开发记录

> 原 ROADMAP §5 决策记录表整体迁入（含决策型与工作型记录）。后续每次开发提交在本表追加一行。

| 日期 | 记录 |
|------|------|
| 2026-08-06 | Sylow 定理可视化落地：core/algebra/sylow.ts 专用查找算法 + Sylow 型视图（元素节点/凯莱图/陪集条带/共轭双选/子群边）+ ActionView Sylow 共轭作用验证；28 文件 542 tests 全绿；版本 1.9.0 |
| 2026-08-07 | 群展示系统落地：core/algebra/presentations.ts（解析器 + Todd–Coxeter 陪集枚举 TC_MAX_COSETS=3000 + 构建 + presentationOf 标准展示分发 + 通用关系发现器）、左侧「群展示」面板（8 预设/防抖预览/持久化+草稿）、关系回路视图（Van Kampen 图 + 关系动画）、展示乘法表视图（单词求值路径）；32 文件 630 tests 全绿；版本 1.10.0 |
| 2026-08-09 | 测试补齐：覆盖率 84.02% → 88.64% stmts（91.18% lines），新增 export.test.ts（15 tests，GIF 编码/SVG/canvas 导出全路径）、exportApi.test.ts（16 tests，导出桥单例 + DOM stub）、utils.test.ts 补 9 用例（groupFactory 嵌套直积/上标幂/回退/越界）；修复 cayleyTree genElsOverride 测试 coverage 下 5s 超时（30s）；35 文件 695 tests 全绿 |
| 2026-08-09 | 小群全量导入：GAP 4.16 SmallGroups 库导出阶 1-31 全部 93 个群（smallGroupData.ts 乘法表数据 + createTableGroup 构建 + structureToSymbol 符号化，结构冲突回退 SmallGroup(n,i)，Dic₃ 强制 Z₃:C₄）；按阶创建面板改为注册表驱动（阶 1-31）；createGroupFromSymbol 支持 D₈-D₁₅/新符号/SmallGroup(n,i)/注册表兜底（会话恢复兼容）；35 文件 704 tests 全绿 |
| 2026-08-09 | 按阶创建面板补回 A₅(60)/S₅(120)（注册表驱动化时丢失回归）；符号 ":" 半直积提示（order-hint 文案，zh/en）；35 文件 706 tests 全绿 |
| 2026-08-10 | 半直积分解（M4）落地：core/algebra/semidirectDecompositions.ts（findSemidirectDecompositions order≤60 守卫 + buildSubgroupGroup/minimalGenerators + verified 双通道）；面板分解列表 + 分裂短正合列 + 点击切换；14 新单测（共 23）；37 文件 763 tests 全绿 |
| 2026-08-10 | M5 第二/第三同构定理验证开发完成并实测通过（38 文件 780 tests），随后**决定移除**：纯数值验证、无可视化价值，不影响本项目；同步明确后续任务筛选标准 = 新建可视化或可视化优化，非可视化任务（M6/M7/M8/E2/P1/P2/P3）不做；群论计算器/多对象工作台方向搁置，作为独立新引擎另行规划 |
| 2026-08-10 | M8 群族识别升级（部分完成）：subgroups.ts detectIsomorphicGroup 阿贝尔分支升级为有限阿贝尔分类定理精确识别（detectAbelianType：因子链枚举 + 公式阶分布 Σ Π φ(eᵢ)，支持任意因子数）；旧实现仅支持 2 因子直积，现 C₂×C₂×C₄ 等全部可识别；8 处调用方自动受益；6 新单测（38 文件 775 tests 全绿）；gappy IdGroup 砍除（E1 未做） |
| 2026-08-09 | 规划定稿（覆盖此前"仅口头确认、不更新 ROADMAP"的决定）：近期 = 数学缺口补全 M1–M8 + 工程质量 E1（gappy 后端集成）/E2（细节收尾）+ FGVE 预铺路 P1（群 JSON 导出+round-trip，乘法表导入否决）/P2（引擎化审计）/P3（benchmark）；推进顺序 M1→M8→E1→E2→P1→P3 |
| 2026-08-09 | 表示论可视化定稿：特征标表 = 中期正式交付（经典矩阵表 + 热力着色 + 正交性验证 + 共轭类联动，数据源 gappy CharacterTable/Irr/ConjugacyClasses，见 ROADMAP §2.4）；矩阵表示动画 → GVL（ROADMAP §3.6）；一维特征标并入特征标表（近期暂缓） |
| 2026-08-11 | 直积 2D 形状（B3 可视化优化）：classifyDirectProduct2D 智能分类（全循环→grid、恰一循环因子→cylinder、无循环→torus）；cylinderLayout2D 同心多层环（循环因子径向层叠 + stagger 错位 + Dₙ 双环副本）、torusLayout2D 主轴环挂副本；factorPipeGroups/parseCompactFactors 紧凑符号因子识别（C₂²×S₃ = 2 组、S₃² 幂合并拆段修复）；buildFactorSubgroup 因子临时群重建；13 新单测；38 文件 788 tests 全绿 |
| 2026-08-11 | 16 阶注册表群直积形状修复：①tableGroupGridFactors 聚类算法（union-find 生成元不可交换聚类 + 混合进制枚举）覆盖注册表群（非 pipe），C₄×C₄/Z₄×Z₂×Z₂/C₂⁴ 全部 4×4 满网格（修复 colMap 只存代表元素致 L 形重叠）；②hasTopLevelTimes 顶层 \\times 检测——'(Z₄×Z₂):Z₂' 半直积不再误判直积，恢复圆形布局；③parseProductFactors pipe 分支支持 3+ 因子平均拆分；④getAvailableShapesForView 直积去重（classify=grid 时不重复 push，修复 React key 冲突）；6 新单测；38 文件 794 tests 全绿 |
| 2026-08-11 | 注册表非 pipe 直积 cylinder 聚类支持（B3 延续）：clusterFactorGroups/tableGroupFactorSplit/clusterIsCyclic 按生成元交换性聚类因子 + 循环性判定，buildFactorSubgroup/cylinderLayout2D 加非 pipe 分支，Z₂×D₄/Z₂×Q₈ 显示 2 层同心环（distinct 半径 2）；classifyDirectProduct2D 移除 isPipeProduct 守卫改 isGroupDirectProduct 防御；7 新单测；38 文件 801 tests 全绿 |
| 2026-08-11 | 2D 凯莱图通用策略（直积归组 + 任意因子数甜甜圈）：analyzeDPFactorsGrouped2D/factorPipeGroupsGrouped 相邻同底循环因子归组（C₂×C₂×S₃ → [C₂², S₃]、C₂×C₂×C₂ 合并 C₂³ 单因子退回网格）；classifyDirectProduct2D 新逻辑（单因子→grid、全循环→grid、全非循环→torus 不限因子数、否则 cylinder）；torusLayout2D 重写为嵌套甜甜圈（递归半径链，主轴环 + 每点挂剩余因子乘积副本），注册表群 clusterFactorGroups 聚类笛卡尔分解；cylinderLayout2D 归组索引支持；17 新单测；38 文件 818 tests 全绿 |
| 2026-08-11 | 半直积重布线形状（策略 2）：isGroupSemidirectProduct 识别注册表 ':' 记号（hasTopLevelColon 顶层检测）；getSemidirectProductMeta 经 findSemidirectDecompositions 规范分解恢复 N/H/φ；semidirectFactorMap 每元素唯一分解 g=n·h（pipe id 拆分 / 注册表 n=g·h⁻¹ 代数分解）+ semidirectFixedPoints 固定点高亮（GroupCanvas 生效）；注册表半直积群默认重布线形状；9 新单测；38 文件 827 tests 全绿 |
| 2026-08-11 | 半直积重布线美观：powerRingOrder 生成元幂序 BFS（循环群正多边形/V₄ 方形环序/bit 向量特判/覆盖不全回退字典序，生成元边旋转对称免交叉）；rN/rH 自适应（rN = max(minRN·1.6, R·0.14)、rH = max(minRH·1.6, (rN+28+copyGap/2)/sin(π/m))、copyGap = max(90, rN·1.35)、R = minDim·0.32、minRN/minRH = 阶·56/2π）防节点重叠 + 副本盘缘间隙 ≥90px；5 新单测；38 文件 832 tests 全绿 |
| 2026-08-12 | 2D 凯莱图布局修复第二轮（按阶创建注册表路径）：注册表生成元标准化（createTableGroup buildGenerators D_m 重建 (r,s) 生成对）；ringOrder 数字感知 gN 环序（C16 圆环连续）；semidirectProductLayout 盘内 dihedralSnakeOrder 蛇形环序（(C4xC2):C2 盘内边整齐）；6 新单测；38 文件 862 tests 全绿 |
| 2026-08-12 | 2D 凯莱图布局修复四连：①循环群默认 circular（C₁₆ 等注册表循环群不再螺旋乱排，spiral 仅手动可选）；②powerRingOrder Cₘ×C₂ pipe 特判（(C₄×C₂):C₂ 半直积盘内环序：外圈 t0 行升序 + 内圈 t1 行降序，生成元边相邻免跳对角）；③注册表 Dₙ 双环（splitDihedralElements 元素阶分类：m=|G|/2 找阶 m 元素 r → 旋转=⟨r⟩ 幂闭包、反射=其余、配对 sᵢ=rⁱ·s₀；dualRingLayout/cayleyCircleLayout value 分类失败自动回退，按阶创建 16-30 阶二面体 D₈/D₉/D₁₀ 双环恢复正常）；④cylinderLayout2D 去掉 stagger 各层同相位（C₃×S₃ 循环因子边成径向母线，俯视圆柱感）；24 新单测；38 文件 856 tests 全绿 |
| 2026-08-13 | GAP 表群可视化完整处理（批量导入准备）：①表群元素词标签（assignWordLabels BFS 最短词 + D_m applyDihedralNormalForm 正规形，恒等元 e/词 a、a b、a^{2} b，生成元元素标签=生成元名，同态/陪集/循环布局 label 匹配对齐，id 不变）；②tableFactorSearch 布局兜底（聚类失败/退化时按群符号从 findAllSubgroups 搜因子：阶+循环性匹配+有序乘积覆盖验证，cylinder/torus/网格全接上——C₃×S₃/C₂×A₄/C₅×S₃ 同心多层环、C₄×C₂×C₂ 4 层×4 节点；圆形兜底 gN 表群 id → powerRingOrder 生成元幂序，C₁₆ 恢复正多边形）；③tableGroups.audit.test.ts 惰性审计（279 tests 扫全部 66 表群：默认/全部形状/圆形/生成元边/半直积 meta，防布局静默回退回归）；39 文件 1174 tests 全绿（覆盖率 88.98% stmts / 91.82% lines） |
| 2026-08-14 | 群展示发现器极小化展示：canonicalCyclicForm 循环旋转/逆折叠（w=e 蕴含旋转与逆也是关系，QD16 868 条共轭冗余词折叠为少数规范词）+ 贪心移除（保护 K 条最短生成元阶关系，其余词 TC 验证逐个移除，1.5s 预算守卫）；**presentationOf 对 QD16 特判返回标准展示 ⟨a⁸, b², bab=a³⟩**（按元素阶定位阶 8 生成元 a，GAP 生成元 g1 阶 4 只能导出非标准 ⟨a⁴, b², (ba)⁴⟩）+ **发现器结果缓存**（symbol|order|生成元数键，信息栏/树视图反复调用不再重算 ~1.6s，QD16 加载 1.6s→64ms）；测试断言补 QD16 标准形式与缓存复用；39 文件 1174 tests 全绿 |
| 2026-08-14 | QD16 命名半直积走重布线：isNamedRewiringGroup 命名特判（符号无 ':' 但结构为半直积，不影响全局 isGroupSemidirectProduct）；getSemidirectProductMeta 经 namedSemidirectOrderPair 恢复 C₈⋊C₂ 分解（generators.length===1 锁定循环 C₈ 而非 Q₈，φ(b)(a)=a³ 实测验证），默认 2D 形状 rewiring、可用 [rewiring,circular,spherical,concentric]；semidirectProduct.test.ts 新增 QD16 meta 恢复测试；浏览器实测凯莱图默认选中重布线；39 文件 1175 tests 全绿 |
| 2026-08-14 | QD16 生成元标准化 (a,b)：GAP 生成元 g1 阶 4（=标准 a²）导致重布线内环只有 4 元环；buildGenerators 新增 QD16 特判按元素阶定位阶 8 元素 a + 阶 2 元素 b（bab=a³ 且闭包=全群）替换 rec.gens；重布线双盘各显完整 8 元环（a 边 16 条 / b 边 8 条浏览器实测）；smallGroups.test.ts 新增标准化断言 + 非二面体群保持 GAP 生成元用例收紧；39 文件 1176 tests 全绿 |
| 2026-08-14 | SmallGroup(16,13)=(C₄×C₂):C₂ 命名半直积走重布线（参考 Group Explorer GroupExplorer.html，GE 标准展示 ⟨a,b,c | a⁴=b²=c²=1, cbc=ba², ab=ba, ac=ca⟩）：isNamedRewiringGroup 特判扩展到 'SmallGroup(16,13)'（符号冲突回退无 ':'，startsWith('S') 原本误判 projection3D → getDefaultShape2D/getAvailableShapesForView 的 isSA 前缀判定排除 'SmallGroup('）；getSemidirectProductMeta byNamed 改用 normal.isAbelian 锁定交换 N=C₄×C₂（排除同阶 Q₈），φ(c)(b)=b·a² 实测验证；buildGenerators 按 rec.n===16&&rec.i===13 特判标准化为 GE (a,b,c)（a 阶4、b 阶2與a交換、c 阶2 且 cbc=ba²、closureSize 闭包=16），GAP gens [2,3,4] 阶 2,2,4 → 标准三元；浏览器实测：默认重布线、3 生成元 a/b/c、16 节点、a 边 16/b 边 8/c 边 8（双盘 C₄×C₂⋊C₂，与 GE 一致）；smallGroups.test.ts 新增 (16,13) 标准化断言（ab=ba、ac=ca、cbc=ba²、闭包 16）；39 文件 1176 tests 全绿 |
| 2026-08-14 | Q₁₆ (16,9) 凯莱图生成元标准化 (a,b) 对齐 Group Explorer Q_8.group（GE 下标惯例 Q_阶/2，标准展示 ⟨a,b | a⁸=1, a⁴=b², aba=b⟩，gens "1 8"→a 阶 8 / b 阶 4）：buildGenerators 按 rec.structure==='Q16' 特判（a=阶 8 元素、b=阶 4 元素满足 b²=a⁴ 且 aba=b 且 ⟨a,b⟩=全群），代替 GAP gens [2,3] 两个阶 4 元素；浏览器实测 2D circular 凯莱图 a 边 16（8 元环双向）+ b 边 16；QD16/(16,13) 3D 走 semidirectCylinder（isNamedRewiringGroup 引入 getAvailableShapes3D/getDefaultLayout3D，layout3D.ts semidirectCylinder 已支持命名特判，浏览器实测 QD16 3D 默认 semidirectCylinder）；smallGroups.test.ts 新增 Q₁₆ 标准化断言（a 阶8/b 阶4、b²=a⁴、aba=b、闭包 16）；39 文件 1178 tests 全绿 |
| 2026-08-15 | 凯莱图形状分配 11 项审计修正：①projection3D 收窄为精确群族（isProjection3DGroup：仅 S₃/S₄/S₅/A₄/A₅，删除 startsWith('S'/'A') 泛化误配）；②archimedean/concentric 从所有可用形状列表移除（非循环群/半直积群不再提供，布局函数与 i18n 键保留）；③ringGrid 推广到任意素数网格（findRingGridDecomposition p∈{2,3}：C₃³=(27,5) 与幂式 'C_{3}^{3}' 3×3 三角环网格；C₂⁴ 无 4 阶环生成元数学上不命中保持 4×4 grid）；④SmallGroup(n,i) 符号冲突类群走重布线（isNamedRewiringGroup 类级放宽 order≤60 && startsWith('SmallGroup(')，SmallGroup(20,3)≅C₅⋊C₄ 经 getSemidirectProductMeta 结构恢复 N/H/φ）；⑤半直积 3D 可用形状收窄为 [spherical,semidirectCylinder,circular]（删依赖直积因子结构、失败静默球化的 lattice/torus）；⑥重布线 φ(h) 扭转（N 环按 φ(h)(n) 索引摆放，QD16(k=3)/C₈:C₂(k=5)/D16(k=7) 辐条连接模式互异）；⑦C₂⁴ 3D 超立方体（isC2Tesseract + hypercube 布局：外立方体×内立方体 r=0.55 同心投影 16 顶点，3D 默认 hypercube）；⑧presentations.ts tableFingerprint 修复（GroupElement.id 为 string 致 TS2363，改 FNV-1a 字符串哈希）；13 新单测；39 文件 1195 tests 全绿（覆盖率 88.98% stmts / 91.82% lines） |
| 2026-08-15 | 3D 轨道控制换成自定义球坐标（Cayley3DView 删除 drei OrbitControls——其每帧 makeSafe 把 phi 钳回 [EPS, π-EPS]，是旧版『上下有角度限制』的根因，▶ 自动旋转先以 enabled=false 绕过、手动拖拽仍被钳，用户反馈后整体替换）：orbit ref 存 theta/phi/radius/target，phi 无界可无限翻越上下两极点；手动拖拽（左键 theta-=dx·0.006 / phi-=dy·0.006、右键平移 target、滚轮缩放 radius 钳 [3,25]）与 ▶ 自动旋转（沿用最后一次拖拽方向与速率约定）共用同一套状态；每帧 position = target + r·sinφ·(sinθ, cotφ 隐式, cosθ)，up=sign(sinφ) 在两极点翻转保持画面正立；resetCamera/dblclick 复位默认视角；浏览器实测（C₂⁴ hypercube）：下拖穿北极 φ→−0.114、上拖穿南极 φ→4.476、▶ 绕行 5 圈零钳制，up 随 sinφ 正确翻转；门禁 lint/test 39 文件 1195 tests/build 全绿；SymmetryView/FreeGroupTreeView 的 OrbitControls 保持不动 |
| 2026-08-15 | 3D 凯莱图 GIF 导出（ViewPanel 导出区 3d 视图新增「GIF 旋转时长」select 3s/5 圈 + 导出 GIF 按钮）：`src/utils/cayley3dControls.ts` 新增注册桥（register/get/unregister，Cayley3DControlAPI：isReady/snapshotOrbit/beginRotation(radPerSec)/endRotation(snapshot)），Cayley3DView SceneContent 注册（仅主视口 .canvas-viewport 实例，useFrame 外部旋转分支优先于 autoRotate，delta 增量），export.ts 新增 cayley3DExportPlan 纯函数（3s/2 圈 60 帧、5 圈 7.5s 150 帧、20fps 可配）与 exportCayley3DGif（逐帧 rAF 离屏 drawImage → quantize 256 → gifenc，异常 reject + finally 恢复相机）；i18n panel.gifOption/gif3s/gif5Cycles；export.test.ts +10 tests（exportCayley3DGif/cayley3DExportPlan/cayley3dControls）；39 文件 1205 tests 全绿；浏览器实测 C₂⁴ hypercube：3s 导出 1.6MB 60 帧、5 圈 4.0MB 150 帧、导出期间采样哈希持续变化（旋转生效）、导出后恢复原视角、0 console error |
| 2026-08-15 | 乘法表大群三策略（TableView：|G|>16 顶部策略栏，默认子群展示）：子群展示 = 随机挑 ≤16 阶子群优先 6-12 阶（合成/循环/`findAllSubgroups`（≤60 阶）多源候选去重、无合适回落随机），随机展示 = 随机挑 6-12 元素，全量展示 = |G|>30 弹确认框 → 全屏模态（fixed 全视口覆盖两侧栏/工具栏）完整 n×n 表，右上角 −/+/⟳ + 缩放滑块 25%-600%（视口锚定）+ 导出 SVG + 退出（ESC 重置为子群展示）；全屏内滚轮=纯上下滚动、拖动平移（React onWheel 被动无法 preventDefault，改原生滚轮滚动 + 滑块缩放分离交互）；主视图画布级平移/缩放 `translate(T) scale(S) translate(offset)` 变换链（滚轮=缩放、双击空白复位，修复子群/随机展示不能缩放移动）；修复行表头标签双重位移错位（<g> translate + text y 均含 ri·cellSize）；i18n table.strategy.*/table.subgroupCaption/table.randomCaption/table.fullConfirmTitle/Msg/table.confirmOk/Cancel/table.fullTitle/table.fullExit/table.fullExport/table.fullZoomIn/Out/Reset/table.zoomSlider/table.fullPlaceholderMsg/table.fullEnter（20 键 zh/en 成对）；全屏背景改用 --bg-canvas（--canvas-bg 未定义导致全透明）；39 文件 1205 tests + lint + build 全绿；浏览器实测 S₄/S₅：默认子群（|H|=8/6）、随机 8 元素、24 阶直接全量、120 阶确认弹窗→全屏 14400 格→滑块 475%→18525px、ESC 复位策略 |
| 2026-08-15 | 3D 凯莱图 GIF 导出改离屏渲染（cayley3dControls 桥扩展 + Cayley3DView/export.ts）：`displayAngVel()` 返回 ▶ 自动旋转当前角速度（拖拽后按最后拖拽向量长度降速 0.35+min(0.65,len/360)，未拖拽 1×2π），导出帧数 = cycles×2π ÷ 展示速度重新推导（≥2，总转角恒整圈无缝循环）——导出动图与展示区转速一致；`frameAt(index, frameDelayMs)` 按帧序号精确求角 = 基准轨道 + 角速度×帧延时×帧序号（与实时渲染耗时无关，大群渲染变慢 GIF 不加速）；`beginRotation` 记录基准 theta/phi/radius/target 并创建独立离屏 WebGLRenderer+相机（preserveDrawingBuffer），渲染完全隔离不触碰实时画面，导出期间展示区照常旋转、结束仅 dispose 离屏渲染器（不再需要恢复相机）；useFrame 移除 externalRotation 分支（外部旋转改纯离屏）；i18n 「约 3 秒」/~3 seconds；export.test.ts +1（displayAngVel 帧数重推导 + frameAt 逐帧 (i, frameDelay) 断言）→ 39 文件 1206 tests 全绿；3D 相机外接球适配（Cayley3DView）：节点云质心 + 最大距离+1.4 外接球半径 R，fitOrbit 视角 = 球直径占视口高 2/3（d=1.5R/tan(fov/2)），minRadius=max(1.5, R+0.8)（不可进球）、maxRadius=max(30, 3×适配距)，切群（symbol|order）/切 3D 形状自动回正，双击复位=适配视角，latestFit ref 防对象身份波动，camera far 400，滚轮缩放方向修正（dY>0 拉近，radius×0.95^(-dY/100)）；主题 token 重构（index.css/App.css）：新增 --accent-blue/green/red/orange/danger(+hover)/--btn-on-accent（深 #0f0f1a / 浅 #ffffff——修复浅色模式 accent 按钮文字不可见）/--text-accent/--bg-elevated/--bg-muted/--bg-panel/--bg-hover/--border-color/--canvas-bg/--input-bg/--panel-bg/--panel-border/--text 别名，App.css ~12 处硬编码 #0f0f1a→var(--btn-on-accent)、#ff6b6b→var(--accent-danger)、杂色→语义 token；z-index 修复（.lang-toggle 100、.history-panel/.hint-box-header 10）；SymmetryView Html 标签 zIndexRange+pointerEvents:none（不再挡画布拖拽）+ 切换按钮主题化；组件 token 化：FloatingViewWindow/SemidirectProductView/SubgroupLatticeView/GroupActionPanel/SemidirectProductPanel/OperationsPanel/RightPanel；39 文件 1206 tests 全绿 |
| 2026-08-15 | 贡献者基础设施：CONTRIBUTING.md（新手引导/项目地图/开发流程/代码与测试约定/文档一致性/风险分级与人工审查/发布流程 扫→修→更→推/PR 清单）；.github/ISSUE_TEMPLATE/（bug_report.yml + feature_request.yml + config.yml，含群与视图/浏览器/主题字段）；.github/PULL_REQUEST_TEMPLATE.md（风险自评 🟢低/🔴高 + 人工审查请求单字段 + 验证清单）；vitest.config.ts 设覆盖率阈值（stmts/lines/funcs ≥ 85、branches ≥ 70，CI 用 test:coverage 强约束）；ci.yml 加 workflow_dispatch 手动触发；README.md/README_zh-CN.md 新增「参与贡献」区块（CONTRIBUTING/ROADMAP/CHANGELOG/Issue 模板入口 + CI 说明）+ 项目结构树补 .github/ 与 CONTRIBUTING.md 行；文档数字同步（Provider 9→12 个领域 Provider 分层、README_zh 1178→1206 tests、覆盖率 88.98/91.82 → 88.56 stmts / 77.78 branches / 92.08 funcs / 91.49 lines 并注明 CI 阈值）；39 文件 1206 tests 全绿 |
| 2026-08-15 | 【文档】新手教程 docs/TUTORIAL.md（EN）+ TUTORIAL_zh-CN.md（zh，14 节：快速开始/工作台导览（左侧 8 分区+右栏信息面板）/7 课实操（选群→6 核心视图→子群陪集 Lagrange→直积半直积展示构建→同态与第一同构→群作用轨道视图 Burnside→Sylow 全流程）/导出与多视图/操作速查/按定理学习路径/疑难解答），所有 UI 入口与控件经源码核对；AGENTS §2 文档索引新增 TUTORIAL 行，README/README_zh 引言新增教程链接，CONTRIBUTING 新手步骤 1 加教程指针；README 视图计数矛盾修复（intro 11 / 功能标题 10 / Usage 9 → 统一 13 + 卡片/入口说明，torus 旧描述改 tree 视图）；ROADMAP 瘦身为只列未做（已完成里程碑与 20 行逐次记录迁入本文件 §1/§2，历史数据保留 verbatim）；doc-sync 数字同步（README_zh 1178→1206、TESTING.md export 25→26、CAYLEY/STATE/UI 3D GIF 离屏与新主题 token 描述）；AGENTS/包/欢迎页版本号 → 1.12.1 |
| 2026-08-16 | 修复 drei Html 覆盖层 z-index 首挂载缺失（用户手机端「说明文字层级太高」二次反馈，v1.12.1 zIndexRange 修复实际依赖 drei useFrame 动态赋值，特定挂载时序下（UnsupportedOverlay→视图切换/并发挂载）useFrame 初始帧未写 `D.style.zIndex`，cssText 初始态 z auto 永久保持，absolute Html div 盖住不受控层）：SymmetryView 3 个 Html（群名/几何说明/变体切换按钮）与 Cayley3DView 节点标签 Html 加 `wrapperClass="gv-html-overlay"`、Cayley3DView 2 个 fullscreen Html（右上群名+|G|+自动旋转/复位、左上乘法律例）加 `wrapperClass="gv-html-fullscreen"`，App.css 新增 `.gv-html-overlay{z-index:2!important}` / `.gv-html-fullscreen{z-index:5!important}` 兜底（层级序：canvas 内容 < 覆盖层 2 < fullscreen 工具栏 5 < hint-box/历史 10 < 抽屉/面板 1100，!important 覆盖内联不管 useFrame 是否运行）；drei wrapperClass prop 直接设外层定位 div className（源码确认 10.7.7）；浏览器实测：手机 390px 对称性视图 computed z=2、手动清除内联 zIndex 模拟 useFrame 未跑 computed 仍 2（CSS 兜底生效）、桌面 1440px 3D 视图 fullscreen 工具栏 z=5 内容正常显示（右上 C₆|G|=6、左上乘法律例）；lint/build/39 文件 1206 tests 全绿 |
| 2026-08-16 | GAP 大群计算引擎 v1.13.0（E1 gappy 换道直连 GAP 4.16）：backend/gap_service.py（direct/cygwin 双模式探测 gap.exe、`symbol_to_expr` 表达式翻译（Cₙ/Dₙ/V₄/Q₈/GL(2,q)/直积/幂^k，不支持→None）、`GV_ser` 序列化（恒等置换 key、元素→置换/binary string、子群→下标列表、fail→"null"）+ `run_script` 进程串行锁 + GV_BEGIN/END 标记 + 15s 超时 + GapComputeError/GapTimeoutError/GapUnavailableError、probe_info）；main.py 六端点 >120 阶自动切 GAP（subgroups/normal-subgroups/conjugacy-classes/center/lattice/properties，`_gap_cache` 进程缓存 + `_gap_elements`/`_gap_subgroups`/`_gap_lattice` 响应适配，非置换群/不支持降级纯 Python）+ 新端点 `/api/compute/series`（derived/upperCentral/lowerCentral/composition，terms+factors 含 is_simple）+ `/api/compute/import-group`（任意 GAP 表达式 ≤4096 阶：SymmetricGroup(3)→S3、SmallGroup(16,8)→QD16，非法 422）+ health 暴露 gap{available,mode,executable}；实测 S₆ 1455 子群/6469 格边/11 共轭类首算 ≈13s 后毫秒级、C₂₀ 非置换群正确降级；前端就绪层：api.ts fetchSeries/fetchImportGroup + ApiSeries/ApiImportGroup/ApiHealth 类型，hybridCompute.ts fetchBackendSeries（>240 自动切后端，terms→GroupElement 映射、平凡项补全、factor label 生成 gapFactorLabel）+ gapFactorLabel 单测，GroupSeriesContext gapSeries/seriesLoading（queueMicrotask 包裹规避 set-state-in-effect，组切换重置），GroupContext 聚合暴露，SubgroupLatticeView 底部链式面板大群 seriesData 优先复用（seriesLoading 提示「后端（GAP）计算系列中…」+ series.loading i18n 键 zh/en）；前端群上限 <240（直积/半直积 144、展示 240）故 GAP 通路为后端能力+就绪层；后端 test_main.py +7（test_health_reports_gap/test_series_api/test_series_invalid_type/test_import_group_api/test_import_group_invalid_expr/test_small_group_structure_detection，needs_gap 跳过标记）33 passed；前端 39 文件 1213 tests 全绿 + lint + build；版本 1.12.1→1.13.0，文档同步（AGENTS §2/§5 GAP 条目、BACKEND.md §3 结构表 + §4 端点表 + 新 §6 GAP 引擎、README ×3、TESTING.md、CHANGELOG 本行、welcome.version） || 2026-08-16 | 修复大群子群格视图整树白屏（S5 实测复现：切子群格视图 → TypeError: Cannot read properties of undefined (reading 'length') → React 无 error boundary 整树卸载）：根因 backendCache.lattice 为后端 raw 结构（nodes 带 elements 数组 + is_normal、edges 用 source/target），SubgroupLatticeView latticeData memo 原样 raw cast 未归一化，centerIdx 等处 
d.elementIds.length 必崩；latticeData memo 现做归一化（elements→elementIds、is_normal→isNormal、level/index/label 补齐、edges source/target→from/to），消除 centerIdx/265/313/394 全部 elementIds 崩溃点；浏览器实测 S5 子群格 156 节点正常渲染 + 导列链式面板 S5⊵A5⊵⟨e⟩（120⊇60⊇1，因子 C2/G60）回归通过、console 0 errors；lint/tsc/39 文件 1213 tests 全绿 |
| 2026-08-16 | 记号导入群：左侧「⇥ 记号导入群」面板 + notationParser（S_5/A_4/PSL(2,7)/C_3×D_4/C_4^2/SmallGroup(n,i)/Aut(S_4) 等记号→TeX 预览+阶+本地/GAP 路径徽标，半直积 : 提示改 SmallGroup）；导入管线 /api/compute/import-group → createGroupFromImport（乘法表→Group，assignWordLabels+applyDihedralNormalForm 复用）；后端导入群注册（_imported_groups+_build_group_from_import，导入后 subgroups/lattice/classes/center/properties/series 全链路可用）+ symbol_to_expr 补 GL/SL/PGL/PSL(n,q) 族 + IsomorphismPermGroup 置换表示注入（PSL(2,7)→PSL(3,2) 179 子群 source=gap 实测通）+ needs_gap 测试；前端 +22 测试（notationParser/importGroup），后端 34 passed，全量 41 文件 1235 tests 全绿；注（后续 2026-08-17 已修复，见下条）：Cygwin bash 调 GAP 在 detached 孤儿进程必挂 120s（exit 124），须前台终端跑 uvicorn |
| 2026-08-17 | 修复 detached/后台下 GAP 计算永久卡死（exit 124 → 422，根因定案）：`-b -q -c 'Read(...)'` 脚本执行完回到 REPL 死等 stdin EOF（capture_output 的 stdin=PIPE 永不关闭），无控制台句柄上下文（Start-Process Hidden/服务/任务计划）更顽固（GAP_READY 已打印、CPU=0 纯等待、120s 被 GNU timeout 强杀）。实验矩阵（detached 对照 8 组：带 QUIT 全成功 / 无 QUIT 全挂，ppid 断链/线程/链深度/脚本重量均排除）后定案，`_run_gap_raw` 双重修复：`stdin=subprocess.DEVNULL`（Read 完立即 EOF 退出）+ `CREATE_NEW_CONSOLE | CREATE_NO_WINDOW`（显式分配隐藏控制台）；新增 `scripts/start-backend.mjs` node 托管 uvicorn（`npm run backend`，--no-reload 可选，stdio inherit + ENOENT 提示 pip install）；detached 端到端实测：import PSL(2,7) 3.3s OK（order=168/PSL(3,2)）+ subgroups 179 source=gap 0.2ms 缓存命中；backend pytest 34 passed 无回归；文档同步 BACKEND.md §2/§6 + AGENTS.md |
| 2026-08-17 | 导入群数据处理修复（用户反馈：SL 群元素显示矩阵字样 / Q64 子群显示 `<identity> of ...` 且无法选中 / 子群格不认真 / 结构「未知」）：根因 = **前后端元素 id 不一致**（前端 createGroupFromImport 元素 id=g{k} 表行序，后端 _build_group_from_import id=label=GAP 原始字符串）→ 前端 apiElementToGroupElement 按 id 匹配全失配 → 幻影元素（矩阵构造串/`<identity> of ...` 字样、选中/陪集失败）；结构未知 = detectStructureType order>60 一律 'unknown'。修复：①后端 _build_group_from_import 元素 id 统一 g{k}（value=[k]）+ python 镜像 assignWordLabels（_import_word_labels/_word_label，恒等元 e、词 a/a^2/a^{10}、未达 g_{k} 兜底）+ generators 改用 Generator dataclass；②新增 _imported_exprs 注册表（structure/gapExpr→原始 GAP 表达式），_gap_expr_for 优先重跑原始 expr（结构名重建可能枚举序漂移）；③gap_service _GV_COMPUTE_ALL/_GV_COMPUTE_SERIES 重写——不再替换置换像：GV_els=Elements(原始群)、GV_iso=IsomorphismPermGroup、keys=ListPerm(Image(iso,x)) 按原始枚举序对齐导入表行序（载荷 8→9 项含 id_pos，series 4 项含 id_pos，terms 在原始群上计算），compute_all_expr/compute_series_expr 新入口；④main.py _gap_elements 位置映射（先 id 精确匹配 S_n/A_n 排列串，失配→_gap_positional_map 三条件校验：len(keys)==order、id_pos==1、抽样子群封闭性→返回后端元素，失败落纯 python，**不再生成幻影**），subgroups/normal/conjugacy-classes/center/lattice/series 六端点接线；⑤前端 importGroup.ts 生成元瘦身 findTwoGeneratorPair（gens>2 时按元素阶贪心找 (最大阶 a, b) 两生成对，闭包=全群，Q64 6 生成元→(a 阶32, b) 标准对，assignWordLabels 自然单词标签；C2^k 无 2 生成对保留原 gens）；⑥detectStructureFromSymbol：order>60 按符号解析（':'/⋊→semidirect、'×'/' x '→direct、否则 indecomposable，S5 正确判不可分解）；后端 test_main +3（PSL(2,7) 179 子群元素 id g{k} 断言 / SL(2,3) group-info ids==g0..g23+单词标签 / Q64 37 子群全 g{k} 去重 64），36 passed；前端 41 文件 1239 tests 全绿 + lint + build；curl 实测 SL(2,3) 24 元素单词标签无矩阵串、Q64 子群 37 个 0 幻影、PSL(3,2) 179 子群/6 共轭类全 g{k} |
| 2026-08-17 | 修复大群子群格塌成一条竖线（用户反馈 Q64 子群格一条竖线，实为**所有走后端 lattice 的群**通病）：根因 = 后端两条 lattice 通路（main.py `_gap_lattice` 与 algebra.py `compute_subgroup_lattice`）level 分配按**节点索引** rank（`sorted` 稳定排序给同阶子群各自独立 rank）→ 每节点独占一层 → 前端布局每层 count===1 全部 x 居中 → 竖线（前端本地路径 subgroups.ts:136 按阶值分层本就正确，≤60 小群不受影响）；修复：两处均改为按**阶值**分层（orders 去重排序 → order_rank[order]=rank，同阶子群共享 level），Q64 37 子群 7 层 1/1/17/9/5/3/1 铺开、S6 1455 子群 21 层（L19:75 等）GAP 通路实测铺开；后端 test_main +1（test_lattice_same_order_shares_level：A5 10 个 2 阶子群同层断言）37 passed；前端无改动 |
| 2026-08-17 | 修复 GAP 计算弹终端框（用户反馈导入群时弹出终端框）：根因 = `_run_gap_raw` 的 `creationflags = CREATE_NEW_CONSOLE | CREATE_NO_WINDOW`（2026-08-17 修复 detached 挂死时加的）——MSDN 规定 **CREATE_NO_WINDOW 与 CREATE_NEW_CONSOLE 连用时被忽略** → 每次 GAP 调用都新建**可见**控制台窗口（标题 = bash.exe 路径）；修复：改为仅 `CREATE_NO_WINDOW`（不继承不新建控制台），EnumWindows 实测三场景——前台终端 2.5s 成功无弹窗、detached（pythonw 模拟服务/任务计划/npm run backend）2.5s 成功无弹窗、旧组合弹窗复现；pytest 导入/健康/系列 9 个真跑通过；curl 实测 PSL(3,2) 179 子群 source=gap 正常；部署到 Linux 本就无此问题（POSIX 无控制台概念） |
| 2026-08-17 | 3D 凯莱图 spherical 形状重构（节点与边全部嵌入球面、边不交叉，替代旧式「极地飞线」）：新增 `src/core/algebra/sphereGraph.ts`（约 900 行，纯函数全确定性）+ `embedSphereGraph(order, edges, {seed=12345, chordCap=max(6,floor(order/12)), maxLayers=3})` 三级降级管线——**档1 单球面路由** `routeLayer`：fibonacciSphere+mulberry32 确定性初始方向、greatArcSamples（对径弧 null 直接残留）、greatArcsCross 精确交叉检（共面保守/端点排除/对径跨弧补区间）、交叉消除 6 轮迭代（bendControl 二次弯 + bezierSamplesCubic 三次平顶弯 + bent-other 弯对方弧分担 + 失败 eject 残留弧，candidates ±0.7/±1.0/±1.35/±0.45/±0.25，bendControl 满 2 弯 ejects，quick 模式 >300 弧跳过全对扫描）、节点避让 minAngularToNodes<clearance 弯弧/eject；**单环图 cycleSeeding**（边集=单环时按环序均匀布大圆 2πk/n，天然 0 交叉全确定——C6 曾因随机初始+爬山从 1 交叉无梯度到 0 而失败，弧上弯起无用：正交交叉双弧法线互在对方平面内且对方弧长覆盖穿越扫掠区）；**档2 内部弦** `buildChords`（剩余边改为球内直线 chord，segmentsNear 采样 9×9 最近距互检 + 节点距离检，冲突 ejects 缓存 → 恶意密度兜底下沉档3）；**档3 同心球分层** `routeLayers`（残边按从长到短分 0.8R/0.64R 层，最多 3 层，fibonacci 轴网格 + 环收缩调距防遮挡，chordCap=0 强制全弦）；结果含 `layers`、`chords` 端点对 + 每根弦 `aIdx/bIdx` 供 StemRod 杆标识（弦端点非节点时画白色杆连接节点↔弦端点）。layout3D.ts `case 'spherical'`（与 default 合并）改用 `sphericalNodeDirections`×`sphereRadiusFor(order)`（radius=5 常量与 fibonacciSphere 兜底未动）。Cayley3DView.tsx 重写 spherical 渲染：`sphereEmbedding` memo（cayleyEdges→去自环/大群 visibleElementIds 过滤→embedSphereGraph，**上移到 cayleyEdges 之后修复 eslint TDZ『Cannot access variable before it is declared』**）、positions 改 directions×R、新增 SphereArcMesh（BufferGeometry 圆柱 polyline + 无箭头若弧两端有弦则箭头同色描边）/ChordEdge（玉色虚线 + 两端白色小杆）/StemRod（线段 + 白杆）/LayerShells（球壳 mesh，0.8R/0.64R 同节点色，非平面徽标态用）；非 planar（chords 或 layers>1）时顶部徽标「非平面：内部弦边 ×N」/「非平面：N 层同心球面」（i18n `cayley3d.embedChordMode/embedLayerMode` zh+en，t 支持 {n} 插值）；杆元素跳过 NodeSphere 绘制。测试 `src/__tests__/sphereGraph.test.ts` 19 用例（mulberry32 确定性/fibonacci 北极/单位化/最小角距/greatArcSamples 对径 9 点 toBeCloseTo(0,9)/greatArcsCross 四例（共面重叠/共享端点非交叉/对径跨弧）/C6 planar 0 残留/不带 seed 500 次全 planar/K5 chord 5 根/K6 chordCap=0 3 层/seed 确定性/stems 端点含非节点/radius 公式/compute3DPositions spherical 回归）；fixtures 可调 SEED=给 Climb 爬山（n≤12 平滑目标：近交叉罚分 (1.6-d)²+硬交叉 c×1000，阈值须>1.2；n≤16 激进 moves n×40/restarts 4/±0.4）仍保留供非环图细调；42 文件 1258 tests 全绿 + lint + build；浏览器实测 C6 球形凯莱图 0 弦 0 残留、K5 5 根玉色弦、S3 三角 planar |
| 2026-08-18 | 3D 球面嵌入共面误判修复（承接上条）：greatArcsCross 共面分支改按弧相位 unwrap 区间交叠判定（intervalOf 沿 slerpUnit 采样相位差累加）——旧实现把回转弧（S3 的 240 度跨 360 度参考线）当长弧误判交叉，致主循环弯弧失败整弧淘汰为弦（S3 丢 1 弧）；修复后 S3 6/6、A4 18/18、D4 12/12、D5 15/15 等平面群全弧保留 0 弦 0 分层 0 近贴，真 Q16=SmallGroup(16,8) 32 边 3 弦+2 层属非平面正常余量；新增 diagReal.test.ts 双用例：cycleSeeding 赤道断言 + 11 群真实边集边守恒/平面群零弦零交叉回归；43 文件 1263 tests 全绿 + lint + build；浏览器实测 A4/S3 spherical 无「非平面」徽标、S4 显示「非平面：2 层同心球面」符合预期；版本 1.13.1 |
| 2026-08-18 | 3D spherical 嵌入再修复（用户四点反馈：不许自动推送 / S4 三生成元截角八面体消失 / A5 平面图嵌入不上 / Q8 弦相交 / GL(2,3) 挤角落）：①**A₅ 生成元换为 Group Explorer 风格 (12345),(12)(34)**（AlternatingGroup.ts，旧 (12)(34),(135) 凯莱图非截角二十面体骨架，DFS 同构 601 calls 失败）+ trySkeletonEmbed 新增 order===60&&edges===90 → truncatedIcosahedron(5) → A₅ **60/60 全弧 0 弦 0 交叉**（kept=90 minAng=0.4063，与 A5 默认 3D 布局 truncatedIcosahedron 特殊动作 ['2,3,4,5,1','2,1,4,3,5'] 完全一致）；②**polyhedra.ts computeSkeletonEdges 修 bug**（距离 bin 升序首个 count==3n/2 为棱长，旧 maxCount bin 对截角二十面体误取 120 条/截角八面体 48 条非真棱，用户探针同构测试失败根因）；③**S₄ 三换位「截角八面体」3D 形状恢复**（truncatedOctahedron3：types.ts Layout3D 联合 + availableShapes3D S4 列表 + layout3D.ts case（v1.13.1 坐标 + placeS4Elements）+ cayleyActions.ts getSpecialCayleyActions/addAllCayleyActionsHelper + ViewPanel.tsx 特殊动作 ['2,1,3,4','1,3,2,4','1,2,4,3']；注：v1.14.0 删除时误判三换位凯莱图非平面，实测 ≅ 截角八面体 DFS 同构 TRUE）；④**Q₈ 凯莱图（i,j 生成元）= K₄,₄ 非平面**（{±1,±k}↔{±i,±j} 完全二分；试正四角反棱柱骨架 DFS 同构失败后删分支；cr(K₄,₄)=4 → 12 弧 + 4 弦恰为最优画法，弦间距 ≥0.08 无视觉相交）；⑤**buildChords 收紧**（CHORD_MIN_SEP 0.02→0.08 ≈ 2.3×管半径，弦-弦永不相交；attempts 10→24；perturb ±(0.06+rng·0.14)）；⑥**climbNodeSpread 新节点分离爬山**（objective=Σdd² for sep<minAng，moves=n·24 restarts=3，单点 ±0.3rad 确定性 rng；0-交叉分支与 FD-nonzero 分支 repairClearance 后收尾展开，目标 2·0.42/R0）——消除 GL(2,3)/A₅ 旧「顶点重合」（挤角落根因）；⑦**repairClearance 兜底加守卫**（强制旋转仅当交叉数不增否则 break 整轮，旧版把归零交叉破坏成 280）；GL(2,3)（48 阶 3-正则 72 边）33 弧+22 弦+3 层、minNodeAng 0.1052（旧 0.0000 重合）、弦距 ≥0.08；D6 六棱柱 SKEL 12/18 miss 后 FD 0 交叉 planar（kept=18 minAng=0.9577）；Q16 18 弧+8 弦+3 层 minAng 0.1752；diagReal.test.ts 增 A5/GL(2,3) 用例 + 每 case minAng（节点-节点）断言；43 文件 1263 tests 全绿 + lint + build；版本 1.14.1 |
| 2026-08-18 | 3D spherical 嵌入 GL(2,3) 偏心修复（用户反馈「节点都挤在不到半个球面」）：**climbNodeSpread 增重心惩罚 + 推离重心移动**——①objective 加 centroid 项 `|Σdir/n|²·n·4`（节点整体偏心时重心偏离球心，均匀分布 ≈1/√n 量级），②moves 循环一半步数选「最靠内节点」（dir·centroid̂ 最大）绕 axis=cross(dir,centroid) 旋转 0.6-1.0rad 远离重心（比单节点随机旋转对重心收敛快）；GL(2,3) 由旧「挤半球」→ **cent=0.0001 节点包围球心均匀分布**，kept=42 弧+19 弦+3 层、minAng=0.1017 无重合（均匀分布代价：交叉统计 17 vs 旧 6，非平面图不可避免，弦距仍 ≥0.08）；Q16 14 弧+9 弦+3 层 minAng 0.1451；diagReal.test.ts 每 case 增 centroid 断言 `cent < 0.25` + 日志行；43 文件 1263 tests 全绿 + lint + build；版本 1.14.1 |
| 2026-08-19 | spherical 球面嵌入全砍 + cone 圆锥布局兜底：**spherical 移除**——删除 sphereGraph/spherePlanar/sphereDecomp/planarLayers 4 源文件 + fibonacci2DLayout + 5 测试文件（sphereGraph/sphereDecomp/spherePlanar/planarLayers/diagReal）+ i18n 3 键，Layout3D/CayleyShape2D 联合类型移除 'spherical'，Cayley3DView 球弧/弦渲染分支删除；**cone 兜底新增**——3D 圆锥（顶点恒等元 + 沿母线按元素阶分圈，第 k 圈摆 k 阶元无 k 阶元素则跳圈，圈半径/高度 = 5·k/maxK 锥面 45°，圈内均匀角分布）与 2D 同心环俯视（coneLayout2D：中心恒等元 + 按阶分环 min(w,h)·0.42·k/maxK）；形状分发（getAvailableShapes3D base=['cone']、getDefaultLayout3D 商群+兜底 'cone'、getAvailableShapesForView 各列表尾部 'cone'）；42 文件 1250 tests 全绿 + lint + build；浏览器实测 S3 3D 下拉 cone/circular/hexagon、D6 2D 下拉 圆形/双环/圆锥；版本 1.15.0 |
| 2026-08-20 | 代码审查修复轮（用户发起全面审查，修 bug/体验/数学问题）：**H1 白屏**——`semidirectProductStorage.ts` `loadSemidirectProductSpecsFromStorage` 补 `Array.isArray` 校验（6 个存储 loader 中唯一漏检，localStorage 脏数据使 `GroupSemidirectProductContext` useState 初始化器 for-of 抛 TypeError 整树卸载）；**H2 布局竞态**——`GroupCoreContext` 新增 `currentGroupRef`（ref 在各 set 调用点同步赋值，暴露于 value），`GroupContext.runForceLayout` 的 `.then/.catch` 守卫改比 `core.currentGroupRef.current`（原闭包捕获 `core.currentGroup` 恒等于 groupAtStart，守卫永不触发 → 切群后旧群 forceLayout 坐标写入新群，元素 id 跨实例可复用致视觉错乱）；**SymmetryView 分类顺序**——`getSymmetryType` 的 `\times`/`^{` 检查前置（原 C/D `startsWith` 先命中使 `C_{4}\times C_{2}`、`C_{2}^{3}` 等误渲染为 cyclic/dihedral 图形）、`C_{2}^{2}`/`C_{2}\times C_{2}` 特判 rectangle（V₄≅矩形对成群）；**dist=0 除零守卫×3**（GroupCanvas.renderEdgePath / FloatingViewWindow / SylowView 主边，拖拽节点重合不再出 NaN 路径消散）；**M3** 切群重置 `symmetryShowAction`（原残留 true 劫持新群元素点击为对称演示）；**M4** `compositionEnum`/`seriesFlags` 大群分支校验 `gapSeries.type === 'composition'`（防导列瞬态冒充合成列）；**M6** `showCosetFromElements` 改 `[...elementIds].sort()`（不再原地改调用方数组）；**L4** `homomorphismStorage` 整批容错（非数组返回 [] + 每记录独立 try/catch + 缺 mapping 字段跳过，不再一坏条全丢）；**M7** `GroupBackendContext` 守卫去冗余 + 补 `.catch` 兜底 loading 复位；**PresentationTableView 裁剪**——cellSize/cellFont 按 viewBox 自适应（order 25-36 不再裁右下角）；**L5** cayleyActions 空群防御 `elements[0]?.id`；新增测试 `symmetryView.test.ts`（6：分类含直积/幂符号矩形特判）+ `storage.test.ts`（7：两个 storage 脏数据容错 round-trip）；44 文件 1264 tests 全绿（覆盖率 88.91/78.22/92.32/91.90）+ lint + build |
| 2026-08-20 | 数学核心审计修复轮（用户要求「把数学核心审计完成」）：**polyhedra 坐标×3**——`truncatedDodecahedron` 第 2/3 组坐标修正（`[1/φ,1,2φ]`→`[1/φ,φ,2φ]`、`[1/φ,φ,1+2φ]`→`[φ,2,φ+1]`，60 顶点 120 伪棱→真 90 棱截角十二面体）、`rhombicuboctahedron` 坐标从错误的 `(±1,±(√2−1),±(√2+1))` 偶排列改为正确的 `(±1,±1,±(1+√2))` 全排列（24 点 4-正则 48 棱）、`truncatedCube` 坐标修正为 `(±(√2−1),±1,±1)` 全排列（silver ratio，3-正则 36 棱；原误与 rhombicuboctahedron 同集 4-正则 48 棱）；**computeSkeletonEdges** 去硬编码 3n/2——改升序扫描 `count ≥ 3·n/2 且 count 为 n/2 整数倍`（支持 4-正则 rhombicuboctahedron 48 棱，伪 bin 如 24/120 对自动跳过）；**automorphisms 静默截断→伪 Aut 群**——删除 `MAX_RESULTS=1000`（`Aut(C₃³)=11232`、`Aut(C₇²)=2016` 等组合数 ≤30000 的群被截断后 `createAutomorphismGroup` 产生阶错误/乘法表损坏的伪群并污染半直积分解；C₇² 全量 2016 = |GL(2,7)| = 48·42 回归断言）；**series 交换因子标签**——`computeFactor` 改用有限交换群分类定理精确不变量链匹配（阶分布 + `abelianFactorChains` 枚举 + `distributionsEqual`，`subgroups.ts` 三函数导出；秩≥3 因子不再错标如 C₂⁴→C₂×C₈，dist 按陪集归一化 ÷|small|）；**presentations 直积交换子**——`presentationOfDirectProduct` 从「仅因子 0×其余」改为全部因子对 (fi<fj) 生成元两两加交换子（≥3 因子直积如 C₂×C₃×C₅ 不再缺 [b,c] 丢展示路径）；**actions 空表守卫**——`verifyAllRelations` 空置换表直接 `{ok:false}`（防 Sylow 无素因子时 perms.get 崩溃）；polyhedra/automorphisms/series/presentations 4 文件 +8 回归测试；44 文件 1272 tests 全绿 + lint + build |
| 2026-08-23 | **P0 引擎化预处理（FGVE 前置，行为零变化的五阶段重构 A-E，各阶段独立 commit + test/lint/build 三绿）**：**A 类型拆分+门面（c078cab）**——types.ts(780行) 拆 types/{group,view,homomorphism,actions}.ts + algebra/groupProps.ts（性质检测/形状选择函数），原 types.ts 变 barrel 零破坏；新建 `src/core/index.ts` 门面全量公共导出（automorphisms 内部版 isAutomorphismGroup 排除只留 groupProps 公共版）+ ESLint no-restricted-imports 边界规则（禁 UI 层深导入 types/{group,...} 与 core/**/_internal）+ coreBoundary.test.ts（105：core 纯净性扫描零 react/localStorage/DOM + 门面 ~100 符号存在性）。**B 大文件拆分（5ee2c1a/8146f83/e9f1455/594ca2b/39125f7）**——原文件变纯 barrel、函数体逐字迁移、私有函数不进公共面：forceLayout(1552行)→algebra/layouts/{shared,factorLayouts,ringShapeLayouts,specialLayouts}；layout3D(1200行)→纯 dispatcher + layouts3D/ 6 模块；presentations(1011行)→presentations/{wordParser,toddCoxeter,minimizer,presentationOf}；subgroups(990行)→subgroups/ 7 模块；SmallGroups(713行)→groups/SmallGroups/{abelianProducts,wordLabels,tableGroup,registry}。**C 错误模型双轨制（0196e6f）**——`src/core/result.ts`（Result<T,E> 判别联合 + EngineError{kind:'parse'\|'guard'\|'unsupported',reason?} + ok/err）+ `src/core/guards.ts` 收敛 11 守卫常量（各模块 re-export 保公共面）；14 处裸 throw new Error 全部 EngineError 类型化（消息保留 toThrow 兼容）；notationParser error 收窄 NotationErrorCode 字面量 union；api.ts ApiError(status,detail) + fetchHealth 补 res.ok；resultGuards.test.ts 10。**D 输入加固（b703791）**——引入 zod@4.4.3 + `src/utils/persistence.ts`（parseStoredJson/loadStoredJson/loadStoredArray 逐条容错/版本化信封 {__gvVersion,data}），11 storage 文件 14 处裸 JSON.parse 全部迁移，Workspace.tsx 会话恢复接入信封（legacy 裸数据兼容）；persistenceFuzz.test.ts 11（坏 JSON/schema 不符/未来版本拒绝/HOSTILE_STRINGS 25 条 fuzz 四解析器）；后端 schemas.py pydantic Field(max_length)+Literal 加固（backend pytest 37 passed）。**E 总验收**——浏览器冒烟通过（视图切换 set/cayley/sublattice/sylow/table、S₃ 创建、重载会话恢复、groupviz-session 信封 __gvVersion:1、console 零错误零警告）；47 文件 1398 tests 全绿 + lint + build；版本 1.15.1 |
| 2026-08-23 | **E1 正式关闭（文档决策）**：gappy 后端集成方案不采用——2026-08-16 已换道直连本机 GAP 4.16 并以 v1.13.0 交付核心能力（六端点 >120 阶自动切 GAP + /series + /import-group、进程缓存、15s 超时守卫、S₆ 实测），IdGroup 已砍除；剩余后端增强项（缓存/超时强化、特征标表端点、大群前端全链路验收）并入中期 FGVE（ROADMAP §2.4/§2.5）；近期功能夯实期收官，ROADMAP/AGENTS/CONTRIBUTING/GROUPS 同步更新（§2.5 更名「GAP 后端完善」、GL(2,5) 提示改记号导入通路） |