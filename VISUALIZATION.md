# GroupViz - 可视化策略指南

本文档记录群论可视化的最佳实践和策略，作为开发参考并持续更新。

---

## 1. 可视化设计原则

### 1.1 核心原则

| 原则 | 说明 | 实现方式 |
|------|------|----------|
| **渐进式揭示** | 先展示整体结构，再逐步深入细节 | 分层渲染、动画过渡 |
| **多表征同一性** | 同一概念提供多种表示方式 | Cayley图↔乘法表↔元素列表 |
| **操作可逆性** | 用户操作可undo，保持探索安全感 | 历史栈、撤销功能 |
| **即时反馈** | 每个操作立即显示结果 | 实时计算与渲染 |
| **上下文保持** | 导航时保持当前分析上下文 | 状态持久化 |

### 1.2 认知负荷管理

```
高认知负荷场景          →  解决策略
─────────────────────────────────────────
群运算过程复杂           →  分步动画演示
大量元素同时显示         →  焦点+背景模糊
抽象概念（商群）         →  具体实例+类比
多种颜色同时出现         →  分批展示+图例
```

---

## 2. 群结构可视化策略

### 2.1 子群 (Subgroups)

**可视化目标**：理解子群作为"群中群"的结构

**策略**：
```
┌─────────────────────────────────────────────────────┐
│  主视图：Cayley图整体                                │
│  ┌───┐    ┌───┐    ┌───┐                          │
│  │e  │────│a  │────│b  │   ← 子群元素用实线圈出     │
│  └───┘    └───┘    └───┘                          │
│                                                     │
│  侧边栏：子群属性                                    │
│  • 阶：3                                           │
│  • 指数：4                                         │
│  • 生成元：{a}                                      │
│  • 是否正规：✓                                     │
└─────────────────────────────────────────────────────┘
```

**关键设计**：
- 子群元素用**实线线圈出**，与周围形成明显边界
- 子群内外用**不同底色**区分（淡入淡出过渡）
- 点击子群元素，高亮其在乘法表中的行/列

### 2.2 陪集 (Cosets)

**可视化目标**：理解陪集是子群的"平移"

**策略**：
```
┌─────────────────────────────────────────────────────┐
│  Lagrange定理演示                                    │
│                                                     │
│  G = {e,a,b,c,d,f}  (|G| = 6)                     │
│                                                     │
│  H = {e,a}     (|H| = 2)                           │
│                                                     │
│  陪集划分动画：                                      │
│  ┌────────┐ ┌────────┐ ┌────────┐                 │
│  │  e  a  │ │  b  d  │ │  c  f  │  ← 3个陪集       │
│  │  (H)   │ │  b+H   │ │  c+H   │                 │
│  └────────┘ └────────┘ └────────┘                 │
│                                                     │
│  验证：|G| = |H| × [G:H] = 2 × 3 = 6 ✓             │
└─────────────────────────────────────────────────────┘
```

**动画序列**：
1. **选择子群H**：高亮子群元素
2. **左乘元素**：逐个选取g∈G，显示gH
3. **颜色编码**：每个陪集用不同颜色
4. **集合动画**：陪集元素移动到对应区域
5. **计数验证**：显示 |H| × [G:H] = |G|

### 2.3 正规子群与商群 (Normal Subgroups & Quotient)

**可视化目标**：理解G/N是"压缩"后的群

**策略**：
```
┌─────────────────────────────────────────────────────┐
│  商群 G/N 构造过程                                    │
│                                                     │
│  原始群 G：D₃ = {e, r, r², s, rs, r²s}             │
│                                                     │
│  正规子群 N = {e, r, r²} ≅ Z₃                       │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │           G 的 Cayley图                      │   │
│  │    (N内元素用蓝色实线圈出)                    │   │
│  │                                             │   │
│  │        r²                                   │   │
│  │       ↙  ↘                                 │   │
│  │     e ←→ r                                 │   │
│  │       ↘  ↙                                 │   │
│  │       s  rs                                │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │           商群 G/N ≅ Z₂                     │   │
│  │                                             │   │
│  │         N     ←→    {s, rs}                 │   │
│  │        (0)    (1)                          │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

**关键设计**：
- 正规子群用**蓝色虚线框**持续高亮
- 商群元素用**压缩节点**表示（显示为代表元）
- 同余关系用**颜色映射**可视化：g ~ h ↔ 同色

### 2.4 共轭类 (Conjugacy Classes)

**可视化目标**：理解共轭是"坐标系变换"

**策略**：
```
┌─────────────────────────────────────────────────────┐
│  共轭类分解                                          │
│                                                     │
│  S₃ 的共轭类：                                       │
│                                                     │
│  ┌────────────────────────────────────────────┐    │
│  │  类1: {e}           |cl(e)| = 1            │    │
│  │  ┌────┐                                     │    │
│  │  │ e  │  ← 中心元素（单独高亮）              │    │
│  │  └────┘                                     │    │
│  ├────────────────────────────────────────────┤    │
│  │  类2: {(12), (13), (23)}  |cl| = 3        │    │
│  │  ┌────┐  ┌────┐  ┌────┐                    │    │
│  │  │(12)│  │(13)│  │(23)│  ← 3-换位同色      │    │
│  │  └────┘  └────┘  └────┘                    │    │
│  ├────────────────────────────────────────────┤    │
│  │  类3: {(123), (132)}   |cl| = 2            │    │
│  │  ┌────┐  ┌────┐                             │    │
│  │  │123 │  │132 │  ← 3-循环同色               │    │
│  │  └────┘  └────┘                             │    │
│  └────────────────────────────────────────────┘    │
│                                                     │
│  类方程：|S₃| = 1 + 3 + 2 = 6                       │
└─────────────────────────────────────────────────────┘
```

**交互设计**：
- 选择元素a，显示所有gag⁻¹的计算过程
- 拖动一个元素，展示其如何"移动"到共轭位置

---

## 3. 群运算可视化策略

### 3.1 Cayley乘法表

**可视化目标**：快速查询运算结果，发现群结构模式

**策略**：
```
┌─────────────────────────────────────────────────────┐
│  D₃ 乘法表                                           │
│       │  e   r  r²  s  rs r²s                      │
│  ─────┼─────────────────────────────────           │
│    e  │  e   r  r²  s  rs r²s                      │
│    r  │  r  r²   e r²s   s  rs                     │
│   r²  │ r²   e   r  rs r²s   s                     │
│    s  │  s  rs r²s   e  r  r²                      │
│   rs  │ rs r²s   s  r²  e   r                      │
│  r²s  │r²s   s  rs   r r²   e                      │
│  ─────┴─────────────────────────────────           │
│                                                     │
│  [高亮模式]                                          │
│  ┌────────────────────────────────────────────┐    │
│  │  点击 r 行 → 高亮整行，显示 r·x = ?         │    │
│  │  点击 s 列 → 高亮整列，显示 x·s = ?         │    │
│  │  r·s = rs (交叉格高亮)                      │    │
│  └────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

**交互功能**：
- **行探索**：点击某行，高亮该元素左乘的所有结果
- **列探索**：点击某列，高亮该元素右乘的所有结果
- **模式发现**：自动检测并标记对称性（阿贝尔群对角线对称）
- **逆元标记**：每个元素的逆元用特殊边框标记

### 3.2 运算律验证动画

**结合律 (a·b)·c = a·(b·c)**
```
步骤1：显示 (a·b) 的计算路径
        a ──→ ab ──→ (ab)c
               ↓
              结果

步骤2：显示 a·(b·c) 的计算路径  
        c ←── bc ←── a(bc)
                      ↓
                     结果

步骤3：两条路径到达同一点，动画闪烁确认
```

**交换律**（仅阿贝尔群）
```
自动检测：若表关于主对角线对称
        → 显示"交换律成立"徽章
        → 允许拖动交换因子顺序
```

---

## 4. 定理可视化策略

### 4.1 Lagrange定理

```
┌─────────────────────────────────────────────────────┐
│  Lagrange定理：|H| 整除 |G|，|G| = |H| × [G:H]     │
│                                                     │
│  1. 选择子群 H ⊆ G                                   │
│                                                     │
│     G = {0,1,2,3,4,5}  (Z₆)                        │
│     H = {0,3}         (2阶子群)                     │
│                                                     │
│  2. 动态生成左陪集                                  │
│                                                     │
│     0+H = {0,3}        ← 蓝色                       │
│     1+H = {1,4}        ← 红色                       │
│     2+H = {2,5}        ← 绿色                       │
│                                                     │
│  3. 可视化计数                                      │
│                                                     │
│     ┌────────────────────────────────────────┐      │
│     │  |G| = 6 = |H| × [G:H]                 │      │
│     │      ↓       ↓      ↓                  │      │
│     │      6   =   2  ×   3   ✓             │      │
│     └────────────────────────────────────────┘      │
│                                                     │
│  4. 推广：任意子群阶必整除群阶                       │
└─────────────────────────────────────────────────────┘
```

### 4.2 同构定理

**第一同构定理可视化**：
```
┌─────────────────────────────────────────────────────┐
│  第一同构定理：G/ker(φ) ≅ im(φ)                     │
│                                                     │
│  示例：φ: Z₆ → Z₃,  φ([x]) = [x] mod 3            │
│                                                     │
│  核：ker(φ) = {0,3} ≅ Z₂                           │
│                                                     │
│  ┌──────────────┐    φ    ┌──────────────┐        │
│  │     G        │ ──────→ │    im(φ)      │        │
│  │  Z₆ = {0-5}  │         │    Z₃ = {0-2} │        │
│  │              │         │              │        │
│  │  [0,3]≡0    │         │      0       │        │
│  │  [1,4]≡1    │         │      1       │        │
│  │  [2,5]≡2    │         │      2       │        │
│  └──────────────┘         └──────────────┘        │
│        ↓ 商群                    ↑ 同构映射        │
│  ┌──────────────┐    ≅      ┌──────────────┐        │
│  │   Z₆/{0,3}   │ ──────→ │     Z₃      │        │
│  │  = {0,1,2}   │         │              │        │
│  └──────────────┘         └──────────────┘        │
│                                                     │
│  验证：|G/ker| = 6/2 = 3 = |im| ✓                 │
└─────────────────────────────────────────────────────┘
```

**可视化要点**：
- 并排展示核、像、商群
- 用相同颜色标记对应元素
- 动画演示压缩过程

### 4.3 轨道-稳定子定理

```
┌─────────────────────────────────────────────────────┐
│  轨道-稳定子定理：|G| = |Orb(x)| × |Stab(x)|        │
│                                                     │
│  示例：D₄ 在顶点 {1,2,3,4} 上的作用                 │
│                                                     │
│  群 G = D₄ = {e, r, r², r³, s, rs, r²s, r³s}      │
│                                                     │
│  选取 x = 1                                        │
│                                                     │
│  稳定子 Stab(1) = {e, s}  (|Stab| = 2)            │
│                                                     │
│  轨道 Orb(1) = {1,2,3,4}  (|Orb| = 4)            │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │           顶点排列                            │   │
│  │              1                                │   │
│  │           ↙   ↘                              │   │
│  │         4       2                            │   │
│  │           ↘   ↙                              │   │
│  │              3                                │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  验证：|G| = 8 = |Orb| × |Stab| = 4 × 2 = 8 ✓     │
└─────────────────────────────────────────────────────┘
```

---

## 5. 颜色编码系统

### 5.1 生成元颜色

```
标准色板：
┌─────────────────────────────────────────────────────┐
│  a → #ff6b6b  (红色)                               │
│  b → #4ecdc4  (青色)                               │
│  c → #ffd93d  (黄色)                               │
│  d → #6bcb77  (绿色)                               │
│  e → #9b59b6  (紫色)                               │
│  f → #3498db  (蓝色)                               │
│  g → #e67e22  (橙色)                               │
│  h → #1abc9c  (薄荷绿)                             │
└─────────────────────────────────────────────────────┘
```

### 5.2 结构颜色

```
┌─────────────────────────────────────────────────────┐
│  子群边界        → 实线蓝色 #3498db                │
│  正规子群边界    → 虚线紫色 #9b59b6                │
│  陪集1~16        → 16色调色盘                      │
│  子集1~8         → 8色调色盘                       │
│  共轭类1         → 红色系渐变                      │
│  共轭类2         → 蓝色系渐变                      │
│  单位元          → 金色边框 #f1c40f                │
│  逆元            → 虚线边框                         │
│  选中元素        → 脉冲发光效果                     │
└─────────────────────────────────────────────────────┘

子集8色调色盘：
- #ff6b6b (红), #4ecdc4 (青), #84cc16 (绿), #a78bfa (紫)
- #f97316 (橙), #38bdf8 (浅蓝), #f43f5e (玫红), #eab308 (金)

陪集16色调色盘：
- #ff6b6b, #4ecdc4, #ffd93d, #84cc16, #a78bfa, #f97316, #38bdf8, #f43f5e
- #eab308, #6366f1, #ec4899, #14b8a6, #0ea5e9, #22c55e, #a855f7, #06b6d4
```

### 5.3 状态颜色

```
高亮/选中     → 边框加粗 + 背景变亮
hover         → 轻微放大 + 边框变色
不可用        → 灰色 50% 透明度
计算中        → 旋转加载图标
错误          → 红色边框 + 抖动动画
```

---

## 6. 布局策略

### 6.1 Cayley图布局

| 群类型 | 推荐布局 | 说明 |
|--------|----------|------|
| 循环群 Zₙ | 圆形 | 元素沿圆周排列，生成元指向下一节点 |
| 二面群 Dₙ | 双环 | 内圈旋转r，外圈反射s |
| 对称群 Sₙ | 力导向 | 自动布局，保持对称性 |
| 阿贝尔群 | 网格 | 行最简形式展示 |

### 6.2 圆圈图平面布局

对于具有共享元素的循环子群（如Q8、直积群），采用分层布局减少边交叉：

| 层级 | 位置 | 元素类型 |
|------|------|----------|
| 核心 | 圆心 | 单位元 e |
| 外层 | 大圆周 | 共享元素（多个循环子群共有） |
| 内层 | 小圆周 | 特定循环子群的独有元素 |

**算法要点**：
- 检测所有共享元素的循环子群集合
- 共享元素沿外层圆周均匀分布
- 各循环子群的独有元素沿生成元方向从共享点向外扇形展开
- 大幅减少边交叉，提升平面性

### 6.3 多视图协调

```
┌─────────────────────────────────────────────────────┐
│  主视图 (Cayley图)     │  侧边栏                    │
│                        │  ┌─────────────────────┐   │
│      ○ ○ ○ ○          │  │ 乘法表              │   │
│      ○ ○ ○ ○          │  │ e  a  b  c         │   │
│      ○ ○ ○ ○          │  │ a  b  c  e  ←高亮  │   │
│      ○ ○ ○ ○          │  │ b  c  e  a         │   │
│                        │  │ c  e  a  b         │   │
│                        │  └─────────────────────┘   │
│                        │  ┌─────────────────────┐   │
│                        │  │ 元素属性             │   │
│                        │  │ a                   │   │
│                        │  │ 阶: 4               │   │
│  [联动选择]            │  │ 逆元: a³            │   │
│                        │  │ 共轭类: {...}       │   │
│                        │  └─────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

---

## 7. 动画设计

### 7.1 动画类型

| 场景 | 动画效果 | 时长 |
|------|----------|------|
| 元素运算 | 沿路径移动 + 缩放 | 800ms |
| 陪集生成 | 元素渐次高亮 | 500ms/元素 |
| 商群压缩 | 缩小组合并移入新节点 | 1200ms |
| 共轭变换 | 对称翻转效果 | 1000ms |
| 定理验证 | 数字递增动画 | 600ms |

### 7.2 动画控制

- **播放/暂停**：用户可控动画进程
- **速度调节**：0.5x, 1x, 2x, 4x
- **步骤回退**：支持单步回退
- **自动播放**：循环演示

---

## 8. 交互设计模式

### 8.1 选择模式

```
单一选择     → 点击选中，背景高亮
多选         → Ctrl+点击，添加到选择集
框选         → 拖拽框选区域内的元素
路径选择     → 点击起点+终点，自动显示路径
```

### 8.2 探索流程

```
1. 自由浏览   →   2. 选中目标   →   3. 深入分析   →   4. 验证定理
      ↓                  ↓                  ↓                ↓
  Cayley图拖拽      点击元素查看        展开子群结构      点击定理卡片
  自由探索          详细属性            陪集分解          动画验证
```

---

## 9. 辅助功能

### 9.1 数学符号渲染

使用 KaTeX 渲染所有数学公式，替代 Unicode 纯文本：
```
Z₆, D₄, S₃, A₄    →  TeX: Z_6, D_4, S_3, A_4
V₄, Q₈             →  TeX: V_4, Q_8
H ⊆ G, N ⊲ G       →  TeX: H \subseteq G, N \triangleleft G
G/N, ker(φ), im(φ) →  TeX: G/N, \ker(\varphi), \operatorname{im}(\varphi)
≅, →, ∈, ∀, ∃      →  TeX: \cong, \to, \in, \forall, \exists
```

KaTeX 渲染覆盖所有位置：2D/3D节点标签、面板内容、图例、提示信息。

### 9.2 自适应复杂度

```
初学者模式：
  • 显示所有标签和说明
  • 限制可选群的范围
  • 提供逐步引导

进阶模式：
  • 隐藏部分提示
  • 允许自定义群
  • 显示抽象结构

专家模式：
  • 纯符号界面
  • 批量操作支持
  • 导出计算结果
```

---

## 10. 性能优化

### 10.1 渲染优化

- **大型群（S₄以上）**：保持 SVG 渲染（通过性能守卫和缓存控制）
- **大量节点**：使用 WebGL (Three.js) 渲染（3D 视图）
- **懒加载**：只渲染可视区域内的元素

### 10.2 计算优化

- **缓存**：子群、陪集、直积乘法等计算结果缓存；多面体顶点/边缓存
- **主线程计算**：所有计算均在主线程执行（无Web Worker），通过 `useTransition` 保持UI响应
- **性能守卫**：子群/共轭类/中心计算 cutoff 降至 60（order>60 返回占位结果，大群通过后端 API 计算）
- **渐进计算**：先显示占位符，后通过混合计算层填充结果

---

## 11. 持续更新记录

| 版本 | 更新内容 | 日期 |
|------|----------|------|
| 1.0 | 初始版本，涵盖基本可视化原则和策略 | 2026-04-20 |
| 1.1 | 新增已实现功能的可视化细节 | 2026-04-20 |
| 1.2 | KaTeX渲染、子群格视图、多视图模式、子集分析、i18n | 2026-04-28 |

---

## 12. 已实现功能

### 12.1 S₃ 对称群可视化

**Cayley图**
- 圆形布局，6个节点均匀分布
- 生成元 σ₁₂ (红色) / σ₂₃ (青色) 带箭头曲线边
- 节点可点击选择，高亮当前选中元素
- 支持hover效果，显示节点详情

**Cayley乘法表**
- 6×6 交互式表格
- 点击单元格查看运算结果
- hover高亮对应结果

**键盘导航**
- ←/→ 键切换选中元素
- 实时更新侧边栏信息

**动画效果**
- 节点和边的过渡动画
- 动画速度可调节 (0.25x - 2x)

### 12.2 多群支持

- 循环群 Zₙ (n=1..120)：圆形布局 / 螺旋布局
- 二面群 Dₙ (n=3..12)：双环布局
- 交错群 Aₙ (n=3..5)：力导向布局
- S₄ (24阶)：截角立方体/菱形截角八面体/截角八面体变体等多形状3D模板
- A₅ (60阶)：截角二十面体/截角十二面体3D模板
- S₅ (120阶)：支持（通过群工厂/会话恢复）
- V₄ (Klein四群)、Q₈ (四元数群)
- 直积群：Z₄×Z₂、Z₂³、Z₃×Z₃、Z₆×Z₂ (晶格布局)
- 任意G×H直积构建（最大阶144，三种构建模式）

### 12.3 子群格视图 (SubgroupLatticeView)

- Hasse图表示子群的包含关系
- 节点按层级排列（平凡子群在底部）
- 正规子群用紫色高亮
- 边表示直接包含关系
- 支持节点点击查看子群详情

### 12.4 对称性视图 (SymmetryView)

- 多面体几何体 (正四面体、立方体、正八面体、正二十面体、正十二面体)
- 群元素映射为几何旋转变换
- 双击元素触发动画演示旋转操作
- 旋转轴与交点标记可视化
- 轴方向从几何数据运行时计算，无需硬编码

### 12.5 多视图浮动窗口

- 支持同时打开多个浮动窗口
- 每个窗口可显示不同视图模式
- 窗口可拖拽、调整大小
- 主画布与浮动窗口联动（共享同一群状态）

### 12.6 子集保存与分析

- 选中元素集保存为子集（8色调色板）
- 自动检测乘法封闭性（子群判定）
- 自动检测共轭封闭性（正规子群判定）
- 可管理多个子集

### 12.7 国际化 (i18n)

- 支持中文 / English 切换
- 所有UI文本、提示信息、操作历史均翻译
- 语言偏好持久化到 localStorage

### 12.8 小群预计算注册表

- 阶<12的所有群（共19个）自动索引
- 预计算子群、正规子群、共轭类、中心数据
- 懒加载机制，首次访问时计算

### 12.9 陪集分解可视化

- 左/右陪集切换显示（左乘/右乘模式）
- 选中子群后自动计算陪集分解
- 显示全部陪集验证 Lagrange 定理（|G| = |H| × [G:H]）
- 乘法表内陪集单元格彩色矩形条纹高亮
- 16色调色盘区分不同陪集

### 12.10 直积群构建系统

- 支持任意两群的直积 G×H（最大阶144）
- 三种构建模式：cayley（基于Cayley表）、table（基于乘法表）、direct（直接群运算）
- 源群/目标群选择器，支持导入当前群
- 直积群符号列表 localStorage 持久化，刷新后自动恢复
- 智能3D形状选择：全循环因子→lattice、一循环→cylinder、无循环→torus

### 12.11 2D Cayley图多形状布局

- 九种节点排列形状：circular（圆形）、grid（网格，直积群）、spherical（球面投影）、concentric（共轭类同心环）、dualRing（双环旋转/反射）、archimedean（阿基米德螺旋）、spiral（多圈螺旋）、coil（线圈）、projection3D（3D平面投影）（另有独立陪集条带视图）
- 通过左侧面板下拉菜单切换
- 可用的形状集合按群类型动态过滤
- 智能默认形状选择（按群类型自动匹配最佳形状）
- 节点位置优先级：用户拖拽 > 形状布局 > circular兜底
- 力导向面板按钮仅在非语义布局时启用

### 12.12 欢迎页群预览弹窗

- 点击群记号弹出圆形预览（210px固定SVG）
- 三种随机预览风格：ring（环形排列）、generators（生成元箭头）、orders（按阶染色）
- ORDER_COLORS 映射：1(黄) 2(红) 3(青) 4(紫) 5(橙) 6(绿) 8(蓝) 10(金) 12(粉)
- 弹性入场动画，再次点击或点击背景关闭

### 12.13 深色/浅色主题切换

- 右上角切换按钮，CSS自定义属性驱动
- 支持系统偏好检测（prefers-color-scheme）
- localStorage 记忆用户选择

### 12.14 会话保存与恢复

- 当前群和视图自动存入 localStorage
- 刷新页面后自动恢复上次状态
- 点击左上角标题返回欢迎页，同时清除已保存会话

### 12.15 视图导出

- SVG视图导出SVG矢量图（serializeSvg内联CSS）
- 3D视图导出PNG截图（canvas.toDataURL）
- 对称性视图支持GIF动图导出（gifenc库，20fps录制2秒）
- 通过左侧面板「操作与子集」区域按钮触发

### 12.16 性能守卫与大阶群支持

- 子群/共轭类/中心计算 cutoff 降至 60（order>60 返回占位结果）
- Cayley边预算同步限流
- 直积乘法缓存（pipe分隔DP的multiply/inverse结果Map缓存）
- 所有计算在主线程执行，useTransition 保持UI响应

---

*本文档将持续更新，记录更多可视化策略和最佳实践*

## 13. 布局路由与后端 FastAPI 规范

本节定义针对直积/大阶群布局的工程化路由策略、预计算数据格式与后端 API 规范（FastAPI）。目标是：
- 对小阶群使用静态表（precomputed），直接读取布局数据；
- 对中阶群在浏览器计算（frontend），尽量利用现有的 torus/fiber/layered 算法；
- 对大阶群或复杂生成元走后端（backend）计算，支持异步队列、缓存与谱/Kronecker 优化。

13.1 路由判定（阈值与启发式）

默认阈值（可配置）：

- PRECOMPUTED_MAX_N = 128
- FRONTEND_MAX_N = 800

启发式判定按两个维度：节点数（order）与复杂度评分（complexityScore）。复杂度考虑因子类型与是否存在跨因子生成元。示例评分规则：

- 循环因子 Z_n: 1
- 二面因子 D_n: 2
- 对称/交错 S_n/A_n: 6
- 四元数/特殊 Q_8/V_4: 5
- 其它任意/未知: 3

若存在混合（mixed）生成元（生成元同时改变多个因子坐标）则将 score 乘以 2。总评分低于阈值且节点数在 FRONTEND_MAX_N 范围内时优先前端计算。

示例 TypeScript 判定函数：

```ts
// src/utils/layoutRouting.ts (建议路径)
export const PRECOMPUTED_MAX = 128;
export const FRONTEND_MAX = 800;

function factorScore(symbol: string) {
  if (/^Z/.test(symbol)) return 1;
  if (/^D/.test(symbol)) return 2;
  if (/^S\d+/.test(symbol) || /^A\d+/.test(symbol)) return 6;
  if (/Q|V4/.test(symbol)) return 5;
  return 3;
}

export function computeComplexityScore(factors: string[], mixed = false) {
  let s = factors.reduce((acc, f) => acc + factorScore(f), 0);
  if (mixed) s *= 2;
  return s;
}

export function chooseRoutingMode(group): 'precomputed'|'frontend'|'backend' {
  const N = group.order;
  const factors = inferFactorsFromSymbol(group.symbol); // 尝试解析 Z4xS3 等
  const mixed = detectMixedGenerators(group.generators);
  const score = computeComplexityScore(factors, mixed);

  if (N <= PRECOMPUTED_MAX && hasPrecomputed(group)) return 'precomputed';
  if (N <= FRONTEND_MAX && score <= 20 && !mixed) return 'frontend';
  return 'backend';
}
```

13.2 预计算数据格式与存放位置

建议把预计算的布局 JSON 放在 public/precomputed_layouts/，命名按照规范化 groupKey，例如 `S4xZ5.json`。仅需存储节点位置（positions），边可在客户端根据当前 multiplyType 与启用的 actions 动态重建（通常 cheap）。示例 JSON：

```json
{
  "groupKey": "S4xZ5",
  "symbol": "S_4 \\times Z_5",
  "order": 120,
  "layoutMode": "fiber",
  "positions2d": {
    "e": [0, 0],
    "(12)": [1.2, 0.1],
    "(1234)": [-0.5, 1.1]
  },
  "positions3d": null,
  "meta": {
    "computedAt": "2026-05-04T08:00:00Z",
    "method": "fiber-compose"
  }
}
```

13.3 后端 API 设计（FastAPI）

基本路由：

- POST /api/v1/layout/product
  - 请求 body:
    - groupKey?: string (优先) 或 完整 group 描述 { elements, generators, symbol }
    - mode?: 'auto'|'torus'|'fiber'|'layered'|'spectral'
    - multiplyType?: 'right'|'left'
    - options?: { dim?: 2|3, spectralK?: number, eps?: number }
  - 返回：
    - 200 OK (立即返回布局)
      ```json
      {
        "status": "done",
        "layoutMode": "fiber",
        "positions": { "id1": [x,y], ... },
        "meta": { "method": "kronecker-spectral", "timeMs": 1234 }
      }
      ```
    - 202 Accepted (已排队)
      ```json
      { "status": "queued", "jobId": "uuid", "eta": 12 }
      ```

- GET /api/v1/layout/jobs/{jobId}
  - 查询任务状态（queued|running|done|failed），done 时返回结果；支持取消（DELETE）以释放资源。

- GET /api/v1/layout/precomputed/{groupKey}
  - 直接读取预计算 JSON（可用于调试/浏览器快速加载）。

可选：WebSocket / SSE 用于实时进度与增量结果（先返回 coarse positions，再返回细化结果）。

13.4 后端实现建议（技术栈与优化）

- 框架：FastAPI + uvicorn（原型可同步实现），队列：Celery / RQ / Dramatiq + Redis（异步/扩展）。
- 数值库：SciPy (sparse.linalg.eigsh / lobpcg)、NumPy、NetworkX（备用）。
- Kronecker 优化：当生成元可分离且 Cayley graph 为 Cartesian product 时，利用因子图的谱（特征向量）通过 Kronecker 组合快速构建低维嵌入，避免对完整 N×N 矩阵做特征分解。
- 缓存：请求 body 的哈希作为 key（Redis 或文件系统）。对重复请求直接命中缓存并返回。
- 资源控制：每个 job 设置超时、最大内存与节点数限制，拒绝或降级超大请求。

13.5 前端集成点（代码位置建议）

- src/utils/layoutRouting.ts — 路由判定函数（chooseRoutingMode）
- src/utils/precomputedLoader.ts — 从 public/precomputed_layouts 加载 JSON
- src/utils/backendLayout.ts — 封装 backend API 客户端（POST/轮询/取消）
- src/core/algebra/forceLayout.ts — 增加 computeProductLayout(elements, factors, mode, options)
- src/context/GroupContext.tsx — 新增配置：cayleyLayoutSource (auto|precomputed|frontend|backend) 与 productLayoutParams
- src/components/Panels/LeftPanel.tsx — 展示布局来源、强制覆盖选项、后端 job 状态指示

示例前端调用（简洁）：

```ts
// src/utils/backendLayout.ts (建议)
export async function requestServerLayout(payload) {
  const res = await fetch('/api/v1/layout/product', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(payload)
  });
  if (res.status === 200) return res.json();
  if (res.status === 202) {
    const { jobId } = await res.json();
    // 简单轮询（可替换为 SSE/WS）
    while (true) {
      await new Promise(r => setTimeout(r, 1000));
      const j = await fetch(`/api/v1/layout/jobs/${jobId}`).then(r=>r.json());
      if (j.status === 'done') return j.result;
      if (j.status === 'failed') throw new Error('layout failed');
    }
  }
  throw new Error('server error ' + res.status);
}
```

13.6 UX 与降级策略

- 请求发出前：先走 chooseRoutingMode 做本地决定；若选择 backend，UI 显示队列/进度并允许取消。
- 后端失败或超时：自动回退到前端力导向布局（runForceLayout），并在提示中说明原因。
- 大图（N>5000）：默认聚合/抽样视图，提供“展开细节”按钮触发后端任务或按切片逐步渲染。

13.7 推荐实施步骤（最小可行路径）

1. 实现路由判定 + 预计算 Loader（public/precomputed_layouts）并集成到现有布局入口（2–5 小时）。
2. 前端实现 torus/fiber/layered 布局并作为 frontend 分支（6–12 小时）。
3. 后端 FastAPI 原型（同步实现 + 文件缓存），支持 POST /api/v1/layout/product（1 天内可做最小可用版本）。
4. 后端扩展：队列、Kronecker 优化、缓存、SSE（1–2 天）。

13.8 最小测试用例（示例）

- 预计算文件： Z12xZ8.json（torus/grid，12×8）
- 中阶前端： S4xZ5（fiber，S4 基底每点 5 个小环）
- 后端示例： S_5 × S_4（若阶数过大，走后端谱分解）

---

## 持续更新记录

| 版本 | 更新内容 | 日期 |
|------|----------|------|
| 1.0 | 初始版本，涵盖基本可视化原则和策略 | 2026-04-20 |
| 1.1 | 新增已实现功能的可视化细节 | 2026-04-20 |
| 1.2 | KaTeX渲染、子群格视图、多视图模式、子集分析、i18n | 2026-04-28 |
| 1.3 | 布局路由与后端 FastAPI API 规范 | 2026-05-04 |
| 1.4 | 陪集分解可视化、直积群构建系统、2D凯莱图多形状、深色/浅色主题、会话保存恢复、视图导出(SVG/PNG/GIF)、欢迎页群预览弹窗、赞助链接、3D直积群智能形状选择、大阶群性能守卫、直积乘法缓存 | 2026-05-15 |
| 1.5 | 新增 6 种 2D Cayley 图形状 (concentric/dualRing/archimedean/spiral/coil/projection3D，陪集条带升级为独立视图)、智能默认形状选择、状态管理模块化重构 (7 providers)、forceLayout 文件拆分、Python FastAPI 后端、混合计算系统 | 2026-07-16 |

---

*本文档将持续更新，记录更多可视化策略和最佳实践*
*最后更新: 2026-07-16*
