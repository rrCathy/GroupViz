# 变更记录 (Changelog)

> 所属文档集：GroupViz 开发文档。入口见 [AGENTS.md](../AGENTS.md)。
> 本文档沉淀**已完成**的功能里程碑与逐次开发记录（2026-08-15 由 ROADMAP §5 决策记录表与已完成规划章节整体迁入）；规划性内容（未做事项、决策）见 [ROADMAP.md](ROADMAP.md)。

## 1. 已完成里程碑

### 1.1 群作用系统（P0）— ✅ 已完成（v1.8）

当前最大的数学缺口：已有陪集、同态、商群，但缺少"作用"这一统一框架。现已补齐统一框架。

- **数学内容**：任意有限群 G 作用在集合 X 上；轨道 Orb(x)、稳定子 Stab(x)；轨道-稳定子定理 `|Orb(x)|·|Stab(x)| = |G|`；Burnside 引理（可选）。
- **作用来源**：群在陪集空间 G/H 上的左乘作用；群在自身上的平移/共轭作用；自定义作用（按元素指定置换）；共轭/正则/陪集/自定义/Sylow 五来源（Sylow 属 §1.2）。
- **可视化**：轨道视图（轨道划分、代表元、轨道-稳定子定理数值自检）、Schreier 图（生成元作用图）、稳定子结构与共轭稳定子展示。
- **工程**：`actions` 领域 Provider；核心逻辑 `core/algebra/actions.ts` 纯函数 + 单测；i18n zh/en 成对补键；与陪集条带（cosetstrip）、同态/置换表示视图打通。
- 几何顶点集置换作用：v1.8 曾开发后移除（视觉不佳；正确路径 = 按元素阶在几何体上找 n 次轴并组织各轴，联系空间群/点群 DLC，暂缓，见 docs/ACTIONS.md §4）。

### 1.2 Sylow 定理可视化（P0）— ✅ 已完成（2026-08-06）

- **数学内容**：Sylow p-子群查找——p-元素 → 循环 p-子群 → pair-join 闭包闭合，本地算到 S₅=120（SYLOW_MAX_ORDER=240 守卫）；逐素数验证计数定理 `n_p ≡ 1 (mod p)`、`n_p | m`（`|G| = p^k·m`）；共轭性（所有 Sylow p-子群互为共轭）。
- **可视化**：Sylow 型视图（SylowView.tsx，第 11 种视图模式）——元素节点 + p 可选素数；凯莱图布局（点击子群边切换为子群生成元作用）；单选 → 陪集条带（Lagrange）；Ctrl/⌘/⊕ 复选两个子群 → 上下布局 + 竖直共轭箭头（Sylow 第二定理）+ 子群内部生成元边（P 青 / Q 紫）；轨道视图验证（G 共轭作用在 Syl_p(G) 上，轨道大小 = n_p，稳定子 = 正规化子）。
- **收尾**：欢迎页"即将推出"列表已移除 Sylow 项；Sylow 概览面板（OperationsPanel 第 5 tab）与子群格高亮联动已被视图取代并移除。

### 1.3 数学缺口补全（M1–M4、M8）— ✅ 已完成（2026-08-10）

- **M1 正则作用 + 陪集作用**（✅ 2026-08-10）：左平移作用 = Cayley 定理可视化（G ≅ Sym(G) 的嵌入，正则表示视角）；G ↷ G/H 传递作用（稳定子 = 共轭 H）；与轨道-稳定子面板打通；types/actions/context/panel/ActionView/RightPanel/i18n + 11 单测（actions.test.ts）。
- **M2 GL(2,p)**：纯 TS 2×2 矩阵模 p 实现（无需 GAP 数据）；GL(2,2) ≅ S₃、GL(2,3) = 48 阶（|SL(2,3)| = 24）；面板/按阶创建/groupFactory 接入（`core/groups/GeneralLinearGroup.ts` + generalLinearGroup.test.ts）。
- **M3 作用深化三小件**（✅ 2026-08-10）：共轭作用特化标注（固定点 = Z(G) 自检，轨道=共轭类/Stab=C_G(x)）；Burnside 引理自检（|X/G| = (1/|G|)·Σ|Fix(g)|，右侧通用行）；Sylow 作用面板入口（p 素数下拉 + 按钮，createSylowAction 接线）；computeBurnsideCount + 右侧固定点/Burnside 标注行 + 面板 Sylow 行 + 7 单测。
- **M4 半直积分解展示（识别方向）**（✅ 2026-08-10）：findSemidirectDecompositions（order≤60 守卫，去重/排序 verified 优先）+ buildSubgroupGroup/minimalGenerators helper + verified 双通道（isoSymbol 相等 || 不变量回退：isAbelian + 元素阶多重集）+ 面板分解列表（✓/✗ 徽标、点击切换）+ 分裂短正合列 TeX + 14 单测（semidirectDecompositions.test.ts，共 23）；S₄ 双型分解（A₄⋊C₂ / V₄⋊S₃）、D₁₂ 19 候选、Q₈ 无分解均验证。
- **M8 群族识别升级（部分完成，2026-08-10）**：subgroups.ts 的 detectIsomorphicGroup 阿贝尔分支升级为**有限阿贝尔分类定理精确识别**（detectAbelianType：因子链 d1|d2|…|dk 枚举 + 公式阶分布匹配 Σ Π φ(eᵢ)，纯算术无构造群、支持任意因子数 C₂×C₂×C₄ 等）；非阿贝尔分支保留 Dₙ/Q₈/A₄/A₅/S₃/S₄/S₅ 候选；8 处调用方（商群/Aut/半直积/M4 verified/展示群）自动受益；6 单测；gappy IdGroup 砍除（E1 未做）。

### 1.4 支撑项与质量 — ✅ 已完成

- **群结构分析升级**：换位子群 [G,G]、子群列（导列/上·下中心列/合成列）、可解/幂零判定——见 core/algebra/series.ts（SERIES_MAX_ORDER=240 守卫，大群二期走后端）。
- **群族扩充**：小群注册表扩至阶 1-31 全部 93 个群（GAP 4.16 SmallGroups 导入）；GL(2,p) 单独交付（§1.3 M2）。
- **质量**：覆盖率 75.8% → 85%+（2026-08-09：88.64% stmts / 91.18% lines，35 文件 706 tests；2026-08-10：88.92% stmts / 91.34% lines，37 文件 763 tests；2026-08-13：88.98% stmts / 91.82% lines；2026-08-15 起 vitest.config 设 CI 阈值 stmts/lines/funcs ≥ 85、branches ≥ 70，实测 88.56% stmts / 91.49% lines）；后端计算与前端本地兜底功能对齐（S₆ 全通）。

## 2. 逐次开发记录

> 原 ROADMAP §5 决策记录表整体迁入（含决策型与工作型记录）。后续每次开发提交在本表追加一行。

| 日期 | 记录 |
|------|------|
| 2026-08-06 | Sylow 定理可视化落地：core/algebra/sylow.ts 专用查找算法 + Sylow 型视图（元素节点/凯莱图/陪集条带/共轭双选/子群边）+ ActionView Sylow 共轭作用验证；28 文件 542 tests 全绿；版本 1.9.0 |
| 2026-08-07 | 群展示系统落地：core/algebra/presentations.ts（解析器 + Todd–Coxeter 陪集枚举 TC_MAX_COSETS=3000 + 构建 + presentationOf 标准展示分发 + 通用关系发现器）、左侧「群展示」面板（8 预设/防抖预览/持久化+草稿）、关系回路视图（Van Kampen 图 + 关系动画）、展示乘法表视图（单词求值路径）；32 文件 630 tests 全绿；版本 1.10.0 |
| 2026-08-09 | 测试补齐：覆盖率 84.02% → 88.64% stmts（91.18% lines），新增 export.test.ts（15 tests，GIF 编码/SVG/canvas 导出全路径）、exportApi.test.ts（16 tests，导出桥单例 + DOM stub）、utils.test.ts 补 9 用例（groupFactory 嵌套直积/上标幂/回退/越界）；修复 cayleyTree genElsOverride 测试 coverage 下 5s 超时（30s）；35 文件 695 tests 全绿 |
| 2026-08-09 | 小群全量导入：GAP 4.16 SmallGroups 库导出阶 1-31 全部 93 个群（smallGroupData.ts 乘法表数据 + createTableGroup 构建 + structureToSymbol 符号化，结构冲突回退 SmallGroup(n,i)，Dic₃ 强制 Z₃:C₄）；按阶创建面板改为注册表驱动（阶 1-31）；createGroupFromSymbol 支持 D₈-D₁₅/新符号/SmallGroup(n,i)/注册表兜底（会话恢复兼容）；35 文件 704 tests 全绿 |
| 2026-08-09 | 按阶创建面板补回 A₅(60)/S₅(120)（注册表驱动化时丢失回归）；符号 ":" 半直积提示（order-hint 文案，zh/en）；35 文件 706 tests 全绿 |
| 2026-08-10 | 半直积分解（M4）落地：core/algebra/semidirectDecompositions.ts（findSemidirectDecompositions order≤60 守卫 + buildSubgroupGroup/minimalGenerators + verified 双通道）；面板分解列表 + 分裂短正合列 + 点击切换；14 新单测（共 23）；37 文件 763 tests 全绿 |
| 2026-08-10 | M5 第二/第三同构定理验证开发完成并实测通过（38 文件 780 tests），随后**决定移除**：纯数值验证、无可视化价值，不影响本项目；同步明确后续任务筛选标准 = 新建可视化或可视化优化，非可视化任务（M6/M7/M8/E2/P1/P2/P3）不做；群论计算器/多对象工作台方向搁置，作为独立新引擎另行规划 |
| 2026-08-10 | M8 群族识别升级（部分完成）：subgroups.ts detectIsomorphicGroup 阿贝尔分支升级为有限阿贝尔分类定理精确识别（detectAbelianType：因子链枚举 + 公式阶分布 Σ Π φ(eᵢ)，支持任意因子数）；旧实现仅支持 2 因子直积，现 C₂×C₂×C₄ 等全部可识别；8 处调用方自动受益；6 新单测（38 文件 775 tests 全绿）；gappy IdGroup 砍除（E1 未做） |
| 2026-08-09 | 规划定稿（覆盖此前"仅口头确认、不更新 ROADMAP"的决定）：近期 = 数学缺口补全 M1–M8 + 工程质量 E1（gappy 后端集成）/E2（细节收尾）+ FGVE 预铺路 P1（群 JSON 导出+round-trip，乘法表导入否决）/P2（引擎化审计）/P3（benchmark）；推进顺序 M1→M8→E1→E2→P1→P3 |
| 2026-08-09 | 表示论可视化定稿：特征标表 = 中期正式交付（经典矩阵表 + 热力着色 + 正交性验证 + 共轭类联动，数据源 gappy CharacterTable/Irr/ConjugacyClasses，见 ROADMAP §2.4）；矩阵表示动画 → GVL（ROADMAP §3.6）；一维特征标并入特征标表（近期暂缓） |
| 2026-08-11 | 直积 2D 形状（B3 可视化优化）：classifyDirectProduct2D 智能分类（全循环→grid、恰一循环因子→cylinder、无循环→torus）；cylinderLayout2D 同心多层环（循环因子径向层叠 + stagger 错位 + Dₙ 双环副本）、torusLayout2D 主轴环挂副本；factorPipeGroups/parseCompactFactors 紧凑符号因子识别（C₂²×S₃ = 2 组、S₃² 幂合并拆段修复）；buildFactorSubgroup 因子临时群重建；13 新单测；38 文件 788 tests 全绿 |
| 2026-08-11 | 16 阶注册表群直积形状修复：①tableGroupGridFactors 聚类算法（union-find 生成元不可交换聚类 + 混合进制枚举）覆盖注册表群（非 pipe），C₄×C₄/Z₄×Z₂×Z₂/C₂⁴ 全部 4×4 满网格（修复 colMap 只存代表元素致 L 形重叠）；②hasTopLevelTimes 顶层 \\times 检测——'(Z₄×Z₂):Z₂' 半直积不再误判直积，恢复圆形布局；③parseProductFactors pipe 分支支持 3+ 因子平均拆分；④getAvailableShapesForView 直积去重（classify=grid 时不重复 push，修复 React key 冲突）；6 新单测；38 文件 794 tests 全绿 |
| 2026-08-11 | 注册表非 pipe 直积 cylinder 聚类支持（B3 延续）：clusterFactorGroups/tableGroupFactorSplit/clusterIsCyclic 按生成元交换性聚类因子 + 循环性判定，buildFactorSubgroup/cylinderLayout2D 加非 pipe 分支，Z₂×D₄/Z₂×Q₈ 显示 2 层同心环（distinct 半径 2）；classifyDirectProduct2D 移除 isPipeProduct 守卫改 isGroupDirectProduct 防御；7 新单测；38 文件 801 tests 全绿 |
| 2026-08-11 | 2D 凯莱图通用策略（直积归组 + 任意因子数甜甜圈）：analyzeDPFactorsGrouped2D/factorPipeGroupsGrouped 相邻同底循环因子归组（C₂×C₂×S₃ → [C₂², S₃]、C₂×C₂×C₂ 合并 C₂³ 单因子退回网格）；classifyDirectProduct2D 新逻辑（单因子→grid、全循环→grid、全非循环→torus 不限因子数、否则 cylinder）；torusLayout2D 重写为嵌套甜甜圈（递归半径链，主轴环 + 每点挂剩余因子乘积副本），注册表群 clusterFactorGroups 聚类笛卡尔分解；cylinderLayout2D 归组索引支持；17 新单测；38 文件 818 tests 全绿 |
| 2026-08-11 | 半直积重布线形状（策略 2）：isGroupSemidirectProduct 识别注册表 ':' 记号（hasTopLevelColon 顶层检测）；getSemidirectProductMeta 经 findSemidirectDecompositions 规范分解恢复 N/H/φ；semidirectFactorMap 每元素唯一分解 g=n·h（pipe id 拆分 / 注册表 n=g·h⁻¹ 代数分解）+ semidirectFixedPoints 固定点高亮（GroupCanvas 生效）；注册表半直积群默认重布线形状；9 新单测；38 文件 827 tests 全绿 |
| 2026-08-11 | 半直积重布线美观：powerRingOrder 生成元幂序 BFS（循环群正多边形/V₄ 方形环序/bit 向量特判/覆盖不全回退字典序，生成元边旋转对称免交叉）；rN/rH 自适应（rN = max(minRN·1.6, R·0.14)、rH = max(minRH·1.6, (rN+28+copyGap/2)/sin(π/m))、copyGap = max(90, rN·1.35)、R = minDim·0.32、minRN/minRH = 阶·56/2π）防节点重叠 + 副本盘缘间隙 ≥90px；5 新单测；38 文件 832 tests 全绿 |
| 2026-08-12 | 2D 凯莱图布局修复第二轮（按阶创建注册表路径）：注册表生成元标准化（createTableGroup buildGenerators D_m 重建 (r,s) 生成对）；ringOrder 数字感知 gN 环序（C16 圆环连续）；semidirectProductLayout 盘内 dihedralSnakeOrder 蛇形环序（(C4xC2):C2 盘内边整齐）；6 新单测；38 文件 862 tests 全绿 |
| 2026-08-12 | 2D 凯莱图布局修复四连：①循环群默认 circular（C₁₆ 等注册表循环群不再螺旋乱排，spiral 仅手动可选）；②powerRingOrder Cₘ×C₂ pipe 特判（(C₄×C₂):C₂ 半直积盘内环序：外圈 t0 行升序 + 内圈 t1 行降序，生成元边相邻免跳对角）；③注册表 Dₙ 双环（splitDihedralElements 元素阶分类：m=|G|/2 找阶 m 元素 r → 旋转=⟨r⟩ 幂闭包、反射=其余、配对 sᵢ=rⁱ·s₀；dualRingLayout/cayleyCircleLayout value 分类失败自动回退，按阶创建 16-30 阶二面体 D₈/D₉/D₁₀ 双环恢复正常）；④cylinderLayout2D 去掉 stagger 各层同相位（C₃×S₃ 循环因子边成径向母线，俯视圆柱感）；24 新单测；38 文件 856 tests 全绿 |
| 2026-08-13 | GAP 表群可视化完整处理（批量导入准备）：①表群元素词标签（assignWordLabels BFS 最短词 + D_m applyDihedralNormalForm 正规形，恒等元 e/词 a、a b、a^{2} b，生成元元素标签=生成元名，同态/陪集/循环布局 label 匹配对齐，id 不变）；②tableFactorSearch 布局兜底（聚类失败/退化时按群符号从 findAllSubgroups 搜因子：阶+循环性匹配+有序乘积覆盖验证，cylinder/torus/网格全接上——C₃×S₃/C₂×A₄/C₅×S₃ 同心多层环、C₄×C₂×C₂ 4 层×4 节点；圆形兜底 gN 表群 id → powerRingOrder 生成元幂序，C₁₆ 恢复正多边形）；③tableGroups.audit.test.ts 惰性审计（279 tests 扫全部 66 表群：默认/全部形状/圆形/生成元边/半直积 meta，防布局静默回退回归）；39 文件 1174 tests 全绿（覆盖率 88.98% stmts / 91.82% lines） |
| 2026-08-14 | 群展示发现器极小化展示：canonicalCyclicForm 循环旋转/逆折叠（w=e 蕴含旋转与逆也是关系，QD16 868 条共轭冗余词折叠为少数规范词）+ 贪心移除（保护 K 条最短生成元阶关系，其余词 TC 验证逐个移除，1.5s 预算守卫）；**presentationOf 对 QD16 特判返回标准展示 ⟨a⁸, b², bab=a³⟩**（按元素阶定位阶 8 生成元 a，GAP 生成元 g1 阶 4 只能导出非标准 ⟨a⁴, b², (ba)⁴⟩）+ **发现器结果缓存**（symbol|order|生成元数键，信息栏/树视图反复调用不再重算 ~1.6s，QD16 加载 1.6s→64ms）；测试断言补 QD16 标准形式与缓存复用；39 文件 1174 tests 全绿 |
| 2026-08-14 | QD16 命名半直积走重布线：isNamedRewiringGroup 命名特判（符号无 ':' 但结构为半直积，不影响全局 isGroupSemidirectProduct）；getSemidirectProductMeta 经 namedSemidirectOrderPair 恢复 C₈⋊C₂ 分解（generators.length===1 锁定循环 C₈ 而非 Q₈，φ(b)(a)=a³ 实测验证），默认 2D 形状 rewiring、可用 [rewiring,circular,spherical,concentric]；semidirectProduct.test.ts 新增 QD16 meta 恢复测试；浏览器实测凯莱图默认选中重布线；39 文件 1175 tests 全绿 |
| 2026-08-14 | QD16 生成元标准化 (a,b)：GAP 生成元 g1 阶 4（=标准 a²）导致重布线内环只有 4 元环；buildGenerators 新增 QD16 特判按元素阶定位阶 8 元素 a + 阶 2 元素 b（bab=a³ 且闭包=全群）替换 rec.gens；重布线双盘各显完整 8 元环（a 边 16 条 / b 边 8 条浏览器实测）；smallGroups.test.ts 新增标准化断言 + 非二面体群保持 GAP 生成元用例收紧；39 文件 1176 tests 全绿 |
| 2026-08-14 | SmallGroup(16,13)=(C₄×C₂):C₂ 命名半直积走重布线（参考 Group Explorer GroupExplorer.html，GE 标准展示 ⟨a,b,c | a⁴=b²=c²=1, cbc=ba², ab=ba, ac=ca⟩）：isNamedRewiringGroup 特判扩展到 'SmallGroup(16,13)'（符号冲突回退无 ':'，startsWith('S') 原本误判 projection3D → getDefaultShape2D/getAvailableShapesForView 的 isSA 前缀判定排除 'SmallGroup('）；getSemidirectProductMeta byNamed 改用 normal.isAbelian 锁定交换 N=C₄×C₂（排除同阶 Q₈），φ(c)(b)=b·a² 实测验证；buildGenerators 按 rec.n===16&&rec.i===13 特判标准化为 GE (a,b,c)（a 阶4、b 阶2與a交換、c 阶2 且 cbc=ba²、closureSize 闭包=16），GAP gens [2,3,4] 阶 2,2,4 → 标准三元；浏览器实测：默认重布线、3 生成元 a/b/c、16 节点、a 边 16/b 边 8/c 边 8（双盘 C₄×C₂⋊C₂，与 GE 一致）；smallGroups.test.ts 新增 (16,13) 标准化断言（ab=ba、ac=ca、cbc=ba²、闭包 16）；39 文件 1176 tests 全绿 |
| 2026-08-14 | Q₁₆ (16,9) 凯莱图生成元标准化 (a,b) 对齐 Group Explorer Q_8.group（GE 下标惯例 Q_阶/2，标准展示 ⟨a,b | a⁸=1, a⁴=b², aba=b⟩，gens "1 8"→a 阶 8 / b 阶 4）：buildGenerators 按 rec.structure==='Q16' 特判（a=阶 8 元素、b=阶 4 元素满足 b²=a⁴ 且 aba=b 且 ⟨a,b⟩=全群），代替 GAP gens [2,3] 两个阶 4 元素；浏览器实测 2D circular 凯莱图 a 边 16（8 元环双向）+ b 边 16；QD16/(16,13) 3D 走 semidirectCylinder（isNamedRewiringGroup 引入 getAvailableShapes3D/getDefaultLayout3D，layout3D.ts semidirectCylinder 已支持命名特判，浏览器实测 QD16 3D 默认 semidirectCylinder）；smallGroups.test.ts 新增 Q₁₆ 标准化断言（a 阶8/b 阶4、b²=a⁴、aba=b、闭包 16）；39 文件 1178 tests 全绿 |
| 2026-08-15 | 凯莱图形状分配 11 项审计修正：①projection3D 收窄为精确群族（isProjection3DGroup：仅 S₃/S₄/S₅/A₄/A₅，删除 startsWith('S'/'A') 泛化误配）；②archimedean/concentric 从所有可用形状列表移除（非循环群/半直积群不再提供，布局函数与 i18n 键保留）；③ringGrid 推广到任意素数网格（findRingGridDecomposition p∈{2,3}：C₃³=(27,5) 与幂式 'C_{3}^{3}' 3×3 三角环网格；C₂⁴ 无 4 阶环生成元数学上不命中保持 4×4 grid）；④SmallGroup(n,i) 符号冲突类群走重布线（isNamedRewiringGroup 类级放宽 order≤60 && startsWith('SmallGroup(')，SmallGroup(20,3)≅C₅⋊C₄ 经 getSemidirectProductMeta 结构恢复 N/H/φ）；⑤半直积 3D 可用形状收窄为 [spherical,semidirectCylinder,circular]（删依赖直积因子结构、失败静默球化的 lattice/torus）；⑥重布线 φ(h) 扭转（N 环按 φ(h)(n) 索引摆放，QD16(k=3)/C₈:C₂(k=5)/D16(k=7) 辐条连接模式互异）；⑦C₂⁴ 3D 超立方体（isC2Tesseract + hypercube 布局：外立方体×内立方体 r=0.55 同心投影 16 顶点，3D 默认 hypercube）；⑧presentations.ts tableFingerprint 修复（GroupElement.id 为 string 致 TS2363，改 FNV-1a 字符串哈希）；13 新单测；39 文件 1195 tests 全绿（覆盖率 88.98% stmts / 91.82% lines） |
| 2026-08-15 | 3D 轨道控制换成自定义球坐标（Cayley3DView 删除 drei OrbitControls——其每帧 makeSafe 把 phi 钳回 [EPS, π-EPS]，是旧版『上下有角度限制』的根因，▶ 自动旋转先以 enabled=false 绕过、手动拖拽仍被钳，用户反馈后整体替换）：orbit ref 存 theta/phi/radius/target，phi 无界可无限翻越上下两极点；手动拖拽（左键 theta-=dx·0.006 / phi-=dy·0.006、右键平移 target、滚轮缩放 radius 钳 [3,25]）与 ▶ 自动旋转（沿用最后一次拖拽方向与速率约定）共用同一套状态；每帧 position = target + r·sinφ·(sinθ, cotφ 隐式, cosθ)，up=sign(sinφ) 在两极点翻转保持画面正立；resetCamera/dblclick 复位默认视角；浏览器实测（C₂⁴ hypercube）：下拖穿北极 φ→−0.114、上拖穿南极 φ→4.476、▶ 绕行 5 圈零钳制，up 随 sinφ 正确翻转；门禁 lint/test 39 文件 1195 tests/build 全绿；SymmetryView/FreeGroupTreeView 的 OrbitControls 保持不动 |
| 2026-08-15 | 3D 凯莱图 GIF 导出（ViewPanel 导出区 3d 视图新增「GIF 旋转时长」select 3s/5 圈 + 导出 GIF 按钮）：`src/utils/cayley3dControls.ts` 新增注册桥（register/get/unregister，Cayley3DControlAPI：isReady/snapshotOrbit/beginRotation(radPerSec)/endRotation(snapshot)），Cayley3DView SceneContent 注册（仅主视口 .canvas-viewport 实例，useFrame 外部旋转分支优先于 autoRotate，delta 增量），export.ts 新增 cayley3DExportPlan 纯函数（3s/2 圈 60 帧、5 圈 7.5s 150 帧、20fps 可配）与 exportCayley3DGif（逐帧 rAF 离屏 drawImage → quantize 256 → gifenc，异常 reject + finally 恢复相机）；i18n panel.gifOption/gif3s/gif5Cycles；export.test.ts +10 tests（exportCayley3DGif/cayley3DExportPlan/cayley3dControls）；39 文件 1205 tests 全绿；浏览器实测 C₂⁴ hypercube：3s 导出 1.6MB 60 帧、5 圈 4.0MB 150 帧、导出期间采样哈希持续变化（旋转生效）、导出后恢复原视角、0 console error |
| 2026-08-15 | 乘法表大群三策略（TableView：|G|>16 顶部策略栏，默认子群展示）：子群展示 = 随机挑 ≤16 阶子群优先 6-12 阶（合成/循环/`findAllSubgroups`（≤60 阶）多源候选去重、无合适回落随机），随机展示 = 随机挑 6-12 元素，全量展示 = |G|>30 弹确认框 → 全屏模态（fixed 全视口覆盖两侧栏/工具栏）完整 n×n 表，右上角 −/+/⟳ + 缩放滑块 25%-600%（视口锚定）+ 导出 SVG + 退出（ESC 重置为子群展示）；全屏内滚轮=纯上下滚动、拖动平移（React onWheel 被动无法 preventDefault，改原生滚轮滚动 + 滑块缩放分离交互）；主视图画布级平移/缩放 `translate(T) scale(S) translate(offset)` 变换链（滚轮=缩放、双击空白复位，修复子群/随机展示不能缩放移动）；修复行表头标签双重位移错位（<g> translate + text y 均含 ri·cellSize）；i18n table.strategy.*/table.subgroupCaption/table.randomCaption/table.fullConfirmTitle/Msg/table.confirmOk/Cancel/table.fullTitle/table.fullExit/table.fullExport/table.fullZoomIn/Out/Reset/table.zoomSlider/table.fullPlaceholderMsg/table.fullEnter（20 键 zh/en 成对）；全屏背景改用 --bg-canvas（--canvas-bg 未定义导致全透明）；39 文件 1205 tests + lint + build 全绿；浏览器实测 S₄/S₅：默认子群（|H|=8/6）、随机 8 元素、24 阶直接全量、120 阶确认弹窗→全屏 14400 格→滑块 475%→18525px、ESC 复位策略 |
| 2026-08-15 | 3D 凯莱图 GIF 导出改离屏渲染（cayley3dControls 桥扩展 + Cayley3DView/export.ts）：`displayAngVel()` 返回 ▶ 自动旋转当前角速度（拖拽后按最后拖拽向量长度降速 0.35+min(0.65,len/360)，未拖拽 1×2π），导出帧数 = cycles×2π ÷ 展示速度重新推导（≥2，总转角恒整圈无缝循环）——导出动图与展示区转速一致；`frameAt(index, frameDelayMs)` 按帧序号精确求角 = 基准轨道 + 角速度×帧延时×帧序号（与实时渲染耗时无关，大群渲染变慢 GIF 不加速）；`beginRotation` 记录基准 theta/phi/radius/target 并创建独立离屏 WebGLRenderer+相机（preserveDrawingBuffer），渲染完全隔离不触碰实时画面，导出期间展示区照常旋转、结束仅 dispose 离屏渲染器（不再需要恢复相机）；useFrame 移除 externalRotation 分支（外部旋转改纯离屏）；i18n 「约 3 秒」/~3 seconds；export.test.ts +1（displayAngVel 帧数重推导 + frameAt 逐帧 (i, frameDelay) 断言）→ 39 文件 1206 tests 全绿；3D 相机外接球适配（Cayley3DView）：节点云质心 + 最大距离+1.4 外接球半径 R，fitOrbit 视角 = 球直径占视口高 2/3（d=1.5R/tan(fov/2)），minRadius=max(1.5, R+0.8)（不可进球）、maxRadius=max(30, 3×适配距)，切群（symbol|order）/切 3D 形状自动回正，双击复位=适配视角，latestFit ref 防对象身份波动，camera far 400，滚轮缩放方向修正（dY>0 拉近，radius×0.95^(-dY/100)）；主题 token 重构（index.css/App.css）：新增 --accent-blue/green/red/orange/danger(+hover)/--btn-on-accent（深 #0f0f1a / 浅 #ffffff——修复浅色模式 accent 按钮文字不可见）/--text-accent/--bg-elevated/--bg-muted/--bg-panel/--bg-hover/--border-color/--canvas-bg/--input-bg/--panel-bg/--panel-border/--text 别名，App.css ~12 处硬编码 #0f0f1a→var(--btn-on-accent)、#ff6b6b→var(--accent-danger)、杂色→语义 token；z-index 修复（.lang-toggle 100、.history-panel/.hint-box-header 10）；SymmetryView Html 标签 zIndexRange+pointerEvents:none（不再挡画布拖拽）+ 切换按钮主题化；组件 token 化：FloatingViewWindow/SemidirectProductView/SubgroupLatticeView/GroupActionPanel/SemidirectProductPanel/OperationsPanel/RightPanel；39 文件 1206 tests 全绿 |
| 2026-08-15 | 贡献者基础设施：CONTRIBUTING.md（新手引导/项目地图/开发流程/代码与测试约定/文档一致性/风险分级与人工审查/发布流程 扫→修→更→推/PR 清单）；.github/ISSUE_TEMPLATE/（bug_report.yml + feature_request.yml + config.yml，含群与视图/浏览器/主题字段）；.github/PULL_REQUEST_TEMPLATE.md（风险自评 🟢低/🔴高 + 人工审查请求单字段 + 验证清单）；vitest.config.ts 设覆盖率阈值（stmts/lines/funcs ≥ 85、branches ≥ 70，CI 用 test:coverage 强约束）；ci.yml 加 workflow_dispatch 手动触发；README.md/README_zh-CN.md 新增「参与贡献」区块（CONTRIBUTING/ROADMAP/CHANGELOG/Issue 模板入口 + CI 说明）+ 项目结构树补 .github/ 与 CONTRIBUTING.md 行；文档数字同步（Provider 9→12 个领域 Provider 分层、README_zh 1178→1206 tests、覆盖率 88.98/91.82 → 88.56 stmts / 77.78 branches / 92.08 funcs / 91.49 lines 并注明 CI 阈值）；39 文件 1206 tests 全绿 |
| 2026-08-15 | 【文档】新手教程 docs/TUTORIAL.md（EN）+ TUTORIAL_zh-CN.md（zh，14 节：快速开始/工作台导览（左侧 8 分区+右栏信息面板）/7 课实操（选群→6 核心视图→子群陪集 Lagrange→直积半直积展示构建→同态与第一同构→群作用轨道视图 Burnside→Sylow 全流程）/导出与多视图/操作速查/按定理学习路径/疑难解答），所有 UI 入口与控件经源码核对；AGENTS §2 文档索引新增 TUTORIAL 行，README/README_zh 引言新增教程链接，CONTRIBUTING 新手步骤 1 加教程指针；README 视图计数矛盾修复（intro 11 / 功能标题 10 / Usage 9 → 统一 13 + 卡片/入口说明，torus 旧描述改 tree 视图）；ROADMAP 瘦身为只列未做（已完成里程碑与 20 行逐次记录迁入本文件 §1/§2，历史数据保留 verbatim）；doc-sync 数字同步（README_zh 1178→1206、TESTING.md export 25→26、CAYLEY/STATE/UI 3D GIF 离屏与新主题 token 描述）；AGENTS/包/欢迎页版本号 → 1.12.1 |
| 2026-08-16 | 修复 drei Html 覆盖层 z-index 首挂载缺失（用户手机端「说明文字层级太高」二次反馈，v1.12.1 zIndexRange 修复实际依赖 drei useFrame 动态赋值，特定挂载时序下（UnsupportedOverlay→视图切换/并发挂载）useFrame 初始帧未写 `D.style.zIndex`，cssText 初始态 z auto 永久保持，absolute Html div 盖住不受控层）：SymmetryView 3 个 Html（群名/几何说明/变体切换按钮）与 Cayley3DView 节点标签 Html 加 `wrapperClass="gv-html-overlay"`、Cayley3DView 2 个 fullscreen Html（右上群名+|G|+自动旋转/复位、左上乘法律例）加 `wrapperClass="gv-html-fullscreen"`，App.css 新增 `.gv-html-overlay{z-index:2!important}` / `.gv-html-fullscreen{z-index:5!important}` 兜底（层级序：canvas 内容 < 覆盖层 2 < fullscreen 工具栏 5 < hint-box/历史 10 < 抽屉/面板 1100，!important 覆盖内联不管 useFrame 是否运行）；drei wrapperClass prop 直接设外层定位 div className（源码确认 10.7.7）；浏览器实测：手机 390px 对称性视图 computed z=2、手动清除内联 zIndex 模拟 useFrame 未跑 computed 仍 2（CSS 兜底生效）、桌面 1440px 3D 视图 fullscreen 工具栏 z=5 内容正常显示（右上 C₆|G|=6、左上乘法律例）；lint/build/39 文件 1206 tests 全绿 |
| 2026-08-16 | GAP 大群计算引擎 v1.13.0（E1 gappy 换道直连 GAP 4.16）：backend/gap_service.py（direct/cygwin 双模式探测 gap.exe、`symbol_to_expr` 表达式翻译（Cₙ/Dₙ/V₄/Q₈/GL(2,q)/直积/幂^k，不支持→None）、`GV_ser` 序列化（恒等置换 key、元素→置换/binary string、子群→下标列表、fail→"null"）+ `run_script` 进程串行锁 + GV_BEGIN/END 标记 + 15s 超时 + GapComputeError/GapTimeoutError/GapUnavailableError、probe_info）；main.py 六端点 >120 阶自动切 GAP（subgroups/normal-subgroups/conjugacy-classes/center/lattice/properties，`_gap_cache` 进程缓存 + `_gap_elements`/`_gap_subgroups`/`_gap_lattice` 响应适配，非置换群/不支持降级纯 Python）+ 新端点 `/api/compute/series`（derived/upperCentral/lowerCentral/composition，terms+factors 含 is_simple）+ `/api/compute/import-group`（任意 GAP 表达式 ≤4096 阶：SymmetricGroup(3)→S3、SmallGroup(16,8)→QD16，非法 422）+ health 暴露 gap{available,mode,executable}；实测 S₆ 1455 子群/6469 格边/11 共轭类首算 ≈13s 后毫秒级、C₂₀ 非置换群正确降级；前端就绪层：api.ts fetchSeries/fetchImportGroup + ApiSeries/ApiImportGroup/ApiHealth 类型，hybridCompute.ts fetchBackendSeries（>240 自动切后端，terms→GroupElement 映射、平凡项补全、factor label 生成 gapFactorLabel）+ gapFactorLabel 单测，GroupSeriesContext gapSeries/seriesLoading（queueMicrotask 包裹规避 set-state-in-effect，组切换重置），GroupContext 聚合暴露，SubgroupLatticeView 底部链式面板大群 seriesData 优先复用（seriesLoading 提示「后端（GAP）计算系列中…」+ series.loading i18n 键 zh/en）；前端群上限 <240（直积/半直积 144、展示 240）故 GAP 通路为后端能力+就绪层；后端 test_main.py +7（test_health_reports_gap/test_series_api/test_series_invalid_type/test_import_group_api/test_import_group_invalid_expr/test_small_group_structure_detection，needs_gap 跳过标记）33 passed；前端 39 文件 1213 tests 全绿 + lint + build；版本 1.12.1→1.13.0，文档同步（AGENTS §2/§5 GAP 条目、BACKEND.md §3 结构表 + §4 端点表 + 新 §6 GAP 引擎、README ×3、TESTING.md、CHANGELOG 本行、welcome.version） || 2026-08-16 | 修复大群子群格视图整树白屏（S5 实测复现：切子群格视图 → TypeError: Cannot read properties of undefined (reading 'length') → React 无 error boundary 整树卸载）：根因 backendCache.lattice 为后端 raw 结构（nodes 带 elements 数组 + is_normal、edges 用 source/target），SubgroupLatticeView latticeData memo 原样 raw cast 未归一化，centerIdx 等处 
d.elementIds.length 必崩；latticeData memo 现做归一化（elements→elementIds、is_normal→isNormal、level/index/label 补齐、edges source/target→from/to），消除 centerIdx/265/313/394 全部 elementIds 崩溃点；浏览器实测 S5 子群格 156 节点正常渲染 + 导列链式面板 S5⊵A5⊵⟨e⟩（120⊇60⊇1，因子 C2/G60）回归通过、console 0 errors；lint/tsc/39 文件 1213 tests 全绿 |
| 2026-08-16 | 记号导入群：左侧「⇥ 记号导入群」面板 + notationParser（S_5/A_4/PSL(2,7)/C_3×D_4/C_4^2/SmallGroup(n,i)/Aut(S_4) 等记号→TeX 预览+阶+本地/GAP 路径徽标，半直积 : 提示改 SmallGroup）；导入管线 /api/compute/import-group → createGroupFromImport（乘法表→Group，assignWordLabels+applyDihedralNormalForm 复用）；后端导入群注册（_imported_groups+_build_group_from_import，导入后 subgroups/lattice/classes/center/properties/series 全链路可用）+ symbol_to_expr 补 GL/SL/PGL/PSL(n,q) 族 + IsomorphismPermGroup 置换表示注入（PSL(2,7)→PSL(3,2) 179 子群 source=gap 实测通）+ needs_gap 测试；前端 +22 测试（notationParser/importGroup），后端 34 passed，全量 41 文件 1235 tests 全绿；注（后续 2026-08-17 已修复，见下条）：Cygwin bash 调 GAP 在 detached 孤儿进程必挂 120s（exit 124），须前台终端跑 uvicorn |
| 2026-08-17 | 修复 detached/后台下 GAP 计算永久卡死（exit 124 → 422，根因定案）：`-b -q -c 'Read(...)'` 脚本执行完回到 REPL 死等 stdin EOF（capture_output 的 stdin=PIPE 永不关闭），无控制台句柄上下文（Start-Process Hidden/服务/任务计划）更顽固（GAP_READY 已打印、CPU=0 纯等待、120s 被 GNU timeout 强杀）。实验矩阵（detached 对照 8 组：带 QUIT 全成功 / 无 QUIT 全挂，ppid 断链/线程/链深度/脚本重量均排除）后定案，`_run_gap_raw` 双重修复：`stdin=subprocess.DEVNULL`（Read 完立即 EOF 退出）+ `CREATE_NEW_CONSOLE | CREATE_NO_WINDOW`（显式分配隐藏控制台）；新增 `scripts/start-backend.mjs` node 托管 uvicorn（`npm run backend`，--no-reload 可选，stdio inherit + ENOENT 提示 pip install）；detached 端到端实测：import PSL(2,7) 3.3s OK（order=168/PSL(3,2)）+ subgroups 179 source=gap 0.2ms 缓存命中；backend pytest 34 passed 无回归；文档同步 BACKEND.md §2/§6 + AGENTS.md |
| 2026-08-17 | 导入群数据处理修复（用户反馈：SL 群元素显示矩阵字样 / Q64 子群显示 `<identity> of ...` 且无法选中 / 子群格不认真 / 结构「未知」）：根因 = **前后端元素 id 不一致**（前端 createGroupFromImport 元素 id=g{k} 表行序，后端 _build_group_from_import id=label=GAP 原始字符串）→ 前端 apiElementToGroupElement 按 id 匹配全失配 → 幻影元素（矩阵构造串/`<identity> of ...` 字样、选中/陪集失败）；结构未知 = detectStructureType order>60 一律 'unknown'。修复：①后端 _build_group_from_import 元素 id 统一 g{k}（value=[k]）+ python 镜像 assignWordLabels（_import_word_labels/_word_label，恒等元 e、词 a/a^2/a^{10}、未达 g_{k} 兜底）+ generators 改用 Generator dataclass；②新增 _imported_exprs 注册表（structure/gapExpr→原始 GAP 表达式），_gap_expr_for 优先重跑原始 expr（结构名重建可能枚举序漂移）；③gap_service _GV_COMPUTE_ALL/_GV_COMPUTE_SERIES 重写——不再替换置换像：GV_els=Elements(原始群)、GV_iso=IsomorphismPermGroup、keys=ListPerm(Image(iso,x)) 按原始枚举序对齐导入表行序（载荷 8→9 项含 id_pos，series 4 项含 id_pos，terms 在原始群上计算），compute_all_expr/compute_series_expr 新入口；④main.py _gap_elements 位置映射（先 id 精确匹配 S_n/A_n 排列串，失配→_gap_positional_map 三条件校验：len(keys)==order、id_pos==1、抽样子群封闭性→返回后端元素，失败落纯 python，**不再生成幻影**），subgroups/normal/conjugacy-classes/center/lattice/series 六端点接线；⑤前端 importGroup.ts 生成元瘦身 findTwoGeneratorPair（gens>2 时按元素阶贪心找 (最大阶 a, b) 两生成对，闭包=全群，Q64 6 生成元→(a 阶32, b) 标准对，assignWordLabels 自然单词标签；C2^k 无 2 生成对保留原 gens）；⑥detectStructureFromSymbol：order>60 按符号解析（':'/⋊→semidirect、'×'/' x '→direct、否则 indecomposable，S5 正确判不可分解）；后端 test_main +3（PSL(2,7) 179 子群元素 id g{k} 断言 / SL(2,3) group-info ids==g0..g23+单词标签 / Q64 37 子群全 g{k} 去重 64），36 passed；前端 41 文件 1239 tests 全绿 + lint + build；curl 实测 SL(2,3) 24 元素单词标签无矩阵串、Q64 子群 37 个 0 幻影、PSL(3,2) 179 子群/6 共轭类全 g{k} |
| 2026-08-17 | 修复大群子群格塌成一条竖线（用户反馈 Q64 子群格一条竖线，实为**所有走后端 lattice 的群**通病）：根因 = 后端两条 lattice 通路（main.py `_gap_lattice` 与 algebra.py `compute_subgroup_lattice`）level 分配按**节点索引** rank（`sorted` 稳定排序给同阶子群各自独立 rank）→ 每节点独占一层 → 前端布局每层 count===1 全部 x 居中 → 竖线（前端本地路径 subgroups.ts:136 按阶值分层本就正确，≤60 小群不受影响）；修复：两处均改为按**阶值**分层（orders 去重排序 → order_rank[order]=rank，同阶子群共享 level），Q64 37 子群 7 层 1/1/17/9/5/3/1 铺开、S6 1455 子群 21 层（L19:75 等）GAP 通路实测铺开；后端 test_main +1（test_lattice_same_order_shares_level：A5 10 个 2 阶子群同层断言）37 passed；前端无改动 |
| 2026-08-17 | 修复 GAP 计算弹终端框（用户反馈导入群时弹出终端框）：根因 = `_run_gap_raw` 的 `creationflags = CREATE_NEW_CONSOLE | CREATE_NO_WINDOW`（2026-08-17 修复 detached 挂死时加的）——MSDN 规定 **CREATE_NO_WINDOW 与 CREATE_NEW_CONSOLE 连用时被忽略** → 每次 GAP 调用都新建**可见**控制台窗口（标题 = bash.exe 路径）；修复：改为仅 `CREATE_NO_WINDOW`（不继承不新建控制台），EnumWindows 实测三场景——前台终端 2.5s 成功无弹窗、detached（pythonw 模拟服务/任务计划/npm run backend）2.5s 成功无弹窗、旧组合弹窗复现；pytest 导入/健康/系列 9 个真跑通过；curl 实测 PSL(3,2) 179 子群 source=gap 正常；部署到 Linux 本就无此问题（POSIX 无控制台概念） |
| 2026-08-17 | 3D 凯莱图 spherical 形状重构（节点与边全部嵌入球面、边不交叉，替代旧式「极地飞线」）：新增 `src/core/algebra/sphereGraph.ts`（约 900 行，纯函数全确定性）+ `embedSphereGraph(order, edges, {seed=12345, chordCap=max(6,floor(order/12)), maxLayers=3})` 三级降级管线——**档1 单球面路由** `routeLayer`：fibonacciSphere+mulberry32 确定性初始方向、greatArcSamples（对径弧 null 直接残留）、greatArcsCross 精确交叉检（共面保守/端点排除/对径跨弧补区间）、交叉消除 6 轮迭代（bendControl 二次弯 + bezierSamplesCubic 三次平顶弯 + bent-other 弯对方弧分担 + 失败 eject 残留弧，candidates ±0.7/±1.0/±1.35/±0.45/±0.25，bendControl 满 2 弯 ejects，quick 模式 >300 弧跳过全对扫描）、节点避让 minAngularToNodes<clearance 弯弧/eject；**单环图 cycleSeeding**（边集=单环时按环序均匀布大圆 2πk/n，天然 0 交叉全确定——C6 曾因随机初始+爬山从 1 交叉无梯度到 0 而失败，弧上弯起无用：正交交叉双弧法线互在对方平面内且对方弧长覆盖穿越扫掠区）；**档2 内部弦** `buildChords`（剩余边改为球内直线 chord，segmentsNear 采样 9×9 最近距互检 + 节点距离检，冲突 ejects 缓存 → 恶意密度兜底下沉档3）；**档3 同心球分层** `routeLayers`（残边按从长到短分 0.8R/0.64R 层，最多 3 层，fibonacci 轴网格 + 环收缩调距防遮挡，chordCap=0 强制全弦）；结果含 `layers`、`chords` 端点对 + 每根弦 `aIdx/bIdx` 供 StemRod 杆标识（弦端点非节点时画白色杆连接节点↔弦端点）。layout3D.ts `case 'spherical'`（与 default 合并）改用 `sphericalNodeDirections`×`sphereRadiusFor(order)`（radius=5 常量与 fibonacciSphere 兜底未动）。Cayley3DView.tsx 重写 spherical 渲染：`sphereEmbedding` memo（cayleyEdges→去自环/大群 visibleElementIds 过滤→embedSphereGraph，**上移到 cayleyEdges 之后修复 eslint TDZ『Cannot access variable before it is declared』**）、positions 改 directions×R、新增 SphereArcMesh（BufferGeometry 圆柱 polyline + 无箭头若弧两端有弦则箭头同色描边）/ChordEdge（玉色虚线 + 两端白色小杆）/StemRod（线段 + 白杆）/LayerShells（球壳 mesh，0.8R/0.64R 同节点色，非平面徽标态用）；非 planar（chords 或 layers>1）时顶部徽标「非平面：内部弦边 ×N」/「非平面：N 层同心球面」（i18n `cayley3d.embedChordMode/embedLayerMode` zh+en，t 支持 {n} 插值）；杆元素跳过 NodeSphere 绘制。测试 `src/__tests__/sphereGraph.test.ts` 19 用例（mulberry32 确定性/fibonacci 北极/单位化/最小角距/greatArcSamples 对径 9 点 toBeCloseTo(0,9)/greatArcsCross 四例（共面重叠/共享端点非交叉/对径跨弧）/C6 planar 0 残留/不带 seed 500 次全 planar/K5 chord 5 根/K6 chordCap=0 3 层/seed 确定性/stems 端点含非节点/radius 公式/compute3DPositions spherical 回归）；fixtures 可调 SEED=给 Climb 爬山（n≤12 平滑目标：近交叉罚分 (1.6-d)²+硬交叉 c×1000，阈值须>1.2；n≤16 激进 moves n×40/restarts 4/±0.4）仍保留供非环图细调；42 文件 1258 tests 全绿 + lint + build；浏览器实测 C6 球形凯莱图 0 弦 0 残留、K5 5 根玉色弦、S3 三角 planar |
| 2026-08-18 | 3D 球面嵌入共面误判修复（承接上条）：greatArcsCross 共面分支改按弧相位 unwrap 区间交叠判定（intervalOf 沿 slerpUnit 采样相位差累加）——旧实现把回转弧（S3 的 240 度跨 360 度参考线）当长弧误判交叉，致主循环弯弧失败整弧淘汰为弦（S3 丢 1 弧）；修复后 S3 6/6、A4 18/18、D4 12/12、D5 15/15 等平面群全弧保留 0 弦 0 分层 0 近贴，真 Q16=SmallGroup(16,8) 32 边 3 弦+2 层属非平面正常余量；新增 diagReal.test.ts 双用例：cycleSeeding 赤道断言 + 11 群真实边集边守恒/平面群零弦零交叉回归；43 文件 1263 tests 全绿 + lint + build；浏览器实测 A4/S3 spherical 无「非平面」徽标、S4 显示「非平面：2 层同心球面」符合预期；版本 1.13.1 |
| 2026-08-18 | 3D spherical 嵌入再修复（用户四点反馈：不许自动推送 / S4 三生成元截角八面体消失 / A5 平面图嵌入不上 / Q8 弦相交 / GL(2,3) 挤角落）：①**A₅ 生成元换为 Group Explorer 风格 (12345),(12)(34)**（AlternatingGroup.ts，旧 (12)(34),(135) 凯莱图非截角二十面体骨架，DFS 同构 601 calls 失败）+ trySkeletonEmbed 新增 order===60&&edges===90 → truncatedIcosahedron(5) → A₅ **60/60 全弧 0 弦 0 交叉**（kept=90 minAng=0.4063，与 A5 默认 3D 布局 truncatedIcosahedron 特殊动作 ['2,3,4,5,1','2,1,4,3,5'] 完全一致）；②**polyhedra.ts computeSkeletonEdges 修 bug**（距离 bin 升序首个 count==3n/2 为棱长，旧 maxCount bin 对截角二十面体误取 120 条/截角八面体 48 条非真棱，用户探针同构测试失败根因）；③**S₄ 三换位「截角八面体」3D 形状恢复**（truncatedOctahedron3：types.ts Layout3D 联合 + availableShapes3D S4 列表 + layout3D.ts case（v1.13.1 坐标 + placeS4Elements）+ cayleyActions.ts getSpecialCayleyActions/addAllCayleyActionsHelper + ViewPanel.tsx 特殊动作 ['2,1,3,4','1,3,2,4','1,2,4,3']；注：v1.14.0 删除时误判三换位凯莱图非平面，实测 ≅ 截角八面体 DFS 同构 TRUE）；④**Q₈ 凯莱图（i,j 生成元）= K₄,₄ 非平面**（{±1,±k}↔{±i,±j} 完全二分；试正四角反棱柱骨架 DFS 同构失败后删分支；cr(K₄,₄)=4 → 12 弧 + 4 弦恰为最优画法，弦间距 ≥0.08 无视觉相交）；⑤**buildChords 收紧**（CHORD_MIN_SEP 0.02→0.08 ≈ 2.3×管半径，弦-弦永不相交；attempts 10→24；perturb ±(0.06+rng·0.14)）；⑥**climbNodeSpread 新节点分离爬山**（objective=Σdd² for sep<minAng，moves=n·24 restarts=3，单点 ±0.3rad 确定性 rng；0-交叉分支与 FD-nonzero 分支 repairClearance 后收尾展开，目标 2·0.42/R0）——消除 GL(2,3)/A₅ 旧「顶点重合」（挤角落根因）；⑦**repairClearance 兜底加守卫**（强制旋转仅当交叉数不增否则 break 整轮，旧版把归零交叉破坏成 280）；GL(2,3)（48 阶 3-正则 72 边）33 弧+22 弦+3 层、minNodeAng 0.1052（旧 0.0000 重合）、弦距 ≥0.08；D6 六棱柱 SKEL 12/18 miss 后 FD 0 交叉 planar（kept=18 minAng=0.9577）；Q16 18 弧+8 弦+3 层 minAng 0.1752；diagReal.test.ts 增 A5/GL(2,3) 用例 + 每 case minAng（节点-节点）断言；43 文件 1263 tests 全绿 + lint + build；版本 1.14.1 |
| 2026-08-18 | 3D spherical 嵌入 GL(2,3) 偏心修复（用户反馈「节点都挤在不到半个球面」）：**climbNodeSpread 增重心惩罚 + 推离重心移动**——①objective 加 centroid 项 `|Σdir/n|²·n·4`（节点整体偏心时重心偏离球心，均匀分布 ≈1/√n 量级），②moves 循环一半步数选「最靠内节点」（dir·centroid̂ 最大）绕 axis=cross(dir,centroid) 旋转 0.6-1.0rad 远离重心（比单节点随机旋转对重心收敛快）；GL(2,3) 由旧「挤半球」→ **cent=0.0001 节点包围球心均匀分布**，kept=42 弧+19 弦+3 层、minAng=0.1017 无重合（均匀分布代价：交叉统计 17 vs 旧 6，非平面图不可避免，弦距仍 ≥0.08）；Q16 14 弧+9 弦+3 层 minAng 0.1451；diagReal.test.ts 每 case 增 centroid 断言 `cent < 0.25` + 日志行；43 文件 1263 tests 全绿 + lint + build；版本 1.14.1 |
| 2026-08-19 | spherical 球面嵌入全砍 + cone 圆锥布局兜底：**spherical 移除**——删除 sphereGraph/spherePlanar/sphereDecomp/planarLayers 4 源文件 + fibonacci2DLayout + 5 测试文件（sphereGraph/sphereDecomp/spherePlanar/planarLayers/diagReal）+ i18n 3 键，Layout3D/CayleyShape2D 联合类型移除 'spherical'，Cayley3DView 球弧/弦渲染分支删除；**cone 兜底新增**——3D 圆锥（顶点恒等元 + 沿母线按元素阶分圈，第 k 圈摆 k 阶元无 k 阶元素则跳圈，圈半径/高度 = 5·k/maxK 锥面 45°，圈内均匀角分布）与 2D 同心环俯视（coneLayout2D：中心恒等元 + 按阶分环 min(w,h)·0.42·k/maxK）；形状分发（getAvailableShapes3D base=['cone']、getDefaultLayout3D 商群+兜底 'cone'、getAvailableShapesForView 各列表尾部 'cone'）；42 文件 1250 tests 全绿 + lint + build；浏览器实测 S3 3D 下拉 cone/circular/hexagon、D6 2D 下拉 圆形/双环/圆锥；版本 1.15.0 |
| 2026-08-20 | 代码审查修复轮（用户发起全面审查，修 bug/体验/数学问题）：**H1 白屏**——`semidirectProductStorage.ts` `loadSemidirectProductSpecsFromStorage` 补 `Array.isArray` 校验（6 个存储 loader 中唯一漏检，localStorage 脏数据使 `GroupSemidirectProductContext` useState 初始化器 for-of 抛 TypeError 整树卸载）；**H2 布局竞态**——`GroupCoreContext` 新增 `currentGroupRef`（ref 在各 set 调用点同步赋值，暴露于 value），`GroupContext.runForceLayout` 的 `.then/.catch` 守卫改比 `core.currentGroupRef.current`（原闭包捕获 `core.currentGroup` 恒等于 groupAtStart，守卫永不触发 → 切群后旧群 forceLayout 坐标写入新群，元素 id 跨实例可复用致视觉错乱）；**SymmetryView 分类顺序**——`getSymmetryType` 的 `\times`/`^{` 检查前置（原 C/D `startsWith` 先命中使 `C_{4}\times C_{2}`、`C_{2}^{3}` 等误渲染为 cyclic/dihedral 图形）、`C_{2}^{2}`/`C_{2}\times C_{2}` 特判 rectangle（V₄≅矩形对成群）；**dist=0 除零守卫×3**（GroupCanvas.renderEdgePath / FloatingViewWindow / SylowView 主边，拖拽节点重合不再出 NaN 路径消散）；**M3** 切群重置 `symmetryShowAction`（原残留 true 劫持新群元素点击为对称演示）；**M4** `compositionEnum`/`seriesFlags` 大群分支校验 `gapSeries.type === 'composition'`（防导列瞬态冒充合成列）；**M6** `showCosetFromElements` 改 `[...elementIds].sort()`（不再原地改调用方数组）；**L4** `homomorphismStorage` 整批容错（非数组返回 [] + 每记录独立 try/catch + 缺 mapping 字段跳过，不再一坏条全丢）；**M7** `GroupBackendContext` 守卫去冗余 + 补 `.catch` 兜底 loading 复位；**PresentationTableView 裁剪**——cellSize/cellFont 按 viewBox 自适应（order 25-36 不再裁右下角）；**L5** cayleyActions 空群防御 `elements[0]?.id`；新增测试 `symmetryView.test.ts`（6：分类含直积/幂符号矩形特判）+ `storage.test.ts`（7：两个 storage 脏数据容错 round-trip）；44 文件 1264 tests 全绿（覆盖率 88.91/78.22/92.32/91.90）+ lint + build |
| 2026-08-20 | 数学核心审计修复轮（用户要求「把数学核心审计完成」）：**polyhedra 坐标×3**——`truncatedDodecahedron` 第 2/3 组坐标修正（`[1/φ,1,2φ]`→`[1/φ,φ,2φ]`、`[1/φ,φ,1+2φ]`→`[φ,2,φ+1]`，60 顶点 120 伪棱→真 90 棱截角十二面体）、`rhombicuboctahedron` 坐标从错误的 `(±1,±(√2−1),±(√2+1))` 偶排列改为正确的 `(±1,±1,±(1+√2))` 全排列（24 点 4-正则 48 棱）、`truncatedCube` 坐标修正为 `(±(√2−1),±1,±1)` 全排列（silver ratio，3-正则 36 棱；原误与 rhombicuboctahedron 同集 4-正则 48 棱）；**computeSkeletonEdges** 去硬编码 3n/2——改升序扫描 `count ≥ 3·n/2 且 count 为 n/2 整数倍`（支持 4-正则 rhombicuboctahedron 48 棱，伪 bin 如 24/120 对自动跳过）；**automorphisms 静默截断→伪 Aut 群**——删除 `MAX_RESULTS=1000`（`Aut(C₃³)=11232`、`Aut(C₇²)=2016` 等组合数 ≤30000 的群被截断后 `createAutomorphismGroup` 产生阶错误/乘法表损坏的伪群并污染半直积分解；C₇² 全量 2016 = |GL(2,7)| = 48·42 回归断言）；**series 交换因子标签**——`computeFactor` 改用有限交换群分类定理精确不变量链匹配（阶分布 + `abelianFactorChains` 枚举 + `distributionsEqual`，`subgroups.ts` 三函数导出；秩≥3 因子不再错标如 C₂⁴→C₂×C₈，dist 按陪集归一化 ÷|small|）；**presentations 直积交换子**——`presentationOfDirectProduct` 从「仅因子 0×其余」改为全部因子对 (fi<fj) 生成元两两加交换子（≥3 因子直积如 C₂×C₃×C₅ 不再缺 [b,c] 丢展示路径）；**actions 空表守卫**——`verifyAllRelations` 空置换表直接 `{ok:false}`（防 Sylow 无素因子时 perms.get 崩溃）；polyhedra/automorphisms/series/presentations 4 文件 +8 回归测试；44 文件 1272 tests 全绿 + lint + build |
| 2026-08-23 | **P0 引擎化预处理（FGVE 前置，行为零变化的五阶段重构 A-E，各阶段独立 commit + test/lint/build 三绿）**：**A 类型拆分+门面（c078cab）**——types.ts(780行) 拆 types/{group,view,homomorphism,actions}.ts + algebra/groupProps.ts（性质检测/形状选择函数），原 types.ts 变 barrel 零破坏；新建 `src/core/index.ts` 门面全量公共导出（automorphisms 内部版 isAutomorphismGroup 排除只留 groupProps 公共版）+ ESLint no-restricted-imports 边界规则（禁 UI 层深导入 types/{group,...} 与 core/**/_internal）+ coreBoundary.test.ts（105：core 纯净性扫描零 react/localStorage/DOM + 门面 ~100 符号存在性）。**B 大文件拆分（5ee2c1a/8146f83/e9f1455/594ca2b/39125f7）**——原文件变纯 barrel、函数体逐字迁移、私有函数不进公共面：forceLayout(1552行)→algebra/layouts/{shared,factorLayouts,ringShapeLayouts,specialLayouts}；layout3D(1200行)→纯 dispatcher + layouts3D/ 6 模块；presentations(1011行)→presentations/{wordParser,toddCoxeter,minimizer,presentationOf}；subgroups(990行)→subgroups/ 7 模块；SmallGroups(713行)→groups/SmallGroups/{abelianProducts,wordLabels,tableGroup,registry}。**C 错误模型双轨制（0196e6f）**——`src/core/result.ts`（Result<T,E> 判别联合 + EngineError{kind:'parse'\|'guard'\|'unsupported',reason?} + ok/err）+ `src/core/guards.ts` 收敛 11 守卫常量（各模块 re-export 保公共面）；14 处裸 throw new Error 全部 EngineError 类型化（消息保留 toThrow 兼容）；notationParser error 收窄 NotationErrorCode 字面量 union；api.ts ApiError(status,detail) + fetchHealth 补 res.ok；resultGuards.test.ts 10。**D 输入加固（b703791）**——引入 zod@4.4.3 + `src/utils/persistence.ts`（parseStoredJson/loadStoredJson/loadStoredArray 逐条容错/版本化信封 {__gvVersion,data}），11 storage 文件 14 处裸 JSON.parse 全部迁移，Workspace.tsx 会话恢复接入信封（legacy 裸数据兼容）；persistenceFuzz.test.ts 11（坏 JSON/schema 不符/未来版本拒绝/HOSTILE_STRINGS 25 条 fuzz 四解析器）；后端 schemas.py pydantic Field(max_length)+Literal 加固（backend pytest 37 passed）。**E 总验收**——浏览器冒烟通过（视图切换 set/cayley/sublattice/sylow/table、S₃ 创建、重载会话恢复、groupviz-session 信封 __gvVersion:1、console 零错误零警告）；47 文件 1398 tests 全绿 + lint + build；版本 1.15.1 |
| 2026-08-23 | **E1 正式关闭（文档决策）**：gappy 后端集成方案不采用——2026-08-16 已换道直连本机 GAP 4.16 并以 v1.13.0 交付核心能力（六端点 >120 阶自动切 GAP + /series + /import-group、进程缓存、15s 超时守卫、S₆ 实测），IdGroup 已砍除；剩余后端增强项（缓存/超时强化、特征标表端点、大群前端全链路验收）并入中期 FGVE（ROADMAP §2.4/§2.5）；近期功能夯实期收官，ROADMAP/AGENTS/CONTRIBUTING/GROUPS 同步更新（§2.5 更名「GAP 后端完善」、GL(2,5) 提示改记号导入通路） |
| 2026-08-23 | **测试体系建设（v1.16.0）**：vitest `test.projects` 双项目拆分——**node**（47 文件 1398 tests，*.test.ts）+ **dom**（happy-dom，6 文件 44 tests：组件测试 Tex 12/AccordionSection 8/TabBar 8 + 集成测试 BasicGroupPanel 5/Workspace 7/SvgSnapshot 4 SVG 结构 inline 快照）；`src/test/setup.ts`（jest-dom matchers + ResizeObserver/matchMedia stub）；新增脚本 `test:dom`/`typecheck`（tsc -b）/`test:e2e`；**Playwright E2E**（e2e/ 6 spec 13 tests：欢迎页入口、三栏布局+pageerror==[]+9 视图卡、非 WebGL 视图遍历、建群 D₄+直积模式进入退出、session reload 恢复+主题持久化、3 截图视觉回归基线 set-C6/table-S3/sublattice-S3 maxDiffPixelRatio 0.02；chromium workers:1 locale zh-CN webServer strictPort）；新增 devDeps happy-dom ^20.11.6 + @playwright/test ^1.62.1；.gitignore 补 test-results/playwright-report；53 文件 1442 tests 全绿 + lint + typecheck + build + E2E 13 passed |
| 2026-08-24 | **FGVE 开发前工程准备收官（文档决策 + 终验）**：前置准备三件套全部落地——①E1 正式关闭（gappy 不采用，GAP 直连 v1.13.0 已交付）、②P0 引擎化预处理 v1.15.1（类型拆分+门面/大文件拆分/错误模型双轨/输入加固，行为零变化五阶段 A-E）、③测试体系建设 v1.16.0（node+dom 双项目 53 文件 1442 tests + Playwright E2E 13）；终验四绿实测——`npm run lint` 零错误、`npm run test` 53 文件 1442 tests 全绿（9.2s）、`npm run build`（tsc -b + vite 477ms）通过、`npm run test:e2e` 13 passed（31.3s）；专项扫描——package.json = welcome.version（zh/en）= AGENTS 文档版本 = 1.16.0 一致，测试计数 AGENTS/README/README_zh-CN/CHANGELOG 四文档均为 53 文件 1442；工作区干净、main 与 origin 同步（faf4964）；纯文档收官不升版（沿 docs: 先例）；下一步进入中期 FGVE 引擎化（ROADMAP §2：`src/engine/` 独立引擎层、GroupDescriptor/ViewConfig 稳定协议、群 JSON round-trip、特征标表视图、GAP 后端完善） |
| 2026-08-25 | **CI 修复 + S3 对称动画测试同步 + favicon 迁移 + README 宣传图（v1.16.1）**：`automorphisms.test.ts` C₇²（Aut=|GL(2,7)|=2016）在 v8 覆盖率插桩下耗时 5231ms 超 vitest 默认 5000ms 超时——CI `test:coverage`（Coverage report 步骤）失败根因，改 `it(name, fn, 30000)` 显式超时；同步修复 HEAD `9c50bd0` 引入 S3 对称动画后的 2 个过期断言——`polyhedra.test.ts`「unsupported」用例 S3→S5（S3 现在返回旋转对象）、`symmetryView.test.ts` 改断言 S3≅D3→`dihedral`（Q8/GL(2,3) 保持 unsupported）；首页 favicon SVG→PNG 三件套迁移（删除 public/favicon.svg + icons.svg，新增 public/favicon.png + favicon-32.png + favicon-64.png）；README 宣传文档配套（docs/images/ 图标裁剪/参考图 + scripts/capture-readme-screenshots.mjs）；版本 1.16.0→1.16.1（package.json、welcome.version zh/en ×2、AGENTS.md 文档版本/最后更新，测试计数 1442→1443 同步）；53 文件 1443 tests 全绿 + lint + build + coverage 阈值 OK（89.16% stmts / 78.57% branches / 92.31% funcs / 91.96% lines） |
| 2026-08-25 | **FGVE 包化范围定案（规划决策，纯文档不升版）**：中期目标升级为「UI 与算法解耦 → 产出可消费 npm 包」（原「npm 包推迟到 GVL」取消）——`GroupDescriptor v1` 序列化协议（src/core/descriptor.ts：元素/乘法表行序隐式索引/性质缓存/构造参数/source 溯源，zod 校验 + 全群族 round-trip 幂等测试）+ 视图组件 props 化四批（set/cayley/cycle/table → 3d/sublattice/cosetstrip → homomorphism/action/sylow/symmetry → tree/prestable，从 `useGroup()` 全局 Provider 改受控 props 注入，GroupCanvas 拆受控组件 + FromContext adapter 壳，每批行为零变化重构独立 commit 测试全绿）+ `@groupviz/core`/`@groupviz/react` 双包（单仓库内子目录 + vite library mode 多入口，不转 monorepo；react/three/@react-three/fiber/drei/katex 作 peerDeps；算法层只输出 TeX 字符串零依赖，KaTeX 渲染归宿主/@groupviz/react；createBackendAdapter({baseUrl,fetchImpl}) 后端可配）+ `examples/host-minimal/` 最小宿主验收（JSON 载群→渲染→切形状→导出 SVG，CI 冒烟）；ROADMAP 同步（§0 核心决策、§2.7 非目标改写为仅排除 monorepo 与包内嵌 GAP、新增 §2.8 双包解耦与打包、§3.8 GVL 消费双包、§4 中期验收标准、§5 决策行）+ AGENTS 文档导航行 |
| 2026-08-28 | **FGVE 阶段 2 批次一（v1.17.0，set/cayley 视图 props 化 + 受控 ViewWindow）**：`GroupDescriptor v1` 序列化协议落地（src/core/descriptor.ts：zod schema + serializeDescriptor/deserializeDescriptor/descriptorToSymbol）+ **ViewConfig JSON 化**（src/core/types/viewConfig.ts：ViewWindowConfig/SetViewParams/CayleyActionParam/CayleyViewParams/ViewWindowGeometry/ViewWindowPersistData 全 zod schema，actions 上限 240，core 零 react/localStorage）+ CAYLEY_SHAPES_2D 常量 + CayleyShape2D 类型入 types/view.ts；**受控 CayleyView**（CayleyView.tsx 纯 props：布局 null 回退 cayleyCircleLayout、normalizeCayleyActions 作用边、右/左乘、每实例唯一 cv{n} marker 前缀防多窗口冲突、自环、半直积 rewiring φ-不动点青色高亮、大群 culling、拖拽会话态形状/群变重置）；**SetView 受控化**（props 化 + SetViewFromContext adapter 壳主应用零变化）；**受控 ViewWindow**（FloatingViewWindow.tsx 新组件，旧 FloatingViewWindow 保留供主应用多视图）：通用参数面板（View Config 4 复选 + Set/Cayley 视图参数 + Reset）+ 受控/非受控双模式 + gv-vw- 前缀 versioned 持久化（按视图 schema 校验回退）+ 窗口拖/8 方向缩放 + Ctrl/Cmd 滚轮缩放（rAF 节流/双击复位）+ 参数面板外置右侧溢出翻左 + VIEWWINDOW_RESET_EVENT/resetAllViewWindows() + `/?test=1` TestPage 参数矩阵（11 set + 5 cayley 窗口）；测试：node +13（descriptor 5 / viewConfig 8）、dom +25（CayleyView 11 / CayleyWindowParams 8 / ViewWindowParams 6）→ 58 文件 1481 tests 全绿（node 49/1412 + dom 9/69）+ lint + typecheck + build；Playwright 浏览器 DOM 实测通过（C₄ 4 节点/4 曲边/cv 前缀 marker、D₄ dualRing 8/12 边、S₃ left 全无向、C₁₂ spiral、11 set 窗口节点）；版本 1.16.1→1.17.0 |
| 2026-08-28 | **FGVE 阶段 2 批次一续（cycle/table 视图 props 化收尾 + 乘法表窗口最小尺寸）**：**CycleView/TableView props 化**——CycleViewProps（showMaximalCycles/nodeRadius/showLabels/showCycleLabels/locked/getNodePosition/onNodePositionChange 桥接主画布共享位置、每实例 cyv{n} filter 前缀、本地拖拽会话态）、TableViewProps（subsets/coset 数据/forceShowLargeGroup/onForceShowLargeGroup/strategy/cellSize/onStrategyChange；大群告警本地状态+可选上下文回传；strategy 受控同步用渲染期 prevStrategyProp 比对 setState 规避 set-state-in-effect；cellSize 可传参）+ 同模式 adapter 壳 CycleViewFromContext/TableViewFromContext（GroupCanvas 与旧 renderViewContent 改用它俩，主应用零变化）；**viewConfig.ts** 新增 CycleViewParams/cycleViewParamsSchema、TableViewParams/tableViewParamsSchema/TableStrategy；**受控 ViewWindow** renderContent 支持 cycle/table、ViewParams 导出四视图联合、持久化 schema 按视图映射、参数面板加 Cycle View/Table View 两组控件；**乘法表窗口最小尺寸**——TableView 经 onLayoutSize 上报实际渲染内容尺寸（导出常量 TABLE_PAD_W=44/TABLE_PAD_H=74），ViewWindow 计算 tableMinSize（上报优先、小群 k=n 首帧直推）+ 渲染期 setGeometry 撑大 + clampResize 可选 min（resize 不可缩到表以下）+ resetAll 同受约束；旧 FloatingViewWindow（主应用多视图）同步加确定性最小尺寸（view==='table' 且 order≤100，k=order≤16?order:16，cellSize 50）；测试：dom +8（CycleTableWindowParams 6 + 最小尺寸 2）→ 59 文件 1489 tests 全绿（node 49/1412 + dom 10/77）+ lint + typecheck；版本 v1.17.0 继续 |
| 2026-08-30 | **循环图 cycleGraphLayout 精确复刻 Group Explorer（v1.17.0 续）**：单位元 e 固定在原点永不重放；极大循环=花瓣圆弧（basePetalPoint：第 k 非单位元 gᵏ 落圆心 (0,R) 半径 R 弧上 (-R·cosθ, R(1+sinθ))，θ=2π(k/n−0.25)）；循环按共享非单位元聚成 part（uniteParts 并查集，合并时被并入 part 的每个循环经 bestPowerRelativeTo 重新轮换 reorbitCycle 使共享元素同弧索引对齐）；各 part 按循环长度之和比例分配角度弧（单 part 退化半圆弧；最大 part>total/2 时各弧长封顶 total/2），最大 part（循环数最多）弧中心旋转正下方（diff=−π/2−maxPartCenter）；part 内多循环 gravity=ringNum/part.length 拉向弧中心（mutateArc，theta2 线性插值）；共享元素只由首个循环放置后续引用（蝴蝶例 Z₂×Z₄、SL(2,3) 7 循环共点 −I 太阳放射）；不在 ≥3 阶循环的 2 阶元素=叶柄线段；**风车式改进（v1.17.0 续二）**：S3 型「所有极大循环只共享 e」群（S3/Dn/A4/A5/V4/纯直积等）不走最大 part 朝下旋转——那会把 2 阶叶柄全压下半圆并与花瓣交叉；改为每片花瓣/叶柄绕 e 均匀 360° 等分扇区（g=0）；并修复共享判定含 e 恒相交（partition 恒并成一个半圆弧）的复刻缺陷（共享判定排除 e，与 GE 循环不含 e 一致）；C₅×S₃ 等复杂共享为 GE 平面固有少量边交叉（无回退）；修复 3 旧失败（C4×C2 节点重叠、SL(2,3) 48 交叉→≤8、C6×C2 自交）+ 新回归测试 **cycleGraphCrossings.test.ts**（38 群 self=0/overlap=0 硬不变式 + S3/D3≤6、C4×C4≤12 软基线）；测试：node +13（cycleLayouts +8 + 新 cycleGraphCrossings 5）/ dom +1（新 CycleView.integration：S3 花瓣 ≥1 多边形 + ≥3 叶柄线段）→ **61 文件 1509 tests 全绿（node 50/1426 + dom 11/83）** + lint + typecheck；docs 同步（VIEWS/TESTING/AGENTS）；版本 v1.17.0 继续 |
| 2026-08-31 | **循环图窗口（v1.17.0 续三）**：循环图浮动窗口类似凯莱图——默认不显示元素标签（ViewWindow cycle render `showLabels={false}`，mirror cayley 块；循环参数面板移除 Show labels 复选框）；窗口点击元素→高亮显示它所在的极大循环（CycleView maximal 分支 `isHighlighted`：CYCLE_COLORS 彩色填充 fillOpacity 0.18 + strokeWidth 4 + `<g>` 居中 ⟨g⟩≅Z_n 标注，仅高亮循环显示标题）；测试：dom +1（CycleTableWindowParams 8→9：无 foreignObject 标签断言 + 点击高亮所在循环）→ **61 文件 1510 tests 全绿（node 50/1426 + dom 11/84）** + lint + typecheck + build；docs 同步（VIEWS/TESTING/AGENTS）；版本 v1.17.0 继续 |
| 2026-08-31 | **FGVE 阶段 2 批次二（v1.18.0，3D 凯莱图 props 化 + 受控 ViewWindow）**：**Cayley3DView 拆分**——原文件内拆 `Cayley3DScene` 受控内核（含 R3F Canvas 全 props：group/selectedElements/onSelectElement/actions/multiplyType/layout3D/nodeScale/autoRotate/showLabels/locked/subsetHighlights；hover 本地化不再依赖全局 HoverContext；导出桥 `.canvas-viewport` closest 检查保留 → 窗口实例天然不注册）+ `Cayley3DView` context 组装壳导出签名不变（GroupCanvas/旧浮动窗零变化）；**LAYOUTS_3D 常量化**（types/view.ts 19 值 as const，Layout3D 改派生类型不变，zod `z.enum(LAYOUTS_3D)`）；**Cayley3DViewParams**（viewConfig.ts：layout3D 缺省 getDefaultLayout3D(group)/multiplyType/actions 复用 normalizeCayleyActions/nodeScale 0.5-2（球半径 0.42/0.55×scale）/autoRotate prop 优先 ▶ 本地态兜底/showLabels；cayley3DViewParamsSchema + 持久化 schema 链 `view==='3d'` 分支）；**ViewWindow 3d 分支**——renderContent 渲染 Cayley3DScene（不传 canvasTransform、不接窗口 hover HUD，3D 自带 Html 标签承担 hover 反馈）+ **三处 3D guard**（wheel handler Ctrl+滚轮透传 3D 相机 radius 缩放不产生窗口 ct、onCtMDown return、zoom slider/figure-hint 偏移 3D 隐藏）+ **参数面板 Cayley 3D View 段**（Layout select=getAvailableShapes3D(group) 可用性回退 getDefaultLayout3D、Multiply a·c/c·a、Node size 滑杆 ×1.0、Auto rotate、Show labels、Edge actions 勾选+All/None——All 传 '3d' 与当前形状使 S₄/A₅ 多面体 canonical 轴序自动生效；locked=禁轨道拖拽/滚轮/右键平移保留点击选中+双击复位）；**TestPage 3D 矩阵 4 窗**（默认 cone/S₃ hexagon/D₄ 受控 autoRotate+左乘/C₄ locked+showLabels:false）+ 覆盖清单追加；**R3F mock 测试先例**（vi.mock @react-three/fiber：Canvas→div、useThree→stub gl/camera/scene、useFrame noop、drei Html→透传 wrapperClass、i18n/theme mock、three 纯数学真实；发现 React19+happy-dom 自定义元素对象 props 序列化 '[object Object]' attribute → 边几何断言走 cylinderGeometry args[2] 边长）；测试：dom +17（Cayley3DView 9/Cayley3DWindowParams 8）→ **63 文件 1527 tests 全绿（node 50/1426 + dom 13/101）** + lint + typecheck + build（545ms）；浏览器实测——4 窗渲染（cone 竖链/S₃ 六边形/菱形锁定/D₄ 受控 ❚❚）、面板切 cone 相机回正、持久化跨刷新（cone+left+autoRotate 恢复）、Ctrl+滚轮窗口 transform 不动、主应用 3D 视图零回归、console 零错误；版本 1.17.0→1.18.0（package.json/package-lock/welcome.version zh/en/AGENTS 文档版本） |
| 2026-08-31 | **FGVE 阶段 2 批次三（v1.19.0，子群格 props 化 + 受控 ViewWindow + 小窗口可读性）**：**四层解法**——① fit：新建 `src/core/algebra/latticeLayout.ts`（`computeLatticeLayout` 紧凑内容包围盒（去掉旧 1000×600 世界下限，它让 6 节点的 S₃ 格在 520px 窗口只剩 7px 文字）+ 每层居中分槽 + `orderLevelsByBarycenter` 交替扫描降交叉 + `countLatticeCrossings` 诊断 + `latticeFitScale` 适配缩放钳 ≤1）；② LOD 三档：按槽位**屏幕宽+高双指标**自动降级 full(≥150×66 完整名片)→compact(≥56×26 胶囊只留一行，顶层群符号走 KaTeX foreignObject)→dots(圆点+类别色编码)，compact/dots 几何按 `1/eff` 反算并以槽位封顶（屏幕恒定且永不重叠），`labelDetail` 可手动定档；③ 共轭轨道合并：`subgroupConjugacyOrbits`（闭包只用生成元——⟨S⟩=G ⇒ 与全元素共轭同果，代价 O(Σ|orbit|·|S|·|H|) 使 S₆ 可用；`size·normalizerOrder=|G|` 轨道-稳定子自检）+ `mergeLatticeByConjugacy`（Hasse 边的像→去重→`transitiveReduce`（新导出，先去重再判删边可达，避免平行边双双被删）→`levelsByOrderRank`（新导出，与原格同一分档规则）；MERGE_MAX_NODES=2000 超限回退提示；全单点轨道走恒等快路径），S₃ 6→4、A₄ 10→5、S₄ 30→更少；④ 信息外置：底部 `lattice-caption` 按 hover ?? 选中给出完整名片（|H| · ≅结构符号 · [G:H] · 轨道数 · |N_G(H)| · ⟨生成元词⟩ · 系列序数），结构符号=新函数 `subgroupStructureSymbol`（母群上就地算子集阶分布+交换性，复用抽出的 `identifyByDistribution`/`candidateTests`+候选分布缓存，O(|H|²) 只在悬停节点跑），series 面板窗口内默认收起；**受控四件套**：`SublatticeScene` 内核（全 props 可选、缺省渲染层 `??` 解析、lattice 未传则 order≤60 自算、>60 提示 `lattice.backendOnly`）+ 尾部同名组装壳（导出签名不变，GroupCanvas/旧浮动窗零改动）+ `SeriesPanel` 纯呈现外提 + ViewWindow 四点接入（ViewParams union/schema 选择器/renderContent/参数面板 Label detail+Merge conjugates+Card size 0.6-1.6+Series panel）；**两个坑**：svg 必须绝对定位在 flex:1/minHeight:0 宿主内（否则 viewBox 宽高比以 min-content 高度撑破定高窗口，Chrome 行为）、内核 ResizeObserver 自测 `getBoundingClientRect` 而不吃宿主 viewBoxSize（ViewWindow 传像素、GroupCanvas 传世界 viewBox，语义冲突；测得 0→fit=1）；**顺带修复受控模式既有缺陷**：ViewWindow 渲染/交互此前直读内部 state 而受控时 state 不更新 → 受控窗口停在初始参数，改 `config = configProp ?? configState`、`viewParams = viewParamsProp ?? viewParamsState` 统一走有效值；**TestPage** 补 5 窗矩阵（S₃ auto/S₄ auto dots/A₄ merge/Q₈ 强制 full 对照/D₄ 受控 compact+0.7）+ `?test=1|2` 补 I18nProvider（此前 t() 回落原始 key）；**e2e 修复**：v1.18.0 起头部两个 `.theme-toggle` 致 strict mode 冲突，选择器改 `button.theme-toggle[title^="Switch"]`；子群格视觉基线重截；测试：新增 node `core/latticeLayout.test.ts`(15)/`core/subgroupOrbits.test.ts`(11) + dom `SublatticeView.component.test.tsx`(12)/`SublatticeWindowParams.component.test.tsx`(7) + viewConfig +5 → **67 文件 1574 tests 全绿（node 52/1454 + dom 15/120）** + lint + typecheck + build（706ms）+ **E2E 13 passed**；浏览器实测——主画布随视口增高自动 compact→full（S₃ 完整名片含 Sylow/正规/Z(G) 角标）、A₄ 合并窗 caption 给出 4·3=12 轨道-稳定子吻合、五窗档位符合预期、console 零错误；版本 1.18.0→1.19.0（package.json/package-lock/welcome.version zh/en/AGENTS 文档版本）
| 2026-09-05 | **FGVE 阶段 2 批次四（v1.20.0，陪集条带 props 化 + 受控 ViewWindow + 自包含子群候选）**：核心问题——主画布 `CosetStripView` 经全局 subsets 状态派生陪集（用户先选子集再生成），受控 ViewWindow 是自包含的（`group` + `viewParams` 驱动），需在 group 上直接派生。**纯函数层** `src/core/algebra/cosetStrip.ts`：`listCosetStripSubgroups`（基于 `findAllSubgroups` + `subgroupConjugacyOrbits` 取代表、index 升序、`order>60` 守卫；A₄ 三项 V₄/C₃×4/C₂×3，S₃ 两项 C₃ 正规/C₂×3）+ `findCosetStripSubgroup`（按元素 id 数组精确恢复，换群后失效→null）+ `cosetDataForSubgroup`（由 H 元素直算 CosetInfo，零 context 依赖）。**内核 `CosetStripScene` props 化**（Sublattice 先例风格——新增 Scene + 尾部同名 context 壳 `CosetStripView()` 保留 GroupCanvas/老浮动窗零回归；Scene 增 `showLabels/showSubgroupCayley/noCosetsText` 可选 prop、`onHover` anchor 世界→视口换算与 CycleView 同约定、`useId` 防多实例 marker/filter id 冲突、节点 `<g data-coset-node>` 与 strip `<g data-coset-strip>` 测试钩子）。**ViewWindow 接入**——schema 选择器加 `view === "cosetstrip" ? cosetStripViewParamsSchema : null`、`updateViewParams` Partial union 并入；视图数据派生用 `csOpts/csSubgroup/csCosetData/csElementMap/csColors/csHighlight` 五个 useMemo（按 csSubgroup 失效回退默认首候选；coset 数据 = `cosetDataForSubgroup` → `computeCosetElementMap/Colors`（复用 `context/cosetActions` 纯函数）→ `computeCosetHighlightSet`，`sel` 选中联动高亮所在陪集）；`renderContent` case cosetstrip 喂 `CosetStripScene`（窗口缺省 `showLabels=false / showSubgroupCayley=false` 省空间给条带区，用户可在面板开关开回主画布观感）；参数面板新增 **Coset Strip View** 段：Subgroup H select（`csOptionLabel` = `${struct} |H|${o.order} · [G:H]${o.index}${orbit}` TeX 结构转 unicode：`C_{2}	imes C_{2}` → `C₂×C₂`、轨道 ×n）+ Coset type gH/Hg 分段 + Show node labels / Subgroup Cayley ring 复选；**持久化**：key 后缀 `|cosetstrip` 互不冲突，`subgroup` 存 H 元素 id 升序数组，坏 schema（cosetType 非 left/right / subgroup 非真子群键）回退默认。**TestPage** 补 4 窗矩阵（S₃ 默认 → 2 条带 / Q₈ cosetType:right → 2 条带 / C₄ `showLabels+showSubgroupCayley` 全开对照 / A₄ 受控），**TestPage2** A₄ 博客加第 6 节「V₄ ⋊ C₃ 的陪集分解」配图窗口（`LOCKED` 配置保留控件，读者可切 H 体验 [G:H] 变化）。测试：新增 node `core/cosetStrip.test.ts`(9：A₄ 选项/index/orbit/structure、C₃ 循环快通道、order>60 守卫、cosetDataForSubgroup 3 条带左陪集 = H) + dom `CosetStripWindowParams.component.test.tsx`(7：默认 H 条带数、C₂ 切换 2→3、cosetType/showLabels 累加受控载荷、hover 就地气泡、版本化信封持久化、坏 schema 回退、持久键含视图名) → **69 文件 1590 tests 全绿** + lint + typecheck + build（`<600ms`）+ **E2E 13 passed**；浏览器实测——A₄/V₄ 三条带红实边（正规子群）+ 蓝/黄虚边、S₃ 切 C₂ 即时 2→3 条带、Q₈ cosetType 切换条带顺序不同、深/浅色窗口切换主题正常、console 零错误；版本 1.19.0→1.20.0（package.json/package-lock/welcome.version zh/en/AGENTS/ROADMAP 文档版本） |
| 2026-09-06 | **FGVE 阶段 2 批次五（v1.21.0，同态视图 props 化 + 受控 ViewWindow）**：**内核拆分**——`HomomorphismView.tsx` 拆 `HomomorphismScene`（纯 props：source/target/mapping/result/name/theoremMode/onTheoremModeChange/theoremAnimation/showLabels/onHover；内部 `useTranslation`；`result` 缺省由 Scene 内部 `verifyHomomorphism` 推导，`result===null`（mapping 空）原样透传不重算避免「✗ invalid」误标；节点 `<g data-homo-source-node>/<g data-homo-target-node>` 测试钩子；`onHover` 上抛源/目标元素 + `svg.getBoundingClientRect` 视口锚点供窗口就地气泡）+ 尾部同名 context 壳 `HomomorphismView()`（签名不变，GroupCanvas/旧浮动窗零回归）；`FirstIsomorphismAnimation.tsx` 拆 `FirstIsomorphismAnimationScene`（纯 props：source/target/mapping/result/onPhaseChange，`setTheoremPhase` 回写改 `onPhaseChange`）+ context 壳。**ViewWindow 接入**——`homomorphism?: Homomorphism` 单 prop 打包双群（view==='homomorphism' 时 group 可为 null）；`HomomorphismViewParams{showLabels?}` + `homomorphismViewParamsSchema`；ViewParams union / updateViewParams Partial union / schema 选择器（`view==='homomorphism'` 分支）/ renderContent homomorphism 分支（喂 HomomorphismScene，缺省 `showLabels=false`，`result = mapping 空 ? null : homomorphism.result ?? verifyHomomorphism`）/ 参数面板 Homomorphism View 段（Show labels 复选）；title/infoText（`|G|→|H|`）/hoverOrder（按元素 id 归属 source/target 群判阶）/persistKey（`|homomorphism` 后缀）/displayKey 派生 homomorphism 优先；视作自管理布局（wheel/onCtMDown/zoom slider 三处 guard 同 3d/symmetry，禁用无效 ct 平移缩放）；窗口缺省隐藏节点标签靠悬停就地气泡。**TestPage** 补 2 窗（C₆→C₂ 自然投影 mod 2：Ker=C₃/Im=C₂ 满射非单射；S₃→S₃ 恒等同构）+ 覆盖清单追加。测试：新增 dom `HomomorphismWindowParams.component.test.tsx`(8：无 group 渲染双群节点/核像标注、标签开关 3→11、悬停气泡、受控 showLabels 回传、持久化、key 冲突、无 homomorphism 占位、S₃→S₃ 恒等同构) + node `s4toS3.test.ts`(1：S₄→S₃ 划分作用满同态构造) → **73 文件 1623 tests 全绿（node 55/1478 + dom 18/145）** + lint + typecheck + build；版本 1.20.0→1.21.0 |
| 2026-09-06 | **同态演示补充（v1.21.0 追加，TestPage test-homo-4to3）**：S₄→S₃ 满同态教科书例（第一同构定理 S₄/V₄ ≅ S₃）——S₄ 作用在三条 2-2 划分 P1={12\|34} P2={13\|24} P3={14\|23} 上诱导划分集合置换：生成元像 φ((12))=(23)、φ((1234))=(13)（自算），extendFromGenerators BFS 展开全 24 元素 + verifyHomomorphism 机器验证同态性；核恰 = V₄（4 双对换）、满射 Im=S₃（6 阶）、G/Ker=24/4=6 ≅ Im 即第一同构定理可视化；窗口底部 Ker=4/Im=6 + notInjective/surjective chips；node `s4toS3.test.ts` 固化该构造防猜测式回归 |
| 2026-09-06 | **FGVE 阶段 2 批次六（v1.22.0，群作用视图 props 化 + 受控 ViewWindow：conjugation / regular / custom 三来源）**：**内核拆分**——`ActionView.tsx`（1028 行）拆新文件 `ActionScene.tsx` 纯 props 内核（`ActionSceneProps`：group/kind/computation/editing/setSize/arrows/error/onAddArrow/onBindArrow/onRemoveArrow/onReplaceGenArrows/selectedElement/onSelectedElementChange/hoveredElement/onHoverElementChange/onSetElementSelect/showLabels/onHover/canvasTransform/viewBoxSize/prime；banner + svg 场景 + Stab box 全场景进 Scene；`CustomActionEditor`/`DisplayMode` props 化——context 成员全部换 props，selectedGen/drag/draw 本地态保留；ClusterNode 增 `testId/onHoverIn/onHoverOut`——环节点 hover 联动仅窗口模式（`onHover` 传入）挂载，主画布壳零行为变化）+ `ActionView.tsx` 降级 46 行 context 壳（useGroup 组装，签名不变零回归）；`GroupContext.actionError` 宽类型收窄为 `CustomArrowError`。**纯函数层**——`actions.ts` 新增 `arrowListAdd/arrowListBind/arrowListRemove/arrowListReplaceGen` 箭头列表纯变换（GroupActionContext 四个 action 改调同一实现，与窗口端共享单一事实源）。**ActionViewParams**（viewConfig.ts：`actionKind` 缺省 conjugation（enum 仅三来源，sylow/coset 拒绝）/`setSize` 1-20/`arrows` JSON 化数组（generatorId nullable、from/to 0-19、max 200）/`showLabels` 缺省 false；`actionViewParamsSchema`）。**ViewWindow 接入**——ViewParams union / schema 选择器 / `updateViewParams` union / `renderContent` action 分支（conjugation/regular 由 `buildActionComputation` 自算、custom 由 viewParams 已验证 arrows 自算——**空 arrows=平凡作用合法可显示**，坏生成元/换群失效 → computation null → noAction 兜底）/ 参数面板 **Action View 段**（kind 三分段 Conjugation/Translation/Custom + custom 时 \|X\| 输入（非编辑态改→清 arrows）+ Edit arrows / Complete & verify / Clear arrows / Cancel + Show labels）。**custom 编辑流窗口承载**——编辑态为 ViewWindow 本地 state（**不持久化**：viewParams 只存已验证结果，编辑中断刷新回已验证态，博客可注入预验证 arrows）；Complete & verify 经 `buildActionComputation` 校验（error / violation 映射与主画布 completeCustomAction 同款）通过才写回 viewParams；displayKey 换群清编辑态/选中/hover。**手势 guard**——编辑模式禁 ct 拖拽（onCtMDown）/Ctrl 滚轮缩放（onWheel）/zoom slider/figure-hint（主画布编辑态同样不吃 transform）。**窗口缺省 UX**——showLabels=false：节点空圈 + 顶部轨道 chips 区隐藏（computeFitVB/DisplayMode 布局条件化），conjugation/regular 节点悬停就地气泡（anchor 世界→视口换算与 CycleView 同约定）+ 金色箭头联动；banner 与 Stab box（点节点 OST \|Orb\|·\|Stab\|=\|G\|）保留。**TestPage** 补 4 窗矩阵（S₃ 共轭 / C₄ 平移 / S₃ custom 预注入已验证 arrows（X=3 自然作用，不动点自环显式编码）/ A₄ 共轭受控）+ 覆盖清单追加。测试：新增 node `core/actionViewConfig.test.ts`(10：schema 三来源接受 + sylow/coset 拒绝 + setSize/arrows 越界 + round-trip + 箭头四纯变换) + dom `ActionWindowParams.component.test.tsx`(7：共轭缺省 6 节点 3 轨道无标签、切 Translation 单轨道、custom 编辑流平凡作用写回、hover 气泡、坏 actionKind schema 回退 + 坏生成元 noAction、Show labels 0→12 foreignObject、custom arrows 持久化) → **75 文件 1640 tests 全绿** + lint + typecheck + build（1.67s）+ **E2E 13 passed**；浏览器实测——S₃ 共轭 3 轨道/★/hover 气泡（e·order 1）/Stab box（元素 23 稳定子 \|G_x\|=2）、C₄ 平移单轨道正则 banner、A₄ 受控切 Translation + Show labels、custom 预注入重开直接显示、console 零错误；版本 1.21.0→1.22.0（package.json/package-lock/welcome.version zh/en） |
| 2026-09-07 | **FGVE 阶段 2 批次七（v1.23.0，对称性视图 props 化 + 受控 ViewWindow）**：**内核拆分**——`SymmetryView.tsx` 拆 `SymmetryViewScene` 纯 props 内核（`SymmetryViewSceneProps`：group/symmetryType?（缺省 getSymmetryType(group)）/dark（场景配色 #0a0a1a↔#f4f4f7，主画布传主主题、ViewWindow 传窗口主题解耦）/variant（cube↔octahedron、icosahedron↔dodecahedron）/showAction/actionElementId/rotateSpeed/showFigureTitle/locked（锁相机交互）/onHint/hintOnIdle（主画布 true 引导去其它视图点元素；窗口 false——引导写在参数面板，浮条只显真实状态）/replaySignal）+ `SymmetryView` 降级 context 壳（签名不变，GroupCanvas/旧浮动窗零改动）；场景内悬浮变体切换按钮移除、入口改由宿主提供（主画布 ViewPanel Shape 选项 / 窗口 ⚙ 面板）——配套 `GroupSymmetryContext` 新增 `symmetryVariant/setSymmetryVariant`（换群复位），SymmetryView 适配器经 ViewPanel 的 Shape 切换喂 variant。**动画缺口修复**：同元素动画播完一次后无入口重看——`replaySignal` 显式重放信号（宿主自增即对当前演示元素重播一次，姿态目标不变）。**SymmetryViewParams**（viewConfig.ts：variant/showAction/rotateSpeed 0.2-5（缺省 1）/actionElementId（换群失效由渲染层忽略）/showFigureTitle + `symmetryViewParamsSchema`）入 ViewParams union / schema 选择器 / updateViewParams union。**ViewWindow 接入**——renderContent symmetry 分支：SymmetryViewScene 直渲（dark=窗口主题、`showFigureTitle` 缺省 false——窗口标题栏已显示群名，避免「标题一个群名、场景又一个群名+几何」重复、locked=config.locked、hintOnIdle=false、replaySignal、onHint→内容区底部唯一浮条 ⟳ Replay / ✕ Reset）+ 视作自管理布局禁 ct 平移缩放（同 3d，wheel/onCtMDown/zoom slider guard）+ 参数面板 **Symmetry View 段**（unsupported 群提示 supported 清单 / dual solid 切换（仅 cube|icosahedron 类群：Cube↔Octahedron、Icosahedron↔Dodecahedron 分段按钮）/ Show element actions 复选 + Speed slider 0.2-5× / Action element 列表（可滚动，点行触发旋转、点已选行重播、✕ Reset pose 复位）/ **config.actionLocked 固定演示模式**——演示元素由 viewParams.actionElementId 钉住、列表与 Reset 隐藏只读显示 fixed 元素（数据钩子 sym-action-fixed/list/row、sym-panel-reset）/ Show figure title 复选）。**TestPage** 补 10 窗矩阵（A₄ 四面体默认 + 演示（预置 3-cycle）/ S₄ 对偶八面体 variant / D₆ 六边形 / C₈ 八边形 / V₄ 长方形 / A₅ 二十面体 / Q₈ unsupported overlay / C₈ 受控参数 / D₆ actionLocked 固定演示对照）+ 覆盖清单追加；**TestPage2** 博客加第 7 节「A₄ 在正四面体上的作用」配图窗口。测试：新增 dom `SymmetryWindowParams.component.test.tsx`(10：C₄ cyclic 渲染无 shape 段且无 hint 浮条 / S₄ 群 shape 选项 Cube·Octahedron / 元素列表点击→演示→再点重播（不 toggle-off）→Reset 受控 / variant·rotateSpeed onViewParamsChange 回传 / 坏持久化参数 schema 回退缺省 / actionLocked 列表与 Reset 隐藏 fixed 只读+Replay 可用 / actionLocked 无预设元素只读占位 / persistKey 含 view 名不冲突) → **75 文件 1640 tests 全绿（node 56/1488 + dom 19/152）** + lint/typecheck/build + **E2E 13 passed**；版本 1.22.0→1.23.0（package.json/package-lock/welcome.version zh/en）
| 2026-09-07 | **FGVE 阶段 3 批次八（v1.24.0，打包最小闭环：`@groupviz/core` + `@groupviz/react` 双包可构建 + `examples/host-minimal` 最小宿主消费冒烟）**：**core 包**——`vite.pkg.core.config.ts` library mode 出 `dist-pkg/@groupviz/core`（566KB，唯一外部依赖 zod）；**react 包**——`vite.pkg.react.config.ts` 自 `src/package/react.ts` 门面出包（首批导出 SetView + CycleView + I18n/Theme Provider + renderTex），仅 19.6KB 不内嵌 core；**关键工程坑**——rolldown(vite8) 不 consult 相对导入的 resolveId 钩子，改 `scripts/pkg/config-common.ts` 的 **coreToPackage transform 钩子**把 121 个源文件中相对 `../../core/...` 导入统一改写为 `@groupviz/core` external 包名；`scripts/pkg/finalize-pkg.mjs` 做 d.ts 树分发 + 相对 core 引用→包名重写 + 双包 package.json 生成 + **theme.css 从 src/index.css 自动提取**变量块（dark/light 134 变量）+ 视图基础类（防双源漂移）；`tsconfig.pkg.json` emitDeclarationOnly 类型产物；npm script `build:pkg` 串起全链。**host-minimal**（examples/，最小宿主演示真实 npm 消费）：vite alias 正则完整匹配（防 `@groupviz/react` 前缀吞子路径）+ `fs.allow` 放行仓库根（KaTeX 字体 403 修复）；Demo 载群（createGroupFromSymbol 符号工厂，descriptor 真 round-trip 留待后续）→ Set/Cycle 视图切换 → 群切换 → SVG 导出全链路。**浏览器实证**（playwright）：S₄ 24 元素 Set 网格 / Cycle 中心 e 辐射 / 切 C₆ / 导出 SVG 21.5KB 合法 viewBox / console 零 error。测试：lint/typecheck/build:pkg ✅；主机 app 不受打包改动影响。版本 1.23.0→1.24.0（package.json/package-lock/welcome.version zh/en）。详见 ROADMAP §2.8 |
| 2026-09-07 | **FGVE 阶段 3 批次九（v1.25.0，首批带 i18n 视图入包：语言包入包 + CayleyView / CosetStripScene 双 Scene 直达）**：延续「先跑通再全量」节奏（用户拍板本轮只做 2 个，先验证带 t() Scene 的入包路径）——**策略换道**：批次八「Scene 文案 props 化、react 包不内置语言包」被用户否决，改 **「语言包入包」**：Scene 保留 `useTranslation`，react 包直接内嵌 `translations.ts`（1385 行 zh+en）并从 i18n 导出 `I18nProvider`；因模块重复，包内 Provider 与主应用 Provider 是**独立实例**，消费端必须用包内 `<I18nProvider>` 包裹 Scene，否则 t() 回落原始 key（Playwright 首验即复现原始 key + 条带缺色，包 Provider 后修复：3 条带 + 中文图注「子群凯莱图 ⟨H⟩」+ console 零错误）。**Scene 拆壳**——`CosetStripView.tsx` 原 Scene+壳混合（Scene 部分用 useTranslation 3 key：canvas.cosetStripCayley/NoSubgroup/NoCosets；壳读 useGroup/useHover），拆新文件 `CosetStripScene.tsx` 纯 render 内核（props：group/selectedElements/canvasTransform/viewBoxSize/cosetElementMap/cosetColors/cosetHighlightSet/subsets/showLabels/showSubgroupCayley/onSelect/onHover）+ `CosetStripView` 降级 context 壳（签名不变，FloatingViewWindow import 改指新文件，主应用零回归）；`CayleyView` 本就零 context 依赖直入包。**react 门面扩展**（src/package/react.ts）：+ CayleyView/CayleyViewProps + CosetStripScene/CosetStripSceneProps + I18nProvider。**d.ts 修复**：`I18nContextValue` 加 export（useTranslation 返回类型引用它 → tsc d.ts emit TS4058）。**构建**：`build:pkg` 产物 core 566KB（唯一外部依赖 zod）、react 19.6KB→103KB（语言包入包的体积来源）；`scripts/pkg/finalize-pkg.mjs` .dts 临时树清理 try/catch 容错（sandbox SAFE_DELETE_BULK_CONFIRM_REQUIRED 对 >50 文件批量删 fail-closed，改 `find -depth -type f -delete` per-file 清理）。**消费实证**（TestPagePkgConsume 批次九区块整体包包内 I18nProvider）：CayleyView 块（S₃ 六边形凯莱图）+ CosetStripScene 块（`useS3CosetMap`：createGroupFromSymbol('S_{3}') → 找非单位自逆元素作 H → 归一化键（成员 id 升序 join）算 3 右陪集 → 3 条带色条带）。门禁：typecheck ✅ / lint（仅改文件）✅ / vitest 1640（75 文件）✅ / build:pkg ✅ / 主应用 build ✅。版本 1.24.0→1.25.0（package.json/package-lock/welcome.version zh/en）。余 6 Scene（table/3d/sublattice/homomorphism/action/symmetry 带 t()/theme 依赖）按同策略后续批次入包。详见 ROADMAP §2.8 |
| 2026-09-07 | **SmallGroup(n,i) 记号解析修复（v1.25.0 追加）**：此前 `createGroupFromSymbol('SmallGroup(n,i)')` 只对 symbol 冲突改名群（SmallGroup(16,13) / SmallGroup(20,3)——registry map 仅在冲突时存 'SmallGroup(n,i)' 键）生效，其余 **91/93 群一律返回 null**，与源码注释宣称的 GAP 记号支持不符（用户实测发现）。修复（src/core/groups/groupFactory.ts）：改名群优先走注册表（保留改名 symbol + precomputed 子群数据，语义不变）；其余直查 `SMALL_GROUP_DATA` 并用 `createTableGroup` 重建——数据表本身按 GAP (n,i) 序存，绕开 registry order≤15 手写条目序 ≠ GAP 序的错位（order 12 尤甚：GAP(12,1)=C₃:C₄ 而 registry index0 是 C₁₂，故不能走 getSmallGroup(n,i-1)）；越界（16,15 / 32,1 / 9999,1）保持返回 null 不抛错（createTableGroup 自身对缺数据仍抛 unsupported，resultGuards 契约不变）。测试：node +2（smallGroups.test.ts：93 条 GAP 记录遍历全可达且 order 精确 + order 12/24 抽样 symbol GAP 精确（C₃:C₄/C₁₂/A₄/C₄×C₄/SL(2,3)）+ 改名群符号回归 + 越界 null）→ **75 文件 1642 tests 全绿** + lint/typecheck + build:pkg（产物 core/react v1.25.0，dist-pkg 产物实测修复生效） |
| 2026-09-07 | **FGVE 阶段 3 批次十（v1.26.0，余 6 个 Scene 全量入包：TableView / ActionScene / HomomorphismScene / SymmetryViewScene / Cayley3DScene / SublatticeScene）**：延续批次九「语言包入包」策略一次性把余下 6 个 Scene 全部入包（前批用户拍板「只做 2 个先跑通」，本批把余下 6 个全量收尾视图入包阶段）。**入场方式按依赖复杂度三档**——🟢 直入：`TableView`（TableViewFromContext 已是独立壳，TableView.tsx 纯 props 内核直接导出；**包内 Scene 引入 triggerDownload 经 utils/export 拉入 gifenc 整个 GIF 库约 24KB**——主应用其它 Scene 仍经 export re-export 不动，将 `triggerDownload` 抽零依赖模块 `utils/download.ts` 切断 gifenc 链路，react 产物零 GIF 库污染；`tsconfig.pkg.json` include 补 `src/types` 拉入 `gifenc.d.ts` 治本治标）+ `ActionScene`（独立文件 ActionScene.tsx，仅 useTranslation + core 零 context 依赖）；🟡 拆文件：`HomomorphismScene`（同文件拆出 Scene + HOMO_COLORS/KERNEL_RED/IMAGE_CYAN/bezierPath/findGeneratorElements，壳 HomomorphismView 留 useGroup 装配 + FirstIsomorphismAnimation 注入 theoremAnimation；无主题依赖）+ `SymmetryViewScene`（同文件拆出 Scene + 几何 helpers（getCyclicFigure/DihedralFigure/Cube/Icosahedron/Octahedron/Dodecahedron/Tetrahedron/Rectangle）+ SymmetryScene/AnimatedGeo/AxisMarker/UnsupportedOverlay/useAnimatedRotation 全渲染链，壳 SymmetryView 留 useGroup + useTheme 喂 dark={theme==='dark'}；原本就纯 props 仅 dark prop 决定场景配色）；🔴 拆文件 + theme props 化：`Cayley3DScene`（NodeSphere 原直接 useTheme 决定 label 阴影二选一，改为新增 `isDark: boolean` prop——Scene 解构 `themeProp ?? ctxTheme.theme` 后透传 Body，Body 传 NodeSphere isDark；Scene 背景色主题二选一并入 prop 化；新加 `theme?: 'dark'\|'light'` 缺省回落 ThemeContext 行为零回归）+ `SublatticeScene`（Scene 主体 `const palette = useMemo(() => buildPalette(theme), [theme])` 由原 useTheme 改 `themeProp ?? ctxTheme.theme`；SeriesPanel 同步加可选 `theme?` prop——保持无外部消费者的内部组件签名兼容——Scene 渲染处显式透传 effTheme 保证 palette 与 chips 配色一致）。**core 索引补 export**——`core/index.ts` 漏 `export * from './symmetryType'`，SymmetryViewScene 引入 `getSymmetryType` 经 coreToPackage 改写为 `@groupviz/core` 后在主应用 build 触发 MISSING_EXPORT（react 包 index.js 引用了 core 未导出的符号），补 export + 重打 core 包 + 重新 finalize（react 端不动），`getSymmetryType` 入 core 公共面与`createGroupFromSymbol`/`buildActionComputation` 对齐。**d.ts 修复**——`src/theme/ThemeContext.tsx` `interface ThemeContextValue` 改 `export interface ThemeContextValue`（Cayley3D/Sublattice Scene 第一次把 useTheme 拉入 react pkg program，其返回类型引用 ThemeContextValue 但匿名，tsc emit TS4058；与批次九 `I18nContextValue` 加 export 同解法）。**react 门面**（src/package/react.ts）——`+ HomomorphismScene/HomomorphismSceneProps + SymmetryViewScene/SymmetryViewSceneProps + Cayley3DScene/Cayley3DSceneProps + SublatticeScene/SublatticeSceneProps + TableView/TableViewProps + ActionScene/ActionSceneProps`（共 +6 Scene，原 4 → 10）。**消费者 import 同步**——`FloatingViewWindow.tsx` 4 处 import 拆分：HomomorphismView / HomomorphismScene、SymmetryView / SymmetryViewScene、Cayley3DView / Cayley3DScene、SublatticeScene 各引相应文件；测试 `Cayley3DView.component.test.tsx` + `SublatticeView.component.test.tsx` import 路径同步改指新 Scene 文件（cayley3d.component.test.tsx 3 用例名仍为「Cayley3DScene · controlled 3D cayley view」测试 Scene 行为零回归，sublattice 同理）。**TestPagePkgConsume 扩展**（主应用懒加载区块）——加 TableView（当前切群 S₄/A₄/C₆/D₄ 即时刷新）/ ActionScene（固定 S₃ + `buildActionComputation(S₃, {kind:'conjugation'}).computation`，展示环轨道与中心 e ★）/ HomomorphismScene（固定 C₆→C₂ 满同态 mod 2，映射用 C₆ 生成元幂遍历构造——不依赖 descriptor 生成元约定漂移，纯 multiply 推算 k%2）/ SublatticeScene（当前切群 ≤60 自算 computeSubgroupLattice）/ Cayley3DScene（当前切群 + `theme="dark"` 显式与容器解耦——包内无 Provider 时 fallback 默认值印证 theme prop 化必要）/ SymmetryViewScene（固定 D₄ + dark + showFigureTitle={false} 避双群名——窗口标题栏已显群名，场景图注关）。**Playwright 实证**（`npm run build` + `vite preview`）——`http://localhost:4174/?test=1` 10 个消费区块全部 present（pkg-set/cycle/cayley/cosetstrip/table/action/homo/sublattice/cayley3d/symmetry）；SVG 层 4 个（table/action/homo/sublattice，4 SVG 视图）、R3F canvas 2 个（cayley3d/symmetry，2 3D 视图）；homomorphism source 节点 6 个 = C₆ 阶验证双群映射正确；console 零 error / pageerror 零；视觉确认——S₃ conjugation 环 + 中心 e ★、C₆→C₂ 双群满同态紫绿映射、D₄ dihedral 长方体三轨道变体渲染。**门禁**——typecheck ✅ / lint（仅改文件 + 全量）✅ / vitest 75 文件 1642 tests 全绿 ✅ / build:pkg（react 产物 103KB→231KB gzip 48→63KB，含 +5 个 Scene 增量，gifenc 0 出现） ✅ / 主应用 build ✅（首次缺 symmetryType export 报 MISSING_EXPORT，补 export 后通过）。**版本** v1.25.0→v1.26.0（package.json/package-lock.json/welcome.version zh+en 双语）。**FGVE 阶段 3 视图入包阶段收官**：10/13 视图入包（set/cayley/cycle/table/3d/sublattice/homomorphism/cosetstrip/action/symmetry）；延后项 = tree/prestable props 化（v1.17.0 起持续延后）+ action 窗口内 sylow/coset actionKind（ActionViewParams enum 拒收，v1.22.0 起持续延后）；阶段 3 余待办 = CI Playwright 冒烟接入 + exports 字段子路径 + descriptor 真 round-trip 载群。详见 ROADMAP §2.8/§4/§5 |

| 2026-09-09 | **双包 npm 发布准备（产物卫生 + 元数据 + 真实安装冒烟，pkgVersion 2.0.0）**：评估结论「pack 可行但缺真实安装验证 + 产物含 favicon 垃圾 + 元数据裸」后集中补齐——① **版本治理**：根 package.json 增 `pkgVersion`（2.0.0）独立于主应用迭代版本，finalize-pkg.mjs 读 `pkgVersion ?? version`（react peer 锁 @groupviz/core ^<pkgVersion>，两包须成对同版发布）；② **产物卫生**：vite.pkg.{core,react}.config.ts 加 `build.copyPublicDir:false` 剔除 public favicon 三件套混入纯算法 core 包；③ **元数据**：双包 package.json 补 license(MIT)/repository(rrCathy/GroupViz)/keywords，包内分发根 LICENSE + 简短 README（快速开始示例以真实 API 签名为准）；④ **peer 独立范围表**（不再跟随主应用 deps）：react ^19.0.0 / react-dom ^19.0.0 / three >=0.184.0 <0.186.0 / @react-three/fiber ^9.0.0 / @react-three/drei ^10.0.0 / katex ^0.16.0（registry 实测 latest 19.2.8/0.185.1/9.7.0/10.7.8/0.18.7，原 ^19.2.5/^0.184.0/^0.16.45 过紧会锁死消费端到旧生态；react 包移除冗余 zod peer——core dependency 已含）；⑤ **d.ts 相对导入补 .js 扩展**（发布级缺陷：NodeNext 消费端对 ESM 声明强制显式扩展，无扩展导致 export * 链断 → TS2305 "no exported member"；bundler 消费端同样兼容 TS 声明映射）；⑥ **发布门禁 `publish:smoke`**（scripts/pkg/publish-smoke.mjs，npm run publish:check = build:pkg + smoke）：npm pack → 干净 os.tmpdir consumer `npm i` file: tarball（peer 由 npm 自动安装，模拟真实安装路径）→ core node 冒烟（C₄/S₃/D₄/A₄/Q₈/GL(2,3) createGroupFromSymbol + serialize/deserialize round-trip 全过）→ react SSR 冒烟（react-dom/server renderToStaticMarkup <I18nProvider><SetView/>→ 5194 bytes svg=1）→ 类型双 resolution 冒烟（NodeNext + bundler 两 tsconfig tsc --noEmit，SetViewProps 可解析可构造）→ 全绿后清临时目录。**坑**：scoped 包 npm pack --json 文件名不带 @（groupviz-core-2.0.0.tgz）；JSX 类型冒烟文件必须 .tsx；d.ts 分发 relative 基线 bug（相对 DTS_ROOT 而非 CORE_DTS 导致产物嵌套 core/core/）已修；本地 WorkBuddy 沙箱对 vite emptyOutDir 大批量删 fail-closed（无沙箱/CI 环境正常，仅本地需 escalation）。门禁：build:pkg ✅ / publish:smoke ✅（npm install 真实解析 exports + peers）/ eslint scripts/pkg ✅。**发布剩余动作（用户侧）**：确认 @groupviz scope 的 npm org 归属（registry 404 ≠ 拥有权）→ `npm publish dist-pkg/@groupviz/core` + `npm publish dist-pkg/@groupviz/react`（两包同版成对发）。 |

| 2026-09-09 | **主应用版本 1.26.0 → 2.0.0(与双包 npm 版本对齐)**：package.json / package-lock.json(顺带修复顶层 version 字段丢失引号致 JSON 损坏的遗留 bug——packages[""] 同步 1.25.0→2.0.0)/ i18n welcome.version zh+en 双语 / AGENTS 文档版本。同时剔除 7 个误提交的浏览器验证截图(homo-*.png ×3 / hostminimal-*.png ×2 + export.svg / testpage-pkgconsume.png——均为 09-07/09-08 批次视觉验证产物,零文档引用,已 git rm + .gitignore 补 homo-*/hostminimal-*/testpage-pkgconsume 模式兜底)。 |

| 2026-09-09 | **3D 凯莱图子群陪集面填色（src 直改，视图窗口新功能，未升版）**：新 `src/core/algebra/faces3D.ts`——语义=「可选面 = 3D 布局几何中被某真子群 H 单个陪集（右乘轨道）占满、共面、边界由作用边闭合的平面凸多边形」；`subgroupFaces`（陪集轨道/平面度校验/2D 凸包/边界边闭合校验，单位元 index-0 误杀修复：`idx===undefined` 判定）、`listFaceSubgroups`（几何可用子群候选；order>60 守卫返回 null）、`buildUndirectedEdgeKeys`、`FACE_COLOR_PALETTE` 12 色（Scene 与面板共用保证 WYSIWYG）；`Cayley3DViewParams.faceFill`（enabled/subgroup 元素 id 升序/faceColors Record（key=陪集元素 id 升序 join,）/opacity 0.15–0.9）+ zod；`Cayley3DScene` props faceFill → memo 构建 fan 三角化 BufferGeometry（meshBasicMaterial DoubleSide/depthWrite:false/renderOrder −2 置于边下，换参自动 dispose）；`FloatingViewWindow` ⚙ 面板「Face fills」区块——开关+子群下拉（结构 ⟨生成元⟩·n face）+透明度滑杆+逐面 color（行首代表元素 label + f.size·H），派生 faceSubgroupCands/faceSelSubgroup 换群/换布局自动失效回落；锚点案例验证：A₄ 截角四面体选 C₃=⟨(234)⟩ → 4 截角三角面、D₅ 棱柱双环选 C₅=⟨r⟩ → 顶/底 2 五边形、C₂³ 立方体选 V₄ → 相对 2 方块（六边形/侧长方因跨多陪集天然不可选）；测试 node +4（faces3D.test.ts 三群几何断言 + 候选过滤）+ dom 计数修正 + A₄ 面板联动用例（选 C₃ → 4 color 行 + 2 range + 改色 #123456 回写）→ 75 文件 1646 tests 全绿 + tsc 0 error；清理遗留 tmp-face.test.ts/faces3D.debug.test.ts。边界：主画布 Cayley3DView（context 壳）暂不接 face UI（仅窗口/包 props 路径）；左乘 multiplyType 不做面（陪集按右乘约定）；>60 阶无候选。 |
| 2026-09-10 | **FGVE 引擎消费卡点优化（v2.1.0，博客嵌入实测反馈全量修复：P0/P1/P2 三层 + 附带挖出一个双包运行期漏导出 bug）**：来源=博客作者将 `@groupviz/core`+`@groupviz/react` 嵌入站点后的实际卡点清单，逐条对源码核验（结论：全部属实，且 P0-1 比描述更严重——未解析元素引用不仅动画不播，还会因 `enableRotate={!showAction && !locked}` 把相机彻底锁死）。**P0-1 元素引用解析**：新 `src/core/algebra/elementRef.ts`（`resolveElement`/`findElement` 按 **id → label → value** 三档归一化匹配（去空格），未命中返回 null；`resolveElementRefs` 返回 `{elements,ids,unresolved}`、`resolveElementIds`、`elementRefId`）；新 `src/utils/elementRef.ts`（`resolveElementWarn`/`resolveElementIdsWarn`——未命中 `console.warn` 一次并按 `context\|symbol\|ref` 去重，**不再静默**；测试用 `clearElementRefWarnCache`）；`cayleyActions.normalizeCayleyActions` 对非法 `elementId` 由「静默过滤」改 warn；`SymmetryViewScene.actionElementId` 经 `resolveElementWarn`（不再静默 null）。**P0-2 受控状态便利层**：新 `src/hooks/useSceneState.ts`（`useSceneState(group?,options?)` → ``；**pan 走 React 合成事件**（节点 `stopPropagation` 不被画布拖拽吞掉）、**wheel 走原生非被动监听**（React root wheel 是被动的，无法 preventDefault → 光标锚定缩放失效）；换群自动清空 hover/选中/节点位置；`sceneProps` 可整体 spread 进 Set/Cycle/Cayley/CosetStrip）。**P0-3 主题统一**：新 `SceneThemeRoot`（`theme?: 'dark'\|'light'` → 注入 `data-theme` 作用域；**未传时直接返回 children，零额外 DOM、零行为变化**）+ 新 `SceneHoverBubble`（默认悬停气泡，读 `theme.css` 变量，显示 label+id+order）；7×2D Scene（Set/Cycle/Cayley/CosetStrip/Action/Homomorphism/Table）+ `SymmetryViewScene` 全部加 `theme?`（默认不注入）。**P1**：新 `src/core/algebra/elementOrder.ts`（`elementOrder`/`elementOrderDistribution`/`elementOrderDistributionOf` group-first facade）、`SymmetryViewScene.onAnimationEnd`（姿态落定回调）、`I18nContext` 默认 `t` 改 `zh → en → key` 三级兜底（**免 Provider 不再回落裸 key**）+ `I18nProvider` 增 `lang?`/`persist?`、新 `src/core/algebra/cosetView.ts`（`buildCosetViewData` 一键产出 `{subgroupElementIds,cosets,cosetElementMap,cosetColors,cosetHighlightSet,side}` + `subgroupFromElementIds`/`isSubgroupElementSet`）+ `CosetStripScene` 增 `subgroup?: string[]` 便捷入口（显式三件套 props 优先）。**P2**：新 `docs/API.md`（351 行 Scene props 全表 + core 新导出 + 元素引用/主题约定，经 finalize-pkg 写入两包）；阈值不再硬编码——`largeGroupThreshold`（Set/Cycle/Cayley）、`maxTableOrder`/`maxHeatmapOrder`（Table）、`maxEnumerateOrder`（Sublattice）+ `viewBox.ts` 新 `sizeLimitFor(view)` + `isTooLarge(order,view,limitOverride?)`；相机门控改 `lockCameraOnAction?: boolean = false`（**默认不再锁死**，需要者可开回）。**⚠️ 顺带挖出的双包运行期 bug（潜伏，本批次若不修就会随 v2.1.0 发出）**：`src/core/index.ts` 从未 `export * from './algebra/faces3D'`，而 react 侧 Cayley3DScene/FloatingViewWindow 从其导入 `FACE_COLOR_PALETTE` → 构建把 core 设为 external，rollup 无法校验具名导出 → **漏导出被静默打进 react 产物**，消费端一 import 就抛 `does not provide an export named 'FACE_COLOR_PALETTE'`（冒烟门禁暴露）。修复：core/index.ts 补 export；并给 `publish-smoke` **新增 react→core 具名导出一致性检查**（正则抽出 react 产物所有 `from '@groupviz/core'` 的具名导入，逐个比对 core 真实导出集，此 bug 类永久门禁）。**已发布 npm 的 v2.0.0 经核不受影响**（其实测产物零 faces3D 引用——该漏导出源自 2026-09-09 未随包发布的 faces3D 直改工作，属未发布区间引入）。**另两处门禁自身修复**：① `publish-smoke` 消费端**钉死宿主已解析的 react/react-dom/three 精确版本**——冒烟目录无 lockfile，只给范围会让 npm 浮动到 react@19.3，与 `@react-three/fiber@9.7.0` 收紧后的 peer `>=19 <19.3` 冲突 → ERESOLVE 假失败（此前误判为「产物有问题」）；② 类型冒烟补 `CayleyView/CosetStripScene/SymmetryViewScene` import + `createGroupFromSymbol(...)!` 非空断言（原文件 TS2304/TS18047）。**反向兼容**：全部附加式（新导出 + 可选 props，未传=旧行为；`SymmetryViewScene.dark` 保留为 `theme` 别名，`theme` 优先）→ 严格 2.x minor，**pkgVersion 2.0.0 → 2.1.0**（与主应用 `version`/`package-lock`/`welcome.version` zh+en 同步）。**验证**：`tsc --noEmit` 干净 + 全量 **81 文件 1721 tests 全绿**（新增 elementRef 15 / cosetView 20 / elementRefWarn 7 / SceneTheme 14 / useSceneState 14 + viewBox 阈值用例）+ `build:pkg` ✅ + `publish:smoke` ✅（真实 npm install 解析 exports+peers、core+react SSR+NodeNext/bundler 双 tsc 全过）。**消费侧文档**：`examples/host-minimal/src/Demo.tsx` 改写为 `useSceneState` + `buildCosetViewData` + `theme` 切换 + label 版 `actions` 示范（无需 `I18nProvider`）；两包 README 快速开始换 `useSceneState` 版。 |

| 2026-09-10 | **v2.1.0 收尾：全量 lint 清零（31 errors → 0）+ 一处真实健壮性缺口**：发布前补跑全量 `npm run lint` 发现 **31 errors**（此前只对改动文件跑过 lint，`tsc` + test 全绿完全无感）。逐条定位后分五类修掉：**① `react-hooks/refs`（eslint-plugin-react-hooks v7 = React Compiler 规则集）的「返回体污名化」**——实测判定规则：hook 返回体**顶层**出现 【`RefObject`】/【写 ref 的 `useCallback`】/【裸 `useState` setter】 中任意一个，**整个返回对象**即被标记为 ref 载体，宿主此后任何 `s.xxx` 读取（spread、`s.hostProps.style`、`{s.hoverBubble}`、`s.selectedElements.size`…）全部报 *Cannot access refs during render*；而**解构**、**元组返回**、**嵌套**（`hostProps.ref`）均不触发。原 `useSceneState` 顶层带 `hostRef: RefObject` → 新写的测试文件一次吃满 21 个报错，**意味着宿主照文档写也一样中招**。修法：`SceneState` 去掉顶层 `hostRef`，回调 ref 只以 `hostProps.ref` 出现（`<div {...s.hostProps}>` 一次 spread，DX 反而更短），新增 `getHostElement()` 取 DOM 节点，文档/包 README/类型冒烟模板同步并明示「别再自己写 `ref`——会顶掉内建 ref」；测试文件加**运行时返回体形状守卫**（断言顶层无 ref-like 对象）并注明该文件全部 `s.xxx` 用法即此规则的**静态回归守卫**。**② 渲染期写 ref（3 处）**：`transformRef.current = canvasTransform`、`groupRef.current = group`、`toScreenRef.current = toScreen`——`transformRef` 改「权威值」只在回调里同步写（`setCanvasTransform` 统一收口，滚轮/拖拽读到的是最后一次已应用值，无陈旧窗口）、`groupRef` 删掉直接用 `group` 并补 deps、`toScreenRef` 是死代码连同未被引用的 `toScreen` 一并删除。**③ `react-hooks/set-state-in-effect`**：换群清会话态（hover/选中/节点位置）从 effect 改 **渲染期校正 state**（React 官方 Storing information from previous renders 模式）→ 少一次提交，且不再存在「一帧带着旧群残留 id」的中间态；原「换群清空」用例仍绿。**④ `SymmetryViewScene`** `endRef.current = onAnimationEnd` 渲染期写 ref → 改 `useEffect` 同步（保住「每帧新闭包不串味」的原始意图）。**⑤ 顺带修掉的真实健壮性缺口**：`cursorToViewBox` 未校验指针坐标，滚轮事件不带 `clientX/clientY` 时 anchor 变 NaN 并**把 `canvasTransform` 写成 NaN——整张图消失且无法自愈**；现缺坐标退回容器中心、`zoomAt` 拒绝非有限 anchor（宁可不动）。**⑥ 顺手清 `react-hooks/immutability`**：`examples/host-minimal/Demo.tsx` 去掉自建 `svgHostRef` + 组合 ref，导出 SVG 改走 `getHostElement()`。**验证**：`npm run lint` **0 problems**、`tsc -b` 干净、全量 **81 文件 1724 tests 全绿**（node 60/1540 + dom 21/184，dom +3：返回体形状 ×2 + 滚轮无坐标不变量 ×1）、`build:pkg` + `publish:smoke` 通过。**教训**：`react-hooks/refs` 的判定粒度是「返回体整体」而非「实际访问的那一行」，任何把 ref/ref 写入函数放在 hook 顶层返回值里的公共 API，对方接入后必踩；此外**新增文件后必须补跑全量 lint**，改动文件级 lint 会漏掉这类跨文件规则。 |
| 2026-09-10 | **元素引用「循环记号」语义档 + Sₙ 多环 label 畸形修复（v2.1.1）**：v2.1.0 发布后实测发现**各群 label 约定不统一**——Aₙ 带括号（`(234)`）、Sₙ 单环被剥成 `234`、多环更被剥成畸形的 `12)(34`、循环群为指数 —— 宿主写**标准循环记号** `(234)` 在 S₄/S₃ 上查不到（`resolveElement` 只做字面串匹配，命中不了便 warn+忽略，动画/相机不生效）。**① `resolveElement` 新增第 4 档「循环记号语义匹配」**（`src/core/algebra/elementRef.ts`）：新 `parseCycleNotation(ref, degree)` 把 `(234)` / `234` / `12)(34` / `(1 2 3)` / `(1,2)(3,4)` 统一解析为置换数组再与 `el.value` 比对，于是 `(234)` ≡ `234`、且**跨群可互写**（A₄ 宿主写 S₄ 记号的 `234`、S₄ 宿主写 A₄ 记号的 `(234)` 都命中同一元素）；仅对「置换群」启用（新 `permutationDegree` 校验所有 value 恰为 `1..n` 的一个排列，循环群 `[i]` / 二面体 `[r,s]` 自动排除，**零误伤**）；全不动点引用（裸 `1` / `(1)`）判 `null`，不误命中恒等元。**歧义防护**：`(12)(34)` 与 `(1234)` 解析结果不同（naïve 去括号会塌成同一串），多环必须保留括号。**② 修 Sₙ 多环 label 畸形根因**（`src/core/groups/SymmetricGroup.ts`）：旧 `permToString` 一律 `.replace(/^\(/,'').replace(/\)$/,'')` 只剥最外层一对括号 → `(12)(34)` → `12)(34`（无效记号）；改「单环去括号保持紧凑（既有约定 `234`/`12`）、多环保留括号（`(12)(34)`）」——**既有 Sₙ 单环 label 不变（5 处既有测试零改动）**，畸形输出消除。**③ 诊断过程中的真 bug**：首版 `parseCycleNotation` 用 `/\d+/g` 切点，把 `234` 当成数字 234（`[234]`）而非 2,3,4 → 解析恒为 null；改「无分隔符的连续数字串按单点逐位拆、有分隔符时 token 即点（支持多位数点 `10 12 3`）」。**文档**：`docs/API.md` §1 补第四档 + `parseCycleNotation` 行并明示「label 约定因群而异、手写标准循环记号即可」；`resolveElementWarn` 告警文案补「循环记号」；`elementRef.ts` 模块注释修正此前**过度概括**的「置换群为 `(234)`」。**验证**：`tsc -b` 干净 + `npm run lint` 0 problems + 全量 **81 文件 1739 tests 全绿**（elementRef 新增 15：`parseCycleNotation` 单测 6 + 语义档跨群 7 + label 自洽 2）。**反向兼容**：纯附加档位 + 只修畸形输出，**pkgVersion 2.1.0 → 2.1.1**（`version`/`package-lock`/`welcome.version` zh+en 同步）。 |


| 2026-09-10 | **npm 包页 README 链接修复：仓库相对链接 → 绝对 URL（v2.1.2）**：博客作者反馈 npm 包主页里「完整 props / API 表：见包内 API.md」的链接指向 `https://github.com/rrCathy/GroupViz/blob/HEAD/API.md` **404**，实际应在 `docs/API.md`。**根因**：`scripts/pkg/finalize-pkg.mjs` 生成的包 README 用了**仓库相对链接** `[API.md](./API.md)`，而 npm 注册表页面会把包 README 里的相对链接按**仓库根**解析成 `blob/HEAD/<path>` —— 但 `docs/API.md` 不在仓库根，故必然 404（该现象与包内是否真的分发 API.md 无关：`finalize-pkg` 确实把它 `cpSync` 进包，但 npm 网页链接走的是仓库而非 tarball）。**① 模板改为生成绝对 URL**：新增 `doc(path,label)`/`dir(path,label)` 辅助（`blob`=文件 / `tree`=目录，均以 `main` 为基线），core+react 两包 README 的 API.md / CHANGELOG / docs/GROUPS·CAYLEY·VIEWS·PRESENTATION 引用与 `examples/host-minimal/` 全部改为绝对链接；**顺带把原本只是纯文本、点不进去的 `docs/*.md` 路径全部变成可点链接**（同一类问题）。**② 新增永久门禁**（`scripts/pkg/publish-smoke.mjs` §0.6「包 README 链接完整性」，fail-closed）：(a) 包 README 内**不得出现非绝对链接**（http/https 之外一律拒绝）；(b) 指向本仓库 `blob|tree/main` 的链接其**仓内路径必须真实存在**。负向验证：旧写法 `./API.md` 被判定「非绝对」、错路径 `/blob/main/API.md` 被判定「路径不存在」，现行 README 通过。**③ 两包 package.json 补 `homepage` + `bugs`**（npm 页面侧栏链接）。**验证**：`npm run lint` 0、`tsc -b` 干净、全量 81 文件 1739 tests 全绿、`build:pkg` ✅ v2.1.2、`publish:smoke` ✅（含新门禁「包 README 链接完整」）。**pkgVersion 2.1.1 → 2.1.2**（`version`/`package-lock`/`welcome.version` zh+en 同步）。 |
| 2026-09-10 | **对称性视图元素旋转轴修复（BUGREPORT 2.1.2）：hash(id) 选轴 → 置换几何反解**：`computeElementRotation` 旧实现用 `elementHash(id) % n` 在候选轴表里挑轴，而 A₄/S₄/A₅ 所有置换的 id 长度与字符集相同（`h=31h+c`，`31≡-1 mod 4`），任意排列余数恒等 → 实测 **A₄ 的 8 个三循环全部返回同一 axis、同一 +120°**（S₄ 三循环/对换/双对换同样大面积撞桶；S₄ 四循环侥幸全对）。react `SymmetryViewScene` 又用同一套 hash 二次覆盖轴，两层同病。**① core 改为几何反解**（`src/core/elementRotation.ts`）：`R = A·P·A⁺`（A=点模型坐标 3×n，P=置换矩阵）——A₄/S₄ 点模型取**正四面体顶点（= 立方体 4 条体对角线）**，S₄ 奇置换得 det=-1 非固有正交阵，取 **-R** 即对应旋转；A₅ ≅ 正二十面体旋转群，以 (12345)↦72° 顶点轴旋转、(12)(34)↦满足 `ord(gh)=3` 的 180° 棱轴旋转（⟨g,h⟩ 阶 60）建同构，再按生成元左乘 BFS 覆盖 60 个偶置换。轴角从矩阵读取（**180° 单独走特征向量分支**——skew 部分为零，否则 2-2/对换全塌到 [0,1,0]）；轴规范化为首非零分量 ≥ 0（(abc)/(acb) 因此同轴反向）。**② S₃ 三循环方向**也顺手改由置换定符号（原 hash 奇偶）；**③ 删除反馈单 §6 多出的第 7 根 A₅ 五阶候选轴**。**④ react 去掉二次选轴**：`computeGeometricRotation` 直返 core 结果，删除 `getElementRotationKind`/`getGeometryAxes`/`AXIS_CACHE`/`cacheKeyForData` → **选轴逻辑单一真源**。**验证**：A₄ 三循环 4 轴（逆元对同轴 ±120°）、A₄ 2-2 3 轴；S₄ 4c=3 / 3c=4 / 2-2=3 / 2c=6；A₅ 5c=6 / 3c=10 / 2-2=15（全部对齐反馈单「正确值」）；新增 7 项回归测试（含 A₄/S₄/A₅ **群同态 R(gh)=R(g)R(h) 全对验证** + 不动点→顶点轴 + 单位轴）；`tsc -b` 干净、`npm run lint` 0、全量 **81 文件 1746 tests 全绿**。**语义**：轴完全由置换决定、与元素命名解耦（A₄ `(234)` 轴 = 不动点 1 所在顶点轴，与博客「红轴穿过黄球顶点」的说明一致）。 |

| 2026-09-10 | **法则型性质测试套件（预防「不崩溃但语义错」的数学映射缺陷）+ 当场揪出 4 类同类缺陷**：动机 = 上一行 rotation-axis bug「类型正确、不崩溃、角度对、**阶也对**」，示例测试（只验形状）与 code review 都拦不住——`hash(id)%n` 读起来像合理的确定性回退。结论：**必须把数学事实编码进断言，而非提高审查强度**。**① 新增法则 harness**（`src/__tests__/helpers/rotationLaws.ts`，非 `.test.ts` 故不被收集）：11 条不依赖已知答案表的 oracle —— L1 已定义 / L2 单位轴 / L3 轴不动 `R·axis=axis` / L4 角度范围 / L5 固有旋转（det=1、正交）/ L6 迹-角一致 / **L7 群同态 `R(gh)=R(g)R(h)`（全元素对穷举，有限群比随机采样强）** / L8 逆元 `R(g⁻¹)=R(g)⁻¹` / L9 `R^ord=I` / L10 旋转阶=元素阶 / **L11 单射（不同元素 ↦ 不同旋转，撞桶检测）**；返回 `Violation[]` + `formatViolations` 可读报告（按法则归并、每类最多列 3 条）；`rotationFn` 可注入以支持负向对照。**② 新增 30 项测试**（`elementRotationLaws.test.ts`）：全群 zoo（C₂₋₁₂、D₃₋₈、S₃、V₄、A₄、S₄、A₅、C₂²/C₂×C₂）× 11 法则 + 数学常数 oracle（A₄ 4/3、S₄ 3/4/3/6、A₅ 6/10/15 共 31、V₄ 三正交轴、Dₙ 反射轴须落在所画正 n 边形镜线 `−π/2+kπ/n` 上、Cₙ 像的势 = n）+ **负向对照**：把旧 `hash(id)%4` 实现注入 harness → **L7/L8/L11 同时检出**，而它**通过 L9/L10**（证明只加阶检查抓不到该类 bug）；A₄ 元素 id 全为 '1,2,3,4' 的排列、`31≡−1(mod 4)` → 余数恒为 70 mod 4 = 2，精确复现「8 个三循环全撞同轴」。**③ harness 当场揪出 4 类同类缺陷并修复**（`src/core/elementRotation.ts`）：(a) **Dₙ 反射轴公式错**——原 `refAngle = kπ/n`，正确 `−π/2 − kπ/n`（由 `ρ(r^k s) = R(β₀ − kπ/n)` 解得 `β₀ = −π/2`，才使轴落在**所画**正 n 边形镜线上；n 偶数时镜线**集合**恰好与 `kπ/n` 重合故肉眼难辨，**n 奇数（D₃/D₅）连集合都错**，且标号在所有 n 都破坏同态）；(b) **S₃ 三循环符号反了**——用群自身 multiply 探针定出 `r = t·s = (13)(23) = (132)`，正确为 `θ = (i − (v[i]−1))·2π/3` → (132) +120° / (123) −120°，原 `id.charCodeAt(0) % 2` 恰好取反（S₃ 的 3 根反射轴原本已正确：`2πf/3 − π/2` 与修正公式仅差 π、同一条线）；(c) **V₄ 整个硬编码分支是死代码**——表用置换 pattern `'1,3,2,4'` 等，而 `createKleinFour()` 元素值是 `[i]` 永不匹配 → 掉进「按序号均分 360°」兜底给出 90°/180°/270°（V₄ 三个非单位元**全是 2 阶**）；(d) **C₂² / C₂×C₂ 同病**——`getSymmetryType` 判 'rectangle' 会进对称视图，但符号以 'C' 开头先撞 `startsWith('C')` 分支 → `count = val[0] ∈ {0,1}` → 4 阶群只映出 2 个旋转（`(0,0)/(0,1)` 都映 0°、`(1,0)/(1,1)` 都映 90°）。**修法**：V₄/C₂² 改为**运行时由 `group.multiply` 构造**轴分配（首个非单位元 `x ↦ X`、次个 `y ↦ Y`、`x·y ↦ Z`，不依赖元素命名/值编码）；Dₙ 与 S₃ 各改一行约定；顺带把轴标签角规范化到 `[0,180)`（直线语义，±v 同一根）。改完 `elementRotation.ts` 不再含任何 hash / pattern 表。**验证**：`tsc -b` 干净、`npm run lint` 0 problems、全量 **82 文件 1776 tests 全绿**（node 61/1592 + dom 21/184；本批次 +2 文件 +30 tests）。**文档**：`docs/TESTING.md` §3.1 补两条并把计数校准到实测值（原志 81/1724 → 实测 82/1776，node 表格内 elementRotation.test.ts 7→14）、`docs/VIEWS.md` §6 新增「各群族约定」表、`AGENTS.md` 计数同步。**pkgVersion 2.1.2 → 2.1.3**（连同上一行 rotation-axis 修复一并出包；`version`/`package-lock`/`welcome.version` zh+en 同步；`computeElementRotation` 属公开导出面，轴输出修正属 bug 修复 → 走 patch）。 |
| 2026-09-10 | **2D 凯莱 circular 布局半径未约束高度（v2.1.4）**：博客内嵌（FGVE `CayleyView`，容器 900×360）S₃ 正六边形上下各 1 个节点被裁到画布外——`graphRadius = min(width × 0.3, 180 + n × 10)` **两项都不引用 `height`**，宽扁容器里半径超过 `height / 2`（S₃ 实测纵向 `[-73, 433]`，画布仅 `[0, 360]`）。**修法**：新增共享纯函数 `circleLayoutRadius(width, height, n, nodeRadius, opts?)`（`core/algebra/layouts/shared.ts`，经 `forceLayout.ts` 导出）——上界同时取 `width × widthFactor`、`width/2 − nodeRadius − padding`、`height/2 − nodeRadius − padding`、`base + n × growth` 四者之 min（半径 r 时节点外沿到 r + nodeRadius，故可用空间须扣节点尺寸），容器过小则 `nodeRadius` 兜底（不退化为 0/负、不叠中心）。五个调用点统一改走它：**`CayleyView`（反馈源；FGVE 嵌入 + ViewWindow）、`FloatingViewWindow` 的 `CayleyGraphViewLocal`、`GroupCanvas`、`SylowView`（`GroupCanvas`/`SylowView` 走内部正方 viewBox，取值与原式完全一致，属防复发统一）**，**`CycleView`**（同族公式 `0.32/160/16`，ViewWindow 窗口 viewBox 为内容区尺寸同样宽扁可触发）。**验证**：`tsc -b` 干净、`npm run lint` 0 problems、全量 **82 文件 1783 tests 全绿**（node 61/1598 + dom 21/185；本批次 +7 tests：`layouts.test.ts` 新增 6 项 `circleLayoutRadius` 用例 + `CayleyView.component.test.tsx` 新增宽扁 viewBox 回归 1 项）。**文档**：`docs/TESTING.md` §3.1/§3.2 计数与两行说明同步。**发布门禁强化**：`publish-smoke.mjs` react 段新增「CayleyView 900×360 SSR 全节点不出画布」断言（`dist-pkg` 产物级，非源码级）——**负向对照已验**：旧公式半径 `min(900×0.3, 180+60)=240` 时节点纵向外沿 `[-88, 448]` 必 throw，修复后实测 `[16.0, 344.0]` 通过。**pkgVersion 2.1.3 → 2.1.4**（`version`/`package-lock`/`welcome.version` zh+en 同步；纯 bug 修复 → 走 patch）。 |
| 2026-09-10 | **feedback 机制建立（本地，不入库）**：`feedback/` 由「一句话 README.txt + 手写 issue」扶正为成体系的**引擎缺陷反馈协议**——`PROTOCOL.md`（自包含宿主侧规范：必填字段 + 严重度 + §3.1 加分项 + 命名 + 状态所有权）、`_TEMPLATE.md` 模板、`README.md`（状态枚举 + 消费端 SOP）、`INDEX.md` 台账；`.gitignore` 排除 `feedback/`；`AGENTS.md` §2 导航加行、§8 阶段 1 新增「反馈收件箱」第 0 步（非「已修复」项列本轮优先清单，按 SOP 复现 → 修 → 顶部回写状态 → 同步台账）。用途：宿主项目（如博客）用引擎踩 bug 时，由其 **Agent 按协议产出标准反馈**并搬运入库。 |
| 2026-09-10 | **修正提交信息规范（文档与事实对齐）**：`AGENTS.md` §8 阶段 4 与 `CONTRIBUTING.md` §「Workflow」第 7 步、「Standard flow」第 4 步此前均写 `vN.N: <主题>`，但仓库自 v2.1.x 起实际已转为 **Conventional Commits**（近 7 次提交为 `feat(pkg):` / `fix(core):` / `fix(canvas):` / `chore(pkg):` / `chore(feedback):` / `docs(pkg):`）。三处统一改为 `<type>(<scope>): <主题>`（type：`feat`/`fix`/`chore`/`docs`/`test`；scope：`core`/`canvas`/`pkg`/`feedback`/`docs`，跨全局可省），并顺带把 `AGENTS.md` 底部 `*文档版本*` 由滞后的 2.1.0 同步到 **2.1.4**。纯文档改动，不涉代码。 |
| 2026-09-11 | **视图控制层（VCL）批次一：2D 凯莱图四件个性化控件（边曲率 / 逐生成元边长 / 路径高亮 / 动态力导向）**：定位 = 在「计算层 + 视图渲染层」之上叠一层**视图控制层**，把 FGVE 从「展示群的各种视图」推到「方便展示、方便教学的插图」。规划见新增 `docs/PLAN_VIEW_CONTROL_LAYER.md`（三层架构 + ViewParams/Decorations/ControlUI/FigurePreset 四子系统 + §8 发散清单 Tier A/B/C）。**① 三个核心纯函数模块**（零 UI 依赖，进 `forceLayout.ts` 桶）：`core/algebra/edgeLengthRelax.ts`（`relaxEdgeLengths` 逐生成元边长**通用后处理**——取各生成元边在基础布局中的平均长 × scale 为目标，边弹簧 + 弱锚定回基础位置 + 近邻斥力，**一套机制覆盖全部形状**；`isIdentityScale` 守卫，倍率全 1 时**原样返回基础布局、逐位不变**）、`core/algebra/cayleyPath.ts`（`resolveCayleyPath` 路径解析：`elements` 元素引用序列 / `word` 生成元单词（从 `start`，缺省单位元，连续作用）→ 顶点序列 + 逐步连边；**方向敏感**——去掉初版的「无向兜底」，非自逆生成元只在其真实方向连通，`e0→e3` 这类反着走的相邻对只高亮节点不画连段；未解析引用记入 `unresolved` 不抛错）、`core/algebra/cayleyForce.ts`（`createCayleyForceSim` 增量式力模拟器——`step()` 逐帧推进、`pin/unpin` 拖拽钉住 + 重新升温、`reheat`；力模型 FR 风格 斥力 `repK/d²` + 弹簧 `(d−rest)×k` + 向心重力 + 速度阻尼 + 边界钳制；**逐生成元弹簧静止长度** `rest = baseLen × linkScale × action.lengthScale`；含重合点伪随机方向注入、热度 `alpha` 退火至 0.005 判定静止）。**② 类型协议**：`CayleyAction.lengthScale?`（1=原始布局）、`CayleyViewParams` 增 `edgeCurvature?`（0–3，**0 = 笔直**）/ `pathHighlight?` / `forceDirected?` / `force?` + 新增 `CayleyPathHighlight`、`CayleyForceParams` 接口与对应 zod schema（含 `cayleyPathHighlightSchema`/`cayleyForceParamsSchema`）；`normalizeCayleyActions` 透传 `lengthScale`。**③ 渲染层 `CayleyView`**：曲率由硬编码 `Math.min(dist*0.08,18)` 改为 `curvatureMul` 驱动，**平行边（同节点对多作用边）按 actionElementId 稳定排序左右分摊**（笔直模式下也不重叠）；位置链 = 基础几何布局 → `relaxEdgeLengths`（有 lengthScale 时）→ 力模拟（开关开启时）；力导向用 **rAF 循环 + 位置以 state 暴露**（避免 `react-hooks/refs` 的 render 期读 ref），拖拽 = `pin` 钉住、松手 `unpin` 升温，`settleSignal` 自增重播；路径叠层 = 高亮线段 + 节点环 + 可选 ①②③ 序号徽标 + `animate` 逐步点亮（用「渲染期 key 校正 + 定时器自增」规避 `set-state-in-effect`）。**④ 控制 UI**：`FloatingViewWindow` 凯莱 ⚙ 面板新增 Edge curvature（滑杆 + Straight/Curved）、Path highlight 编辑器（`CayleyPathEditor`：Elements/Word 双模式 + 文本输入 + Animate/Order/Closed + 5 色 + Clear）、Live force-directed（开关 + Link length/Repulsion/Gravity 滑杆 + ⟳ Re-settle）、Edge actions 每行新增逐生成元 `len` 滑杆。**关键语义澄清（用户当场纠正）**：力导向**不是一种新凯莱形状**，而是「选定形状之后的一个开关」，故最终未新增 `shape2D` 枚举值，改为布尔 `forceDirected`（初版曾误加 `'force'` 形状，已回退）。**验证**：`tsc -b` 干净、`npm run lint` 全仓 0 problems（修掉了本批引入的 3 类 React Compiler 报错：render 期读 ref / set-state-in-effect ×2 / 无用依赖）、全量 **84 文件 1831 tests 全绿**（+2 文件 +50 tests：`vclCayleyControls.test.ts` 20 项核心纯函数 + `CayleyView.component.test.tsx` 新增 8 项渲染层 + `CayleyWindowParams` 面板断言更新与新增 1 项）；`vite build` 通过（因沙箱安全删除守卫无法清空既有 `dist/`，改 `--outDir` 临时目录验证）。**零配置安全**：所有新 props 可选，未传时曲率/位置/渲染与既有完全一致。**未做（后续）**：主画布 `GroupCanvas`/`ViewPanel` 暂未接这四件控件（仅 FGVE 窗口/包 props 路径）；装饰性抽象 `Decorations`（注释）与 `FigurePreset` 见规划文档 Phase 2/3。 |
| 2026-09-12 | **字长球球壳 + 参考实现细节登记入 VCL**：**① 球壳**（用户反馈「看起来有些畸形，可以套一个球壳上去」）——`Cayley3DScene` 的 `wordLengthSphere` 分支新增半透明球壳：半径 = 最外层节点所在球面（S₅ 有节点贴壳 ⇒ 恰为布局半径 R，S₄ 全体贴壳），材质 `meshStandardMaterial`（受光才有球面明暗渐变——纯 `meshBasicMaterial` 无光照，整个球会退化成一块均匀色圆盘）+ `transparent opacity 0.07` + `depthWrite:false`（不遮挡球内节点/边，像玻璃球）+ `renderOrder −1` + 主题双色；**仅该布局渲染**（其它布局零影响，`shellRadius` 为 memo 派生，非 wordLengthSphere 时返回 0）。组件测试 +1（`Cayley3DView.component.test.tsx`：S₄ wordLengthSphere = 24 个节点球 + 1 个半径 7.75 的壳；cone 布局 24 球无壳）。**② VCL 参考登记**（用户：「那份代码还有很多细节可以给 VCL 做参考，记录一下」）——`docs/PLAN_VIEW_CONTROL_LAYER.md` 新增 **§10 参考实现可借鉴细节**：把参考 HTML 的交互/编排层按四类登记（10.1 面板读数：布局质量指标行「松弛前后最小边距 + 近距边对计数」、字长直方图悬停高亮该层、生成元图例点击切换显隐；10.2 悬停联动：节点信息卡字段清单模型（one-line/轮换/字长/**最短表达数**/四邻边 ±1）、悬停同步高亮图例项、边悬停标生成元与字长变化；10.3 渲染开关：**球壳/纬度环**（前者本次已实现待控件化）、**「重新优化布局」按钮**（3D 侧缺的 reheat 等价物）、重置视角；10.4 布局旋钮：`BAND_RATIO 0.32`（层松紧，值得暴露）、`SNAP_RATIO 0.9`（壳吸附）、`EDGE_CLEARANCE 1.05`；10.5 形态层：单文件自包含交付 → 强化 §3.4 `FigurePreset` 的「可分享/可嵌入」目标）+ §9 实施状态补球壳行（标注尚未控件化 → 待补 `Cayley3DViewParams.shell`）；`docs/CAYLEY.md` 形状表 wordLengthSphere 行「渲染配套」补球壳。门禁：`tsc -b` 干净 · `npm run lint` 0 problems · 全量 **85 文件 1843 tests 全绿** · `vite build` 通过。 |
| 2026-09-12 | **S₅ 字长球定版：纬度分层 + 正根胞格向量初值 + 边距松弛（前三版全部废弃）**：用户提供了一版可用的独立实现（单文件 HTML + three.js），本行记录按该算法**逐行移植**进引擎（`core/algebra/layouts3D/wordLengthSphereLayout3D.ts` 的 `sphericalLayeredLayout` 取代 `diskBallLayout`，删除 BALL_* 参数与旧对称投影）。**算法**：① **纬度层**：y_l = R·cos(πl/10)（e/w₀ 钉在两极），第 l 层球截面半径 r_l = R·sin(πl/10)；小层（n ≤ 6，即层 1/9 的 4 点）直接贴球面圆环；大层按投影模长排名取 r_l·√((rank+0.5)/n) 铺满圆盘（盘心→盘缘都有点，实心球而非壳）。② **正根胞格向量初值（本布局核心）**：把 A₄ 的 10 个正根手工投影到 2D（`CELL_VECTORS`）——四个单根 (1 2)(2 3)(3 4)(4 5) 恰落在 +x/+y/−x/−y 四个方向（**四个生成元 = 四个方位瓣**），其余六个正根落在中间方位且模长更小；每个置换的投影 = 其逆序集上这些向量的和 ⇒ 父子（相差一次相邻对换）投影方向天然相邻，边近似沿经线自上而下流动。③ **边距松弛** 320 轮：同层节点斥力（作用半径 ≈ 1.9·r_l/√n）+ **边-边斥力**（AABB 预筛 + 每条边 3 采样点的线段-线段最近距离 segSegDist Bradley 钳制算法）+ 角度弱锚定（拉回初值方位，保持四瓣/偶极场）+ y 弹簧；硬约束 y ∈ 纬度带 ±0.32×层间距、水平半径 ≤ min(r_l, √(R²−y²))；收尾把 r > 0.9·r_l 的节点**精确吸附到球面**。**非平面性说明**：S₅ 相邻对换凯莱图非平面（二部图要求 F ≤ E/2 = 120 < 欧拉公式要求的 F = V−E+2 = 122），固定纬度面 + 直线边必有极个别交叠，松弛负责把它们在 3D 中错开。**实测**（R=15）：布局耗时 107 ms（确定性无随机）、|p| ≤ R 且外圈精确贴球面（37 点）、边长度 p50 = 5.9 / max = 9.9（< 1.05R，无穿球长弦）、边竖直度 p50 = 0.67、节点最小间距 2.40（节点直径 0.84）、非共端点边对最近距离 0.228（近距对 <0.55 仅 1 对）、4 个生成元方位 0°/90°/179°/271°（≈90° 一瓣）。**测试**：`wordLengthSphere.test.ts` 重写为「纬度分层 + 带内浮动（极点精确 ±R）/ 实心球（外圈贴壳 + 内部有点）/ 盘内 n·t² 铺点 / 北极四星（贴球面 · 方位 ≈90°）/ 只有层间有边 / 边倾向竖直 + 无长弦 / 边距松弛近距对 ≤3 / 布局确定性」，删除已不成立的 Klein 四元群等变断言（新算法为投影构造，不具该精确对称）；`wordLengthSphereCrossing.test.ts` 保留量级护栏。全量 85 文件 1841 tests + `tsc -b` + `eslint .` + `vite build` 全绿；浏览器实测 S₅ 3D 视图截图（spherical 分层 + 四瓣 + 球内填充）0 console error（仅后端未启动的 502，与布局无关）。文档：`docs/CAYLEY.md` 形状表 wordLengthSphere 行同步。 |
| 2026-09-12 | **S₄/S₅ 字长球形状写入引擎（core 公共面 + 应用层委托 + 发布门禁补强）**：**① 核心公共面**——新建的 `wordLengthSphereActions(group)`（`core/algebra/layouts3D/wordLengthSphereLayout3D.ts`）：**按结构判定**返回该形状的标准作用边 = 相邻对换生成集（S₄ 3 条 / S₅ 4 条，`COLOR_PALETTE` 顺序配色），非「n ∈ {4,5} 的 one-line 置换群」或相邻对换不齐备一律 `null`（与 `wordLengthSphereLayout3D` 可用性一致）；`core/index.ts` 新增 `export * from './algebra/layouts3D/wordLengthSphereLayout3D'` ⇒ `wordLengthSphereLayout3D` / `wordLengthSphereActions` / `wordLengthOf` / `wordLengthColor` / `findAdjacentTranspositionGenerators` 全部进 `@groupviz/core` 公共面（此前只有布局函数经 `layout3D` 间接可达，其余为内部细节）。**② 顺带修掉一个已发出的包缺陷（潜伏）**：`Cayley3DScene` 深导入 `wordLengthColor`，而 pkg 构建把**所有** core 相对导入改写成裸包名 `@groupviz/core`——core 门面未导出该符号 ⇒ 重打包后 `@groupviz/react` 会 `import { wordLengthColor } from '@groupviz/core'` 而消费端 ESM **链接期直接报 "does not provide an export named"**（整包不可用，非运行期小错）。修法即 ① 的门面导出；实测 `build:pkg` 后 react 产物 `import { …, wordLengthColor as le } from "@groupviz/core"` 与 core 产物 `ac as wordLengthColor / Qs as wordLengthSphereActions / ic as wordLengthOf / rc as wordLengthSphereLayout3D / Zs as findAdjacentTranspositionGenerators` 一一对上（publish-smoke §0.5「react→core 具名导出一致」门禁，core 共 307 导出）。**③ 应用层委托单一真源**（消灭三处硬编码元素 id）：`context/cayleyActions.ts` 的 `getSpecialCayleyActions`（wordLengthSphere 早退 → 核心函数）与 `addAllCayleyActionsHelper` canonical3D 集合、`ViewPanel.canonical3DEdgeIds`（动作列表「按元素」提示用）全部改调核心实现；**其他形状（S₄ 多面体 / A₅ 二十面体）的硬编码清单保持原样不动**（本次只收口新形状）。**④ 测试**：`wordLengthSphere.test.ts` 新增 describe「wordLengthSphereActions（引擎公共面）」6 项——S₄ 3 条/S₅ 4 条相邻对换（id 序 + 调色板配色 + enabled）、**每条作用边把字长改变 ±1**（本形状「只有层间有边」的结构根据，480 次群乘校验）、结构不符/超出支持（C₄ / S₃ / S₆）返回 `null`、应用层委托与核心**逐位一致**、**core 门面导出可达**（动态 `import('../core/index')` 身份比对，等效消费端解析）。**⑤ 发布门禁补强**：`scripts/pkg/publish-smoke.mjs` 的 core 冒烟新增字长球段——`wordLengthSphereActions(S₅)` 恰 4 条且序为 `2,1,3,4,5｜1,3,2,4,5｜1,2,4,3,5｜1,2,3,5,4`、`C_{4}` 返回 null、`wordLengthSphereLayout3D(S₅, 5)` 120 点且**无一节点出球**（|p| ≤ R=15）、`wordLengthOf(单位元)=0`、`wordLengthColor` 返回颜色串；`docs/API.md` §4.9 加「字长球必须显式传 actions」提示 + §7 增**字长球形状导出一览表**与可复制消费示例（core + react 双包用法）；`docs/CAYLEY.md` 形状表已含该形状；`AGENTS.md` §5 新增字长球条目（含引擎导出与「硬编码清零」说明）、§2 导航补 VCL 规划文档行、测试计数全量对齐 85 文件 1849 tests（node 64/1654 + dom 21/195）；`docs/TESTING.md` §3.1/§3.2 表头与 17 行逐文件计数按实测刷新 + 补 11 个缺失行（wordLengthSphere / wordLengthSphereCrossing / vclCayleyControls / notationParser / combinatorics / planarity / detectIsomorphicGroup / importGroup / core/cosetStrip / SymmetryWindowParams / CosetStripWindowParams）。门禁：`tsc -b` 干净 · `npm run lint` 0 problems · 全量 **85 文件 1849 tests 全绿** · `vite build` 通过 · `build:pkg`（core 598.81 kB / react 258.35 kB，v2.1.4 产物）· `publish:smoke` **全绿**（真实 npm 安装 + core/react SSR/类型双 resolution + 新增字长球断言）。版本号暂留 2.1.4（按发布流程在推送阶段统一 bump）。 |
| 2026-09-12 | **VCL 扩容到 3D 凯莱图：逐生成元边长 + 路径高亮**（用户点名：「3D凯莱图也要逐生成元边长，路径高亮，先做吧」）。**① core 新模块 `core/algebra/edgeLengthRelax3D.ts`**：与 2D 版同一套力模型（各作用元素平均基础边长 × 倍率 → 弹簧 + 弱锚定 + 近邻斥力），差异有二——位置为三维 `Vec3`（三维欧氏距离）、**斥力按布局尺度自适应**（3D 各布局世界尺度差异大：cone 半径 5 / wordLengthSphere R=15 / 小群 1–5，2D 的固定常数会失效；改以基础布局平均边长 `ref` 为参考尺度：f = repulsion·ref²/d²、作用半径 0.25·ref、默认 repulsion=0.0025 与 2D 同量级，只在节点快叠死时生效）；无边界钳制（3D 无视口可钳，形状靠锚定保持）；全 1 时返回同一引用（零配置安全）。**定标过程**：初版取「作用半径 3·ref」，正常间距节点也在互推 → 缩小倍率几乎不生效（C₆ 环 mean 4.96 vs 目标 2），3D 缩小用例当场抓出后按 2D 同比例重定标。**② 类型/schema**：`Cayley3DViewParams.pathHighlight`（复用 `cayleyPathHighlightSchema` 与同款 JSDoc 语义）+ `cayley3DViewParamsSchema.actions[].lengthScale`（对齐 2D schema）。**③ 渲染 `Cayley3DScene`**：位置链 = `compute3DPositions` → `relaxEdgeLengths3D`（仅当存在非 1 倍率，`isIdentityScale` 短路）→ `Vector3`；路径叠层复用 `core.resolveCayleyPath`（与 2D 同一份解析：元素序列 / 生成元单词、方向敏感、closed、unresolved 不抛错），drei `Line` 像素宽线段 + 节点半透明球环 + `showOrder` 序号 `Html`（位置 [0,0.95,0]），动画沿用「渲染期 key 校正 + 定时器自增」规避 set-state-in-effect。**④ 控制 UI**：FGVE 窗口 ⚙ 面板 3D 分支新增 `CayleyPathEditor`（复用 2D 双模式编辑器）与逐 action `len` 滑杆（`setCayley3DActionLength` 写回 `actions[].lengthScale`）；**⑤ TestPage1 引擎消费卡片** `Cayley3DCard` 加逐生成元 lengthScale 滑杆 + word/elements 路径输入（换群清空用「渲染期 key 校正」），props 注入走 dist-pkg 产物实测（`npm run build:pkg`）。**测试**：+13 → **85 文件 1862 tests 全绿**（node 64/1660 + dom 21/202）——`vclCayleyControls` +6（z 分离计入距离、缩放拉长/缩短、小尺度布局有界、全 1 返回同一引用、不改 base）、`Cayley3DView.component` +5（lengthScale 拉长边 / =1 逐位不变 / 路径线段+节点环 / 无连边相邻对只亮节点 / 序号徽标）、`Cayley3DWindowParams` +2（len 滑杆与路径编辑器写回）+ 控件计数同步（滑杆 1→2、A₄ 面测试 2→4）。drei `Line` 进两个测试的 mock（`data-points`/`data-line-width` 可断言）。**门禁**：`publish:smoke` core 段新增 `relaxEdgeLengths3D` 断言（全 1 原样返回 + 2× 拉长边长）、类型冒烟新增 `Cayley3DSceneProps` 的 lengthScale/pathHighlight 构造。**验证**：`tsc -b` 干净 · `npm run lint` 0 problems · `vite build` 通过 · `publish:smoke` CORE/REACT 双 PASS · 浏览器实测 `/?test=1`（C₄ 单生成元 2.5× 明显拉长；D₄ 双生成元 2.2×/0.5× 差异化成图 + word 路径金线/节点环/序号，0 console error）。文档：`API.md` §4.9 表 + core 门面表（`relaxEdgeLengths3D`）、`AGENTS.md` §5 新增 VCL 条目与测试计数、`TESTING.md` §3.1/§3.2、`PLAN_VIEW_CONTROL_LAYER.md` §9。 |
| 2026-09-12 | **TestPage1 四个 VCL 一键示例 + 卡片参数按群动态化**（用户：「没看到啊，你在 testpage1 里放这几个例子：S₃ 的 2D 凯莱图边弯曲/笔直；A₄ 的截角四面体让 (234) 变短、(12)(34) 变长；S₄ 的字长球高亮哈密顿路径；D₇ 的 2D 凯莱图设置动态力导向」——上一轮只在 FGVE 窗口面板接了 VCL，TestPage1 的卡片默认值中性、看不出效果）。**① 页头「VCL 示例」按钮组**（`data-testid=pkg-vcl-demos`，4 键）：每键 = 切群 + 以示例参数**重挂载**对应卡片（`key=nonce`，避免渲染期 setState：卡片用 `useState` 初值直接落预设，用户随后可继续手调）——S₃ → 2D 曲率 0（笔直）；A₄ → 3D 截角四面体 + 逐生成元倍率 `[2.2, 0.4]`（按生成元序 = a=(12)(34) 拉长 / b=(234) 缩短）；S₄ → 3D 字长球 + 哈密顿路径；D₇ → 2D 双环 + 力导向开。**② 2D `CayleyCard` 补 VCL 控件**：`edgeCurvature` 滑杆 + Straight/Curved 快捷按钮、`forceDirected` 开关（展开 link/rep 滑杆）、`shape2D` 下拉由写死 7 项改为 `getAvailableShapesForView(group,'cayley')` **按群动态枚举**（S₃ → circular/projection3D、D₇ → circular/dualRing/cone）。**③ 3D `Cayley3DCard` 补全**：`layout3D` 下拉由写死 6 项改为 `getAvailableShapes3D(group)` 按群动态（A₄ 才有 truncatedTetrahedron、S₄ 才有 wordLengthSphere）；**字长球布局自动改用 `wordLengthSphereActions(group)` 相邻对换集作 actions**（该布局的前提，否则字长分层不成立）；`lengthScale` 滑杆行改由当前 actions 派生（字长球 = 3 条相邻对换、其它 = 群生成元）；新增 **Hamiltonian 按钮**（显示步数，本地 DFS 求覆盖全群的一条 walk 并填入 word 路径）；容器 280 → 320。**④ 新工具 `findHamiltonianWord(group, gens)`**：Warnsdorff 启发式（优先访问后继最少的邻居）+ 回溯，步数上限 400k；S₄（24 顶点 3-正则）毫秒级命中 23 步。**⑤ `GROUP_OPTIONS` 加 D₇**（原列表缺，用户点名）。**验证**：四例逐个浏览器实测（S₃ 直线边 + straight 徽标；A₄ truncatedTetrahedron + (12)(34) 2.2×/(234) 0.4× 红青边长差异；S₄ wordLengthSphere + 23 步哈密顿 word + 金色路径/序号；D₇ dualRing + forceDirected 收敛成有机网络）· 页面 runtime error 面板 ✓ 0 条 · `tsc -b` 干净 · `npm run lint` 0 problems（修掉一处 lenScales 条件字面量导致的 exhaustive-deps warning，改模块级 `EMPTY_SCALES` 常量）· 全量 85 文件 1862 tests 全绿 · `vite build` 通过 · `npm run build:pkg` 重出产物。 |
| 2026-09-12 | **VCL 体验修复：力导向拖拽 1:1 跟手 + 整体变硬；路径高亮"一眼可见"**（用户反馈两串问题：① 力导向「节点不跟手；节点之间太软，稍微拖动一下节点形状就大变样」② 路径高亮「你估计求出路径了，但视图里没显示出来；金色线完全看不出来因为边还有自己的生成元颜色；序号只能看见 1」）。**① 力导向跟手的真 bug**：`CayleyView` 拖拽处理器把 `getNodePos(el.id)` 的返回值当拖拽起点直接持有 —— 而它返回的是 `sim.positions` **同一对象引用**，`sim.pin()` 会**原地改写**该对象 ⇒ 每次 mousemove 都基于"上一次结果"再叠加位移，实测拖拽位移呈**三角数累积**（鼠标 120px → 节点 300px，2.5×；更早一次 4×，节点被甩出画布）。修法：mousedown 时快照 `{ x, y }`；并在 mousemove 里 `setForcePositions(new Map(sim.positions))` 立即同步（不等 rAF 的下一帧 step）。修复后实测**逐像素 1:1**（30/60/90/120/150px 鼠标位移 → 节点 30/60/90/120/150px）。**② "太软"三处调硬度**：`kSpring` 0.06 → **0.28**（初版拖一个节点会把整张图拽变形走样）；**斥力截断** 3.5×idealDist（FR 的全局斥力让任意两点耦合，是"软"的另一半根因）；**拖拽中刚度 ×3**（`DRAG_STIFF`，拖动的节点带一小片、其余保持形状，松手恢复）；`maxSpeed` 1+12α → 0.6+6α（不"甩"）。新增 `CayleyForceParams.stiffness`（0.4–3，缺省 1）+ FGVE 面板 **Rigidity** 滑杆 + TestPage1 2D 卡片 `rigid` 滑杆。**③ 路径高亮"看不见"的真 bug**：TestPage1 的哈密顿 word 用了**元素 id**（Sₙ 的 id 是 `2,1,3,4` 这种**含逗号**形式），而单词输入框按 `/[\s,]+/` 分词 ⇒ 被拆成单字符、`resolveCayleyPath` 解析出 **0 条边**（调试输出 `0/-1` 定位）——"金线"其实是生成元 34 的黄色边。修法：`findHamiltonianWord` 返回**生成元 label**（'12 23 12 …'，无逗号且可读）；TestPage 路径改用**白色 + 宽度 6**（避开生成元调色板）。**④ 视觉语义按用户建议重做**：新增 `pathHighlight.dimOthers`（**缺省 true**）——路径高亮时**其余边淡化**（2D `strokeOpacity 0.12` + 细线 1.6；3D `opacity 0.12/0.2` + 细管），"只显示路径上的边"；**序号改 hover 显示**（`showOrder` 不再常显——长路径上徽标互相遮挡、远处看不清，用户只能看见"1"）：2D 用 `hoveredElementId`、3D 用内部 hover 或新增的**受控 `hoveredElementId` prop**（与 2D 对称，供图例/侧栏联动）。**⑤ 顺带修渲染时序缺陷**：3D 边材质的 `transparent` 切换需要显式 `material.needsUpdate = true`（否则"淡化只在材质首次创建时生效、之后改路径无效"）。**测试**：+1 → **85 文件 1866 tests 全绿**（node 64/1661 + dom 21/205）——`vclCayleyControls` +1（`pin` **原地改写** positions 对象的语义固化：拖拽起点必须快照）、`CayleyView.component` +1（dimOthers 非路径边 0.12 / 路径边 0.9）+ showOrder 测试改 hover 语义、`Cayley3DView.component` +1（受控 hover 驱动序号徽标）+ dimOthers 材质 opacity 断言 + needsUpdate 相关。**验证**：`tsc -b` 干净 · `npm run lint` 0 problems · `vite build` 通过 · `publish:smoke` CORE/REACT 双 PASS · 浏览器逐项实测（S₄ 字长球哈密顿路径：白色粗线 + 其余边几乎不可见，改路径后淡化保持；hover 路径节点显示序号（扫描命中 "19"）；D₇ 力导向拖拽 1:1 跟手、相邻节点渐进响应）。 |
| 2026-09-12 | **力导向交互第二轮修复（对齐 Obsidian 图谱手感）：点击不再坍缩 + 最小间距约束防纠缠**（用户：「我打开力导向之后，稍微点击了一下元素，形状就大变样了。节点跟手是跟手了，但我一点击节点，图立马就坍缩了，许多节点都纠缠在一起。我想要 obsidian 里的交互式图谱那种效果」）。**① "点击即坍缩"根因**：`CayleyView` 的 `onMouseDown` 在力导向下**无条件** `sim.pin()` + `alpha = max(alpha, 0.5)` + wake —— 等于每次点击都把整张图重新加热重排（且 `pin` 期间 stiff ×3 让图整体收紧）。修法：**4px 拖拽阈值**（`FORCE_DRAG_THRESHOLD`）——指针位移未越过阈值前不 pin、不升温；松开时若从未越界则不做 unpin。实测**点击前后 avgR 132 → 132（完全不变）**。**② 热度大幅降低**：`DRAG_ALPHA` 0.5 → **0.22**、`RELEASE_ALPHA` 0.3 → **0.1**（拖拽只让邻居轻微让位、松手缓慢回稳，不再触发全局重排）。**③ 平衡尺度校准**：`gravK` 0.012 → **0.006**（向心重力减半，力平衡半径贴近初始布局尺度，不再"缩成一团"；D₇ 实测收敛态 avgR 129–137 对初始双环 ~198 的收缩率显著降低）。**④ 新增最小间距硬约束**（`CayleyForceOptions.minSeparation`，缺省 `idealDist × 0.85` ≈ 节点直径量级）：每帧积分后对过近节点沿连线直接推开（钉住的节点不动、由对方让位；两边都可动时各让一半），解决"低热度下纯力场推不开 → 视觉纠缠"（修复前三次拖拽后 minGap 35 < 节点直径 56，修复后 **58**）。**⑤ 边界与同步收口**：`pad` 钳制与 `positions` 同步从积分循环移到每帧末尾统一执行（钉住节点同样受画布边界约束）。**测试**：+1 → **85 文件 1867 tests 全绿**（node 64/1662 + dom 21/205）——新增"把节点拖到另一节点上，硬约束把自由节点推开"回归；`tsc -b` 干净 · `npm run lint` 0 problems · `vite build` 通过 · `publish:smoke` 双 PASS · 浏览器实测（点击布局不动 / 拖拽逐像素 1:1 / 三次拖拽后节点无重叠、整体铺开）。 |
| 2026-09-12 | **力导向第三轮（真正的 Obsidian 局部性）：位移按 alpha 缩放 + 拖拽低热度 + 松手不回弹**（用户：「离 obsidian 差远了……我拖动一个节点，直接牵一发而动全身，形状都大变样了」）。**关键根因**：力模型里**力从不乘热度**——`alpha` 只限制速度上限（`maxSpeed`）与循环寿命，弹簧/斥力/重力的位移每帧**满量**施加 ⇒ 拖一个节点会沿弹簧链把位移传遍整张连通图（之前两轮调的都是"力的大小/刚度"，所以怎么调都挡不住传播）。**修法**：积分改为 **`x += vx × alpha`**（Fruchterman-Reingold 标准做法）——热度低则力几乎传不动；配合 `DRAG_ALPHA` 0.22 → **0.02**（拖拽只维持"碰撞推开"级别的响应）、`unpin` **不再升温**（松手不重排；要重排用 `reheat()`，即面板「⟳ Re-settle」按钮）、**删除拖拽刚度提升**（`DRAG_STIFF` 移除——刚度越高位移沿链传得越远，是反效果）。最小间距约束不受 alpha 影响，拖拽经过别的节点时仍会把它推开（碰撞响应保留）。**实测（D₇ 力导向，拖拽 150px）**：被拖节点 1:1 跟随；**其余 12 个节点位移全部为 (0, 0)**（修复前：邻居 (26,100)、远处节点 (-26,-112)）；松手 1.5s 后位置与拖拽结束时完全一致（**不回弹、不全局重排**）；拖拽路径撞到的节点被推开 57px。**测试** +1 → **85 文件 1868 tests 全绿**（node 64/1663 + dom 21/205）：新增「拖 e0 到圆心 → 其余节点位移 < 12；松手后 e0 停在原地」把局部性锁死。`tsc -b` 干净 · `npm run lint` 0 problems · `vite build` 通过 · `publish:smoke` 双 PASS。 |
| 2026-09-12 | **力导向第四轮（去僵硬 + 滑杆降敏）：参数就地更新 `setOptions` + 拖拽粘性跟随标定**（用户：「节点拖动后动态效果僵硬，对 link/rep/rigid 数值又过于敏感（只是稍微调调，形状就大变样）」）。**① 僵硬根因**：上一轮把拖拽热度压到 0.02 又"松手不升温"，邻居完全不动 ⇒ 死板。**标定**：`DRAG_ALPHA` 0.02 → **0.2**（1 跳邻居约 20-30% 粘性跟随）、`RELEASE_ALPHA` **0.25**（松手有轻微弹性收尾）。实测（D₇ 拖 150px）：被拖节点 1:1（202）；1 跳邻居 44px（28%）；其余 12 个节点 9–22px（**2–7%**，仍不"牵一发而动全身"）；松手后仅 +5–9px 回稳。**② 滑杆敏感根因**：`CayleyView` 的模拟器 effect 依赖 `force.*` ⇒ **每动一下滑杆都重建 sim + `initialAlpha: 1` 全局重新收敛**（这才是"稍微调调形状就大变样"）。修法：sim 新增 **`setOptions(partial)`**（就地改 `repulsion/linkScale/gravity/damping/stiffness`，重算派生量 `kSpringBase/repK/gravK/damping/每边 rest`，**保留位置与速度**，仅温和升温 `SET_OPTIONS_ALPHA = 0.55` 让过渡走完）；`CayleyView` 拆成两个 effect——**结构依赖**（群/作用边/方向/视口）才重建，**参数依赖**走 `setOptions` + wake。实测：link 滑杆 1.0→1.3→2.0→1.0 每次都产生 **20–31px 的平滑调整**（有界、可感、连续），不再整图重排。**③ 测试** +1 → **85 文件 1869 tests 全绿**（node 64/1664 + dom 21/205）：新增「`setOptions` 就地更新（位置保留、同帧无跳变、轻微升温）」；拖拽局部性阈值按新热度重标（邻居 < 20、松手回稳 < 25）。`tsc -b` 干净 · `npm run lint` 0 problems · `vite build` 通过 · `publish:smoke` 双 PASS。 |
| 2026-09-12 | **力导向第五轮（积分格式对齐 d3-force/Obsidian + 均衡态标定，根治坍缩/纠缠/走样）**（用户：「目标 obsidian 交互式图谱效果；拖动后牵一发而动全身，其他节点有坍缩、纠缠倾向，整体形状走样；link/rep/rigid 不知道有什么用，甚至让形状大变样」；先核实参考：Obsidian 图谱物理 = d3-force 同源——向心力/斥力/连线力/连线距离四力 + 拖拽 alphaTarget 升温）。**① 真根因 = 积分格式错**：前几轮「力全额进速度、位移才 ×alpha」——速度场是力的泄漏积分器，跨帧持续累积（稳态速度 ≈ 5×单帧力），拖拽时位移 ≈ 无热度缩放，全图照样被拽动，松手还释放累积速度造成弹跳；第三轮「位移 ×alpha」只是把正确机制（**力 ×alpha 进速度，位置直接积分速度，速度衰减**——d3-force 标准）装反了半边。本轮 `cayleyForce.ts` 重写积分：`vx += fx×alpha` → 限速 → `x += vx`；**拖拽热 0.12 + alphaTarget 架构**（`alpha += (target−alpha)×0.96` 松弛，拖拽中恒热=长按持续松弛、松手 target 归零自然冷却，不再一次性 bump）。**② 坍缩根因 = 重力未按群阶归一**：固定 `gravK` vs 弹簧径向抵抗力 ∝ k/n²（推导：n 环收缩 δ/R = gravK·n²/11）⇒ 大群激活/重排被压塌（40 阶环 δ≈174px）。修法 **`gravK = 0.3×gravity/n²`** ⇒ 收缩恒 ≈2.7% 半径、与群阶无关。**③ 纠缠根因 = 斥力硬截断**：2.5–3.5×idealDist 间 smoothstep 淡出（硬边界注入能量→漂移抖动）；斥力基数 0.9→**0.55**（静图近平衡）。**④ 拖拽刚度增强回归**：`DRAG_STIFF_BOOST ×2.5`（pin 时 ×2.5、unpin 恢复——拖拽局部更"硬"不易走样；补上 viewConfig 一直承诺的语义），damping 缺省 0.82→**0.75**、激活热度 `initialAlpha` 缺省 1→**0.4**（CayleyView 不再传 1：静图是标定过的近平衡态，温和松弛到位不重排）、setOptions 升温 0.55→**0.35**、settleSignal reheat 1→**0.8**。**⑤ 滑杆语义补全**：FloatingViewWindow 四滑杆全加 title（Link length=连线距离 / Repulsion=排斥力(远距淡出) / Gravity=向心力(已归一) / Rigidity=连线刚度(拖拽自动加强)）；TestPage1 CayleyCard 补 **center**（gravity）滑杆 + 同款 title。**⑥ 测试重写 4 条（契约升级为物理语义）** → **85 文件 1872 tests 全绿**：快拖局部性（位移沿弹簧链单调衰减 m1>m2>m3、对面 <20px）、松手停在哪附近+结构完好（Obsidian 软体语义：无形状记忆但有边长护栏 0.7–1.4×）、长按 5s 不坍缩不纠缠（平均边长 ≥0.55×rest + 两两间距 >30）、**激活静图几乎不动（最大位移 <18px = 12% 半径）**；`tsc -b` 干净 · `npm run build:pkg` 重出产物（TestPage ?test=1 走 dist-pkg 同步生效）。 |
| 2026-09-12 | **力导向第六轮（结构性根因：装饰性静态布局非力平衡态 → Obsidian 风格一次性 settle 投影）**（用户：「没修好。牵一动全，坍缩纠缠，形状走样的问题依然存在」——上一轮虽然把积分格式修对，但用户实测仍有大漂移）。**真根因不在力模型**：写了诊断脚本直接跑 `cayleyForce` + D₇ `dualRingLayout` —— **dual ring 静态布局不是力平衡态**（r1 边长 72–132、s0 边长 68；外环半径 152、内环半径 83，差 70px）。无论怎么调参，激活时图谱都会被斥力+重力场从装饰性布局拽到"边长均匀、节点均匀"的全局平衡 —— **从 init 到 force-equilibrium 漂移 152px**。**修法：改成 Obsidian 真行为**：① **per-edge 静止长度**（每条边 rest = 自身初始长度，初始零预张力）；② **新增 `settle(steps, settleAlpha)` 方法**：一次性投影到最近力平衡态（实测 maxDrift 仅 3.9px），清零速度、`alpha=0` 冻结；③ **默认 `initialAlpha=0`**（启动冻结）；④ **`pin` 瞬时升到 DRAG_ALPHA**（不等松弛）；⑤ **`CayleyView` 改流程**：forceActive → 创建 sim + `settle()` → 冻结渲染；拖拽 = pin + wake；松手 = unpin + 自然冷却；参数变化 → `setOptions` + `settle()` 一次性重投影；settleSignal → `settle()` 重投影；⑥ **删除 forceActive 时的持续 rAF 循环**。**实测（D₇ dualRing）**：被拖 r0 80px → 1 跳 r6 跟随 75.7（95%）、r1 跟随 62.0（78%）、2 跳 r2 46.8（58%）、远端 s4 仅 8.8（11%）；松手后图谱停在新位置；settle 后漂移 3.9px（之前 152）。测试改用 settle() 投影断言；**85 文件 1872 tests 全绿**。 |
| 2026-09-12 | **力导向第七轮（塑性弹簧：拖到哪记到哪——探索新形状）**（用户：「做的没什么问题……但是有点难搞哦。我本意是给定形状后，用户可以通过动态力导向来探索新形状。但你直接固定了一些边的长度，导致用户很难探索出其他新形状了」——第六轮 per-edge rest 修好了坍缩/走样，但纯弹性弹簧 = 形状记忆锁死，用户拖不动、探索不出新构型）。**核心机制 = 塑性（plasticity）**：`PLASTIC_RATE = 0.15`，塑性窗口内每帧让每条弹簧 restBase 向当前实际长度迁移 15%（`restBase += (d/linkScale − restBase)×rate`，linkScale 仍作全局倍率作用于塑性后的形状）。**塑性窗口**（`plastic` 标志）：`pin` 开启 → **unpin 后保持**（冷却早中期继续塑性定影，避免"冷却期移动不被记忆 → 系统性弹回"——实测关窗过早会弹回 51px）→ 热度冷却到 `PLASTIC_END_ALPHA = 0.03` 自动关；**settle/setOptions 强制关**（收敛目标就是当前 rest，收敛中改写 rest 会破坏滑杆语义：调大 linkScale 再调回必须能回缩）。同时 `DRAG_ALPHA` 0.12 → **0.2**（邻域重排更快完成，慢拖弹回更小）。**⟳ Re-settle 语义 = 硬重置**：新增 `resetShape()`——位置 + rest 全部恢复 initialPositions/initialRestBase（创建时快照），即刻冻结、精确回归（maxDrift < 1px）。**弃用「resetRests + settle 投影」**：实测投影收敛不到位（50px 回归只走一半）甚至震荡（二次 settle maxDrift 46.9 → 54.2，斥力/重力均衡 ≠ 初始布局，两次投影在两个不重合的均衡间摆动）；且 settle 的 break 条件改为 alpha 自然冷却完（≤SETTLE_ALPHA，60 步只够 ~4px 小扰动，200 步兜底大形变）。CayleyView settleSignal effect 改调 `resetShape()`；`CayleyForceParams.settleSignal` 注释更新（"回到给定形状"）；FloatingViewWindow Re-settle 按钮加 title。**测试** +3 → **85 文件 1875 tests 全绿**：塑性记忆（拖 80px 保持住 + 再 settle 也拉不回去）、settle 幂等（反复 settle 漂移 < 2px = 静止不漂移）、resetShape 精确回归（< 1px）；`tsc -b` 干净 · `npm run lint` 0 problems · `npm run build:pkg` 重出产物。**用户交互模型**：给定装饰性形状 → 拖拽逐步"捏"出新构型（塑性记忆，松手停在新形状）→ 不满意点 ⟳ Re-settle 一键回原形。 |
| 2026-09-12 | **pkgVersion 2.1.4 → 2.2.0（本批次推送门禁）**：自 v2.1.4 起累积 11 个 CHANGELOG 条目的功能增量（字长球写入引擎 + VCL 批次一：2D/3D 边曲率/逐生成元边长/路径高亮/动态力导向七轮迭代），全部为新 API/新行为、无破坏性变更 ⇒ 按语义化版本走 **minor**。同步点：`package.json` `version`+`pkgVersion`、`package-lock.json` 2 处、`welcome.version` zh+en（`src/i18n/translations.ts`）。门禁：`tsc -b` 干净 · `npm run lint` 0 problems · 全量 **85 文件 1875 tests 全绿** · `build:pkg` 重出产物 · `publish:smoke` CORE/REACT 双 PASS。 |
| 2026-09-12 | **修复反馈 #3：core 门面漏导出 `types/viewConfig`（消费端类型全线报错）+ publish-smoke 补 strict 类型关卡**。**① 缺陷**（反馈 `BUGREPORT-groupviz-2.2.0-core-viewconfig-not-exported.md`）：`src/core/types.ts` 汇总层缺一行转发，`src/core/types/viewConfig.ts` 成孤儿 —— 它定义了 `CayleyViewParams`/`Cayley3DViewParams`/`CayleyActionParam`/`CayleyPathHighlight`/`CayleyForceParams`/`Cayley3DFaceFillParams`/`TableViewParams`/`TableStrategy`/`SymmetryViewParams`/`ActionViewParams`/`CosetStripViewParams`/`SublatticeViewParams`/`CycleViewParams`/`SetViewParams`/`ViewWindowConfig` 等 32 个类型与 zod schema，但既没进 `types.ts` 也没进 `index.ts` 门面（引擎内部全走深路径 `../core/types/viewConfig`，恰好躲过缺口）。后果：`@groupviz/react` 的 `.d.ts` 里有 5 处 `from '@groupviz/core'` 的 import 指向不存在的名字（`CayleyActionParam`/`CayleyPathHighlight`/`CayleyForceParams`/`Cayley3DFaceFillParams`/`TableStrategy`），消费端 `skipLibCheck:false` 时满屏 TS2305/TS2724，且无法显式标注这些类型（运行时无碍，纯类型声明层）。**② 修法**（1 行）：`src/core/types.ts` 补 `export * from './types/viewConfig'` —— 选 types.ts 而非让 `index.ts` 直连，避免与 `export * from './types'` 重复导出冲突；补前已用脚本核对 32 个导出名与现有 6 个分片（group/view/homomorphism/actions/import/groupProps）**零同名冲突**。**③ 复现与验证**：`scripts/pkg/_repro-viewconfig-gap.mjs`（拷 dist-pkg 进临时消费端 node_modules，模拟真实 registry 布局——**不能用 file: 链接**，tsc 会顺软链回引擎仓库报无关 TS2307）重跑，5 条 viewConfig 相关 TS2305/TS2724 全消；另建干净探针（只 `import type`、避开 react/jsx-runtime 噪音）在 `skipLibCheck: false` 下 **0 error**；`tsc -b` 干净 · `eslint .` 0 problems · 全量 **85 文件 1875 tests 全绿**。**④ 门禁补强（根治盲区）**：`scripts/pkg/publish-smoke.mjs` 新增第 4e 道 **strict 类型关卡**（`tsconfig.strict.json` + `smoke-ts-strict.tsx`）—— `skipLibCheck: false` + 构造 `CayleyViewProps`/`Cayley3DSceneProps`/`TableViewProps`（覆盖 viewConfig 一族）+ 显式 `import type` 那 5 个名字。原门禁因 `skipLibCheck: true`（跳过所有 .d.ts 语义检查）叠加「只构造 SetViewProps」而**完全看不见**此类缺口，此关卡专治。另加 `--log=<file>` 诊断开关（本机 PowerShell 工具 stdout 重定向不可靠，脚本自写日志文件）。`publish:smoke` 全绿（`rc=0`：core 冒烟 / react SSR / NodeNext + bundler / strict 四关全过）。**⑤ 版本**：2.2.0 → **2.2.1**（纯 fix，patch）—— 同步 `package.json` `version`+`pkgVersion`、`package-lock.json` 2 处、`welcome.version` zh+en（`src/i18n/translations.ts`）。**文档**：`feedback/INDEX.md` 台账状态 待处理 → 已修复（含修法摘要）、反馈文件顶部状态块回写（修法/验证/涉及文件）、本 CHANGELOG。 |
| 2026-09-12 | **v2.2.1 发布后 registry 验收（反馈 #3 闭环）+ 新增 `consume:types` 常驻门禁**。**① 验收四关全绿**：`consume:compare` —— registry tarball 与本地 dist-pkg **逐字节一致**（core `888414be…`/608117B、react `b63a55c9…`/263612B）⇒ 线上包就是测过的产物；`consume:registry` —— 真消费端从 registry 装 **2.2.1**，core 冒烟（6 群往返 + 字长球 + relaxEdgeLengths3D）/ React SSR / 宿主 tsc 全绿；**专项类型验证** —— `skipLibCheck:false` 下显式 `import type` **15 个 viewConfig 类型**，**总 error 0**（反馈 #3 缺口在线上包彻底消除，react 的 .d.ts 内部 import 亦全部可解析）；`consume:browser` —— 真 Chromium 点 TestPage `?test=1` 四个 VCL 示例 **18/18 全绿**、0 console error（D₇ 拖拽跟手比 1.00 / S₄ 哈密顿 23 步 / S₃ 直边控制点偏差 0.000 / A₄ 逐生成元 len=2.2）。**② 修掉一个静默误报**：`consume-npm.mjs` 默认版本号原**硬编码 `2.2.0`**（且取 `argv[2]`，受 npm run 的 `--` 分隔符影响下标偏移）⇒ 发版后跑验收实际测的仍是旧版本、新版本的缺口会被旧版本的「通过」掩盖（本次实测复现）。改为读 `package.json` 的 `version`（与 `compare-registry.mjs` 对齐），显式参数改为「argv 里找第一个 x.y.z 形态项」。**③ 新增常驻门禁 `npm run consume:types`**（`scripts/pkg/consume-types.mjs`）：从 registry 真实安装双包 → 构造纯类型消费端（只 `import type`，不碰 react/jsx-runtime，隔离类型缺口）→ `skipLibCheck:false` 跑 tsc 覆盖 15 个 viewConfig 类型 → **fail-closed**（任何 TS error 即 exit 1，报告写 `viewconfig-verify.txt`）。与 `publish-smoke` 第 4e 道 strict 关卡互补：前者查**本地 dist-pkg**、本关查**线上 registry 包**（真实 npm 安装后的 .d.ts 布局）。`ROOT` 由脚本位置推导（不再硬编码路径）。**④ 文档**：反馈文件顶部状态块补发布后验收结果、`feedback/INDEX.md` 备注补「v2.2.1 发布后 registry 验收全绿」（字节一致 + 真消费端 2.2.1 + 15 类型零错 + 浏览器 18/18）。`tsc -b` 干净 · `eslint .` 0 problems · 全量 85 文件 1875 tests 全绿。 |
