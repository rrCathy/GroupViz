{
  "name": "host-minimal",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "FGVE 阶段 3 最小宿主示例：JSON 载群 → 渲染视图 → 切形状 → 导出 SVG（消费 dist-pkg 双包产物）",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
